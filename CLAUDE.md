@AGENTS.md

# Claude Adapter

Shared process lives in `docs/**`; discover installed skills through
`.governance/runtime/skills/catalog.yaml` when that runtime state exists.
Use host-native delegation only when the operator requests it, and keep every role in the current
checkout unless the operator explicitly authorizes another worktree.
Keep Claude-specific files thin adapters only.
