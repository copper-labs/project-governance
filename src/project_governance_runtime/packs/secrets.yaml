id: secrets
label: Secret detection
implementation_status: active
enforcement: blocking
stages: [pre-commit, pre-push, pre-pr, ci-pr, release]
run_when: matched
impact_role: supplemental
path_globs: ["**"]
depends_on: []
commands:
  - builtin: secrets
