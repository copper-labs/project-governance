// Responsibility: Define a public Kotlin type with deliberately incomplete KDoc structure.
// Context: The fixture proves public types need both a summary and relationship context.
package fixtures

/** Provide stable normalized vocabulary entries that interpretation clients use across repository workflow boundaries. */
public class VocabularyCatalog
