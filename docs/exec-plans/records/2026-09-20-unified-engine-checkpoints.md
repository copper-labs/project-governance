---
id: exec-plans.unified-engine-checkpoints
title: Unified Engine Implementation Checkpoints
type: reference
status: current
owner: project-governance
created: 2026-09-20
updated: 2026-09-20
summary: Preserved implementation checkpoint history; current work belongs to the active plan.
---

# Unified engine implementation checkpoints — 2026-09-20

Historical observations, preserved verbatim below. Counts and pending statements apply to their
checkpoint, not automatically to current sources. The [active plan](../active/2026-09-20-unified-development-engine.md) owns remaining work.

Current checkpoint: E5 product-lifecycle integration and qualification. The compiled preview targets
major version 3.0.0. Native governance planning/checkers, durable execution, resource ownership,
provider dispatch, context selection, optional JEV and composed installation flows are implemented.
The latest integrated source suite passes 360 engine tests; detailed checkpoints below identify
the narrower package, recovery and device evidence and their source applicability.

RN iOS simulator and wired-device scenarios have passed on earlier preview artifacts. They do not
qualify the final package or establish measured reliability/turnaround improvement. JEV's frozen
four-case comparison found no incremental selection benefit over the improved lexical reference;
whole-workflow and token savings remain unproven. The existing installed product remains active.

Startup application now has source-proven commit hooks, task closure handling, coordinator ownership,
pre-activation cancellation, pre-write restoration, explicit post-write commit retry and a shared
installation deadline. Consolidated installed-package proof passes 76 focused tests, eight lifecycle
integration scenarios and fresh init/repair replay. Native automatic startup
wiring, remaining category/adopter parity and retirement, final workflow/CI scope qualification,
measured acceptance, major-version release readiness and the final Opus review remain open. A broken
post-write candidate requires deliberate runtime repair; commit retry is not candidate repair.
Existing dirty/concurrent work is preserved. Runtime receipts and adopter identities remain external.

Implementation note: C02/C14 pack configuration and pure selection now have native TypeScript
owners under `components/engine/src`. Nine retained Python output fixtures cover shipped-pack
selection; additional tests cover replacement ownership, gaps, dependency cycles and command-stage
coverage. Checker result normalization preserves finding lifecycle states and distinguishes check
failures from native execution and malformed output. The old runtime remains active until complete
cutover; these modules do not call Python. Immutable Git subjects, actual checker ports, hooks and
installed-product parity remain pending. The new loader rejects duplicate YAML keys and non-string
pack lists explicitly; audit adopter manifests before activation for these stricter schema checks.

C02 checkpoint: native TypeScript change-packet capture and validation-subject reads now preserve
staged blob identities, worktree digests, renames/deletes, symlink payloads and immutable-base reads.
A direct Python/TypeScript comparison produced identical full packets for staged and worktree fixtures
including Unicode paths. Five focused Git-backed tests cover dirty/index divergence, later edits,
unsafe paths, missing bases and immutable graph reads. Capture verifies two full snapshots rather than
combining status/ranges from one view with byte identities from another. Unsupported quoted hunk paths
fail closed; arbitrary-byte Git filenames and complete packet materialization still need qualification
before cutover. Checkout-wide all mode deliberately carries no immutable subject digest.

Checker migration checkpoint: formatting, commit-message, PR-description, prose and test-quality
logic now run natively in TypeScript. Formatting reads exact subject bytes and verifies preserved
notice provenance/digests. Thirteen retained Python narrative fixtures match the new commit/PR
validators. Prose and lexical test signals remain advisory; malformed or unreadable input remains
blocking after shared normalization. Command/file resolution and installed hook invocation still
need integration; these source-level ports do not establish full pack or product cutover.

Naming and secrets checkpoint: native naming analysis matches ten retained Python outputs for new
names and existing debt. Exact naming waiver scope/expiry is covered. Secret signature streaming,
per-image waiver union and shipped-schema validation are implemented and tested, including binary
input and matches spanning chunk boundaries. Git subject reads now disable replacement objects.
Secrets repository/index traversal and streamed process integration remain incomplete; this core
detector proof is not a claim that the complete secrets pack has migrated.

Secrets integration checkpoint: the TypeScript scanner now covers immutable narrow after-images
and the union of publishable worktree files plus exact index blobs. Regular-file scans stream through
no-follow descriptors; Git blob reads disable replacement refs and preserve process failure as a
blocking result. Integration tests cover staged secrets hidden by repaired working files/replacement
refs, changed after-images, binary files above the ordinary subject-buffer limit and final symlinks.
A direct full-output comparison matched the Python checker for the staged-secret/clean-worktree case.
CLI/default-policy resolution and installed hook qualification remain part of the later cutover.

Maintainability decision checkpoint: the TypeScript disposition owner preserves v1 migration
refusal, unique stable keys, reviewed cohesion acceptance, exact temporary waiver identity/metric,
expiry, explicit replacement/resolution and active-decision relocation checks. Tests cover these
core boundaries. Invalid registries grant no acceptance while reporting their blocking diagnostics.
The existing shipped schema is reused directly; AJV strictRequired linting is disabled only because
its valid required-only conditional branches rely on parent properties. Schema validation remains
active. Native declaration analysis and complete maintainability checker integration remain pending.

Changed-source unit checkpoint: native TS measurement now subtracts nested architectural extents,
limits function checks to directly changed ranges, preserves parser-free size review, and prevents
policy from raising the shared 500-line review ceiling. Pure renames trigger architectural review
without rescanning untouched function metrics. Focused tests, typecheck and compilation passed.
Native parser invocation/capability reporting and full checker assembly remain outstanding.

### Implementation checkpoint: captured TypeScript analysis

The native TypeScript analyzer now consumes captured source text directly and uses the
product's pinned TypeScript compiler. Runtime packaging therefore declares TypeScript
as a production dependency. Declaration extents and qualified names preserve the
existing checker behavior; JavaScript JSX now uses the correct compiler syntax mode.
Only declaration capabilities are advertised, with no invented complexity metrics.
This is the analyzer seam, not a completed maintainability checker or installed-product
qualification. Python and other language adapters, policy wiring, and CLI integration
remain outstanding. Validation: typecheck and compiled build passed; the integrated
engine suite passed all 62 tests, including captured-source parsing, JSX/TSX, and syntax
failure coverage.

### Implementation checkpoint: native parser dispatch and finding integration

Captured source now feeds source-unit findings, exact-byte disposition fingerprints,
symbol inventories, and explicit adapter coverage. Missing optional tools retain the
physical-size fallback; syntax and execution failures produce blocking findings and
cannot claim a successful symbol inventory. Shared findings omit native diagnostic
text because it may contain source literals.

Native parser dispatch covers Python AST, TypeScript, Swift, Kotlin, and ShellCheck.
Python supplies AST facts through an isolated interpreter; TypeScript owns governance
metrics and decisions. Other external parsers consume private captured-source scratch
files with deadlines and output bounds. Scratch cleanup runs after success or failure.
Kotlin failure handling is deliberately stricter than the old implementation: only a
recognized unresolved-reference diagnostic may bypass syntax rejection; arbitrary
compiler failures no longer pass as successful analysis.

Validation: typecheck, compiled build, and all 68 engine tests passed. Native Python
integration ran; Swift/Kotlin/ShellCheck failure handling used controlled process
results and is not real-toolchain qualification. Full checker policy/selection wiring,
CLI integration, installed-package proof, and adopter/device qualification remain open.

### Implementation checkpoint: maintainability checker composition

The TypeScript checker now joins exact after-image selection, ownership/ignore exclusions,
native measurements, policy validation, disposition application, and before-to-after
review-obligation checks. Configuration comes from the same validation subject as source;
unrelated dirty policy edits cannot change a staged result. Registry deletion remains an
invalid current configuration instead of silently becoming an empty registry.

Historical registries are retained as transition evidence without requiring today's
schema. This preserves the upgrade path from version 1 and repairs of older documents;
only the current validated registry can grant exceptions. Focused integration tests
exercise staged source followed by worktree repair, captured policy and registry edits,
version-1 history, registry deletion, and unavailable history. Five focused integration
and disposition tests plus typecheck passed. Command-line exposure and packaged default
schema/policy ownership remain pending; this checkpoint does not qualify the full product.

### Implementation checkpoint: composed source-comment checker

The TypeScript comment checker now composes registry/schema checks, exact-source selection,
Python AST and Kotlin token adapters, advisory fallback, fixture conformance, and scoped
waivers. Python shares the native AST bridge with maintainability. The Kotlin v6 declaration
contract matches all 11 shipped Kotlin fixture sources; this preserves the existing parser's
behavior rather than claiming compiler-level syntax coverage. Both language adapters pass
all active shipped comment fixture expectations. New declarations and changed signatures
remain enforceable while body-only edits retain advisory documentation debt.

The integrated test proves captured staged declarations remain the checked source after a
working-tree edit and executes both active adapters' fixture cases. Invalid waiver documents
cannot grant exceptions. All 82 engine tests and the compiled build passed. Packaged fixture
resolution, command-line/configuration plumbing, and installed-product qualification remain
pending. No simulator or physical-device qualification is claimed by these checker tests.

### Implementation checkpoint: dependency extraction and registry trust

Native TypeScript extraction now covers npm manifests, modern/legacy npm lock graphs,
Python requirements, pnpm catalogs/overrides, and GitHub Actions references. Npm entry
and workflow-reference defects retain repair identities for incremental remediation.
Scoped registry trust compares complete origins and decoded package paths. Npm/Yarn
configuration checks reject credential-bearing and transport-weakening settings without
including values in error messages. Lock entries require SHA-512 artifact integrity.

Eleven focused dependency tests, typecheck, and build passed. These are offline extraction
and trust checks, not release-age evidence qualification. Maven, workspace reconciliation,
evidence/override evaluation, full checker orchestration, and installed CLI proof remain
pending. The current implementation has not replaced the active Python runtime.

### Implementation checkpoint: dependency evidence and override validation

Dependency evidence now binds authoritative URLs to exact coordinates and evaluates
release ages with microsecond precision. Relevant duplicate records invalidate their
index entry. Overrides require operator attribution, exact release evidence, bounded
approval/expiry windows, and nonempty rationale, ownership, follow-up, and evidence.
Policy validation preserves the minimum 14-day release age and fail-closed behavior;
invalid registry configuration cannot populate the trusted registry map.

Typecheck, build, and all 100 engine tests passed at this checkpoint. Full dependency
checker composition still needs Maven extraction, workspace reconciliation, changed-entry
repair reconciliation, and result/CLI wiring. These tests do not establish host adoption,
local CI, simulator/device proof, or the full E5 release gate.

### Implementation checkpoint: remaining checker composition

Dependency composition now includes Maven XML, workspace reconciliation, captured
release evidence, and changed-entry repair reconciliation. Apple dependency checks,
documentation metadata/link/catalog checks, exact documentation routing, and optional
context-router validation have native TypeScript implementations. Context configuration
uses captured candidate files and the actual validation-pack IDs supplied by its caller.
Malformed token-budget mappings and explicit null budget values now fail instead of
silently selecting defaults; positive group limits and the runtime packet ceiling apply.

All 113 engine tests passed, including staged context configuration remaining stable after
later worktree edits. Typecheck and compiled build passed. Shared checker invocation,
installed package asset loading, and complete product activation remain outstanding.
These source tests do not qualify SDK adoption, simulator/device execution, CI authority,
JEV benefit, or completion of E5. The active Python runtime has not been replaced.

### Implementation checkpoint: shared built-in invocation and packaged proof assets

One TypeScript invocation boundary now dispatches the 13 standard pack checks. It supplies
captured policy inputs, explicit evaluation time, narrative inputs, managed paths, and
package-owned schemas/conformance fixtures. Missing narrative inputs and invalid policy
loads produce blocking findings without echoing parser/source contents. Target policy
overrides take precedence; absent policies use package defaults while selected deletions
cannot silently fall back. The dependency and maintainability compositions still require
reconciliation of their existing internal default loaders with this common policy boundary.

The compiled build now includes generated default and pack assets from their single authored
source. Asset lookup rejects traversal and target fixture aliases. All 115 engine tests passed
before the default-policy reconciliation; focused invocation tests and typecheck passed after
that change, and the compiled build passed. CLI planning/execution, KMP surface dispatch,
installed archive proof, and complete migration/adopter qualification remain outstanding.

### Implementation checkpoint: captured planning and policy defaults

Dependency and maintainability composition now receive packaged policy defaults while
preserving captured overrides and rejecting selected deletion. Target pack loading also
uses the candidate graph, so staged source and staged ownership cannot be mixed with
later working-tree pack edits. Repeated schema validation no longer collides on reused
schema IDs; regression coverage verifies that changed schema content still applies.

All 118 engine tests passed before adding the initial compiled CLI. The preview package
now declares a native Node executable with version and plan commands; unsupported commands
fail explicitly. Typecheck/build and direct compiled version invocation passed. Check execution,
remaining lifecycle commands, installed archive proof and host qualification remain pending.

### Implementation checkpoint: native checks and initial npm installation proof

The native CLI now executes selected built-in checks in dependency order. Ordinary
findings allow independent checks to continue; failed prerequisites block dependents.
Unsupported command adapters and invocation failures cannot be downgraded by advisory
pack enforcement. Custom command execution, narrative-file arguments, durable check-run
telemetry, and remaining lifecycle commands still need integration.

A locally packed 3.0.0-preview.1 npm archive was installed offline into a fresh temporary
prefix. Its executable symlink resolved correctly; version output worked; an all-mode
explicit format check passed clean source and returned exit 1 for trailing whitespace.
The temporary install was removed after proof. This proves that narrow installed path,
not complete v3 activation, adopter migration, or release readiness. The archive integrity
was sha512-v0Qr0ygVPAWURkxHitfinHRvLOojDW5qiTHMiam/pGfJ4bkArZtaw2VouUvyVUmT8/amTpC98V3y+7bX8eMMQA==.

### Implementation checkpoint: narrative hooks and lifecycle secret scope

Native check arguments now accept commit-message files and paired PR title/body inputs.
Narrative files are bounded ordinary UTF-8 files, with final symlinks refused, and commit
cleanup honors Git's comment character. Commit-msg selection works before the first commit
without requiring HEAD or upstream. Built-in pack argument bindings are validated and
supported. Pre-push, pre-pr, ci-pr and release secret checks preserve full repository/index
coverage even when the candidate selection is narrow.

Focused tests cover invalid narrative inputs, comment cleanup, initial-repository hook
execution, built-in argument dispatch, and lifecycle secret coverage. Typecheck and build
passed. Custom command adapters, durable check-run records and full installed host lifecycle
remain pending; no simulator/device or SDK-adoption claim follows from these checks.

### Implementation checkpoint: native command result boundary

The existing durable process owner now retains separate stdout/stderr files under its
shared byte limit, alongside the combined operational log. Native checker normalization
preserves nonzero exits, timeouts, cleanup uncertainty and malformed output. Command
argument parsing preserves quoted legacy declarations without shell expansion; placeholders
replace whole tokens only and missing inputs fail before dispatch. Captured change packets
can now materialize exact before/after bytes with the established per-image and packet hashes.

The 126-test engine suite passed; a subsequent focused packet test also passed. Typecheck
and compiled build passed. These components are not yet wired to custom pack execution in
the public CLI; durable run retention, evidence manifests and full host/device proof remain
outstanding. No additional worker is left running by this checkpoint.

### Implementation checkpoint: custom pack execution and durable check records

Custom validation packs now execute through the native CLI and durable process owner,
with bounded timeouts, literal argv, captured source packets, isolated per-pack evidence
directories and normalized process results. Run intent and terminal results are stored
outside source trees. Process/integrity failures cannot become advisory passes. Optional
manifest summaries are checked for duplicate keys, schema shape and matching source digest;
artifact references remain inert. Null-prototype JSON maps now serialize canonically so
planner maps can be persisted without changing their meaning.

All 129 engine tests passed, as did typecheck and build. A compiled CLI custom-pack run
passed with a durable run ID. Integration proof verifies staged packet bytes after a
working-tree edit. Whole-run reconnection/recovery, pack-final manifest aggregation,
retention/telemetry lifecycle, installed custom-pack proof and adopter qualification remain
outstanding. A lost CLI observer does not yet resume the remaining pack sequence; native
command receipts survive, and no automatic replay is authorized by this checkpoint.

### Implementation checkpoint: pack-final evidence and read-only run observation

Evidence manifests are now inspected once at pack completion, allowing multiple commands
to assemble a manifest without treating an intermediate draft as terminal proof. A real
native two-command regression verifies final source binding and successful aggregation.
The CLI now exposes `check-status --run <id>` to inspect durable results or unfinished
native commands. It validates run identity and distinguishes a live orchestrator from an
incomplete run using the recorded process fingerprint. Missing terminal evidence never
becomes success and observation never replays a command.

Typecheck and build passed. Observation tests cover live, incomplete, terminal and
mismatched-identity cases. This is read-only reconciliation, not automatic continuation of
an interrupted pack sequence; whole-run recovery and the remaining E1–E5 qualification
requirements are still open.

### Implementation checkpoint: detached check-sequence ownership

The native CLI now dispatches the complete check sequence to one detached worker and
observes it by run ID. `--detach` returns the identity immediately; ordinary invocation
waits on the same worker. Dispatch persists its bound inputs and worker identity before
launch, and an exclusive worker claim rejects duplicate sequence execution. Existing
managed-path and fixture-proof options survive worker serialization. A stopped worker
still requires reconciliation; no automatic relaunch occurs on lost acknowledgment.

A real subprocess regression proves both commands finish after the submitter exits and
a duplicate worker invocation does not repeat either effect. Typecheck and build passed.
This establishes caller-disconnection survival, not recovery from a killed worker or
qualification of the SDK, simulator, physical device, CI, JEV benefit or full v3 activation.

### Implementation checkpoint: detached run cancellation

`check-cancel --run <id>` records an attributable cancellation request. The check worker
stops admitting later packs/commands and forwards cancellation to the active native owner;
only that owner signals its process group. Terminal run output remains failed with an
explicit cancellation reason, retaining native cleanup evidence. Built-in checks currently
observe cancellation between invocations rather than interrupting synchronous analysis.
Run receipts now carry an explicit kind/version and elapsed duration.

A live subprocess test verifies owner-confirmed cleanup and that the following command
never executes. Focused validation, typecheck and build passed before the receipt-envelope
addition. Cancellation does not provide hard-killed-worker recovery or establish device,
adopter, CI, or release qualification; those requirements remain open.

### Implementation checkpoint: first-adopter baseline execution started

A separate committed-source worktree now owns the initial RN iOS baseline preparation.
Installed npm trees were cloned with workspace-local links rebound; this is preparation,
not dependency qualification. The checkout received its exact existing locked runtime,
and the project-owned dev-loop preflight was submitted through that runtime's external
batch mechanism with completion directed to the initiating host task. Runtime evidence,
source identity, worktree location and job identity remain outside this repository.

The existing active adopter checkout retains its unrelated changes and runtime. No
simulator build/launch or physical-device test is yet qualified. Baseline outcomes must
be assessed before freezing comparison thresholds and beginning candidate comparison.

### Implementation checkpoint: compact native check telemetry

Native check completion now emits a compact metric projection bound to the terminal
result digest. `telemetry status` summarizes the current workspace's native check outcomes,
cancellations, elapsed-time samples, pack/command counts and unfinished/unprojected runs.
Stage/time filters and bounded scans expose their limits. The report reads compact records,
not command logs or source packets. Missing metric projection does not undo authoritative
terminal execution evidence. Model-token coverage remains unavailable and benefit remains
unevaluated until the planned native-usage and baseline comparisons supply actual evidence.

Focused tests cover filtering, incomplete coverage, timing aggregation and explicit unknown
usage. Typecheck and compiled build passed. Retention, operator review records, old-runtime
telemetry migration and cross-provider usage correlation remain outstanding.

### Implementation checkpoint: bounded context consumer and paired evaluation

The compiled preview exposes `context-packet` and `context-evaluate`. Explicit source paths
are captured with content hashes; required entries bypass ranking, optional entries share a
byte budget, and invalid or unavailable advice preserves deterministic order. Packet receipts
store hashes, selected/omitted IDs and decision metadata outside the checkout without copying
source excerpts. Required paths remain caller-supplied: this command does not replace the
mandatory context router or establish complete source discovery.

Both commands load `continuity.decisions` from the project profile unless an explicit decision
configuration file is supplied. Absent configuration remains off. The profile uses `provider`,
`mode`, `config_revision`, `model`, `allowed_questions`, `allowed_data_classes`,
`allowed_source_paths`, `deadline_ms`, `evidence_bytes`, `max_candidates` and
`minimum_confidence`. Explicit source-path patterns are required in addition to source-class
approval before the adapter transmits source candidates. Symlink aliases are rejected by the
file-capture command. The captured-profile configuration check validates decision settings.

The paired evaluator freezes its input dataset before calls, separates absent candidates from
selection errors, and records per-case regressions, delivered bytes and native provider usage.
Shadow replay scores captured suggestions separately without changing delivery or calling the
provider twice. Unknown token usage remains unknown; byte counts and shadow quality do not
establish development benefit or host-token savings.

Focused tests cover provider-free operation, optional selection under auto/shadow modes,
required-context preservation, invalid/stale answers, source scope, profile defaults and invalid
settings, source-free receipts and evaluation accounting. Typecheck passed. Representative held-out
comparisons, authoritative routing, bounded excerpts/indexing, stale-source reread, doctor/schema
integration and complete lifecycle/cancellation qualification remain open. This checkpoint does
not complete E3 or authorize provider-enabled adoption in any target project.

### Implementation checkpoint: workflow CLI and project operation catalog

The compiled preview now exposes workflow submission, observation and cancellation against
an existing authorized task action. Pending and unsuccessful states have distinct nonzero
shell exit codes. Submitted recipes select project catalog operations rather than carrying
executable definitions. A stable catalog read supplies both resolved commands and a source
hash checked by the stage executor. The catalog is project configuration; its filename does not establish approval. A trusted
host adapter now binds each authorized action to the exact resolved workflow before submission.
Ledger schema 2 preserves prior records without automatically approving old runs. Focused
negative cases reject unbound actions, changed commands and reuse for a second run identity.

Focused checks cover catalog selection, injected/missing operations, changed catalog input,
symlink rejection, CLI option boundaries, status/cancellation and shell outcomes. Existing
executor checks cover dependency failure, resource cleanup and cancellation. Typecheck and
compiled build pass. These checks do not replace the pending actual simulator workflow and
physical-device qualification.

### Implementation checkpoint: deterministic context and skill composition

The source preview now routes mandatory context and selects attached skill leaves from
explicit task signals and target facts. Skill selection matches the existing Python selector
across four task/fact combinations against the actual package catalog. Route path matching
retains the context router's fnmatch behavior, including required directory separators.
Missing facts, unavailable declared skills, evaluation-only declarations and conflicts remain
visible blockers. Conflicts include explicit declarations as well as automatically selected leaves.

Materialization preserves the separate 16,000-byte skill ceiling. Required skills take priority
over recommendations; required omissions prevent readiness. Package-owned skill bytes come
from the canonical package catalog rather than replaceable target copies. The catalog remains
authored in its existing source location pending the single-product asset cutover.

Eleven focused checks and TypeScript validation pass. This is a source-level composition
boundary, not a completed command or installation migration. CLI integration, packaged asset
delivery, target-owned skill resolution, persisted packet/reference handling and final installed
proof remain outstanding. No device batch was restarted for this work.

The source CLI now exposes `context-route --task <text> --revision <revision>` with optional
`--staged`, `--base-ref`, `--include-expansion` and `--include-evaluation-skills`. It reads captured
profile/facts configuration, rejects mismatched profile identities, and delivers bounded required
files and canonical skills without a provider call. A blocked packet exits with status 2. Compact
receipts retain source identities and omissions outside the checkout without source contents.
The packaging script includes generated skill assets from their existing canonical source.

A command-level test proves staged policy isolation, required skill delivery, missing-skill
blocking, profile identity rejection and source-free receipts; typecheck passes. Compiled artifact
verification is deferred while an already submitted device qualification uses that artifact.
The explicit optional-ranking command remains separate; composing its advice with routed packets,
target-owned skills, references and activation remains unfinished.

`context-route` now also accepts `--optional-path` and `--discover-path`. Optional ranking uses
the captured profile's decision configuration and only the remaining expansion/total byte
allocation. Mandatory route paths never enter the provider request. Blocked mandatory context
prevents provider calls; advice failure retains deterministic order. Every optional source,
including omitted candidates, is revalidated after advice. Drift refuses delivery while retaining
the source-free decision/usage receipt. Tests cover these paths with a controlled provider and
continue to report token savings as unknown. This connects the consumer; representative held-out
JEV benefit qualification and installed-artifact proof remain required.

### Implementation checkpoint: installation generation coordination

A transactional installation registry now tracks the selected staged generation and active
readers. Switching requires the expected revision and no readers. Pre-write rollback verifies
the prior installation; a recorded write requires forward repair instead. Reader ownership is
explicit and never expires automatically. This registry coordinates installation, not task policy
or execution authority.

The managed invocation API verifies the selected installation and retains its reader until the
child process closes. Commands that may write mark the generation before dispatch. Detached
submission remains refused by this API until worker-lifetime integration is complete; the existing
preview submission path is unchanged. Installation tests cover competing registry connections,
stale revision, reader ownership, pre-write rollback, post-write refusal and installed invocation.
Typecheck passes. Public lifecycle commands, crash reconciliation, coherent backup, legacy-job drain
and final lock/launcher cutover remain unfinished; no adopter was activated by these tests.

Detached check and workflow dispatch now retain their own installation readers before launch.
Reconnect reuses the committed workflow request; it does not reserve another reader. Native terminal
completion releases readers only when execution and cleanup are resolved. Reservations atomically
mark possible writes, and unknown workers keep their obligation. Focused tests verify reader retention
after submitter exit and release after native completion for both paths. Managed invocation now
supports these submissions; the earlier refusal described above is superseded.

Installation protocol 2 adds durable maintenance ownership. Admission stops while existing readers
drain; closing/reopening the registry does not clear maintenance. Explicitly declared database/file
backups require that drained state. SQLite snapshots include WAL contents; file copies, database
integrity and source identities are checked before publishing durable receipts. Earlier inputs are
rechecked after later copies. Failed backups retain failure evidence and maintenance. Legacy writers
and completeness of the declared migration inventory remain the migration coordinator's obligations.

Backed activation verifies receipt payloads, the maintenance interval, current generation/revision and
the exact declared input set before switching. It retains maintenance for readback. The staged-package
integration test covers backup, rejected scope, activation and stale-backup rejection. This does not
yet update the target product lock/launcher, restore snapshots, or close interrupted-update recovery.

The lock/launcher API path now replaces only backed-up unchanged files, verifies exact readback,
and recognizes already-applied changes. Generation activation identity is committed atomically
in installation protocol 3, so a repeated request recognizes its earlier switch. Finalization runs
an exact version-only probe through the generated launcher while maintenance remains owned,
then reopens admission after successful readback. Managed invocation checks the project lock
against the selected installation before execution.

An actual compiled npm archive was staged offline and activated in an isolated temporary project
through backup, lock replacement, launcher replacement and finalization. Normal launcher version
invocation then passed with no retained readers or maintenance. Evidence stays outside the source
checkout. This qualifies that preview artifact's activation path, not adopter migration, immutable
release-source identity, full init/update/repair UX, state restoration or all E5 requirements.


### Recovery permissions checkpoint

Pre-write recovery now covers both a previous compiled generation and a first-cutover legacy
wheel launcher. Legacy recovery validates the saved lock and requires exact launcher version
readback before clearing compiled selection. This has fixture coverage; it does not establish
legacy wheel integrity, legacy writer drain, or an adopter cutover.

Backup receipt version 2 records original file permissions and rejects unsupported special bits.
Recovery restores those permissions independently of the process umask and checks them on replay.
Backup also rejects permission drift during capture. Older preview receipts lacking permission
metadata are rejected rather than guessed. Focused backup/recovery tests and TypeScript typecheck
pass. Full lifecycle orchestration, installed recovery proof and remaining E5 requirements remain
open.

### Packaged transition completion checkpoint

The preview `runtime-complete` command coordinates backed activation, lock replacement, launcher
replacement and native readback. It requires an existing owned, drained maintenance interval and
an exact declared backup scope; it does not establish migration inventory or legacy drain itself.
Failures retain maintenance. A retry verifies the original activation identity, resumes incomplete
steps and reuses completed finalization without selecting another candidate. Changed authored files
remain protected.

Focused integration coverage exercises interrupted completion, retained maintenance, reconnect,
changed candidate and changed scope refusal. Typecheck and build pass. A newly bundled artifact
also completed this path through its installed CLI in an isolated fixture, including identical retry,
ordinary launcher readback and zero remaining readers/maintenance. Temporary installation cleanup
completed. External proof retains artifact identity; source remains an uncommitted preview, not a
release-source claim. Public init/update/repair orchestration, inventory/drain and adopter cutover
remain outstanding.

### Native Git hook entry checkpoint

The compiled CLI now exposes `hook commit-msg|pre-commit|pre-push|pre-pr`. Generated thin shell
launchers call only the installed runtime and preserve literal arguments and exit status. The core
fixes each hook's selection boundary: staged pre-commit, explicit commit-message file, branch-aware
pre-push and only the all-subject PR-description pack for pre-PR. Pre-PR resolves explicit inputs,
provider environment inputs or Git worktree-local draft files. Managed invocation classifies hook
checks as writes so their durable evidence prevents unsafe pre-write rollback.

Focused tests cover argument safety, missing runtime, invalid inputs, selection boundaries and linked
worktree draft isolation. Build/typecheck pass. Compiled CLI commit-message and pre-PR execution also
passed through detached check workers in an isolated repository, with temporary state removed.
The first proof setup used the wrong fixture field and failed before dispatch; the corrected setup
used the fixture's actual text field. No existing hooks or user Git configuration were replaced.
Managed hook installation, authored-hook reconciliation and native startup migration remain open.

### Managed hook installation checkpoint

The preview CLI now supports hook inspection and explicit apply/configure. Installation preflights
all four destinations and Git hook configuration, preserves unknown authored hooks (including Git's
default hook directory), refuses symlinks and supports exact prior-template replacement through the
migration API. Matching hooks are reused; missing executable bits are repaired. Partial interruption
can be retried against the observed state. Linked worktrees without worktree-local configuration
cannot silently change their siblings' hook selection.

Seven focused hook tests pass, including execution through `git hook run`, custom-hook preservation,
mode repair, repeat installation and worktree configuration isolation. Typecheck/build pass; compiled
CLI preview, apply/configure and repeat apply passed in a temporary Git repository. No active checkout
hooks or Git configuration were changed. Integration with the coordinated migration backup/recovery
and verified prior-template inventory remains open; this checkpoint does not close C05/C12 or E5.

### Bounded check analytics checkpoint

Terminal checks now project compact metrics into a per-workspace SQLite analytics store with
1,000-record and 1 MiB payload retention limits. Retained run identities deduplicate; conflicting
observations are rejected. Eviction totals remain visible. An independent database page ceiling bounds
physical growth. The projection accepts only declared measured fields, not arbitrary source/log
content. Unknown schema, lock contention, invalid input or storage failure cannot change the execution
result or dispatch another command.

Operational run results, command evidence and frozen evaluation material are not deleted. Existing
per-run projections remain readable during migration. Telemetry status exposes the rolling store's
availability, retention and eviction counts, with rejected-write count and complete write coverage
explicitly unknown. Deduplication applies within the retained window; this is not an unlimited ledger
of every historical observation.

Six focused tests pass across projection retention, identity conflict, locked/unknown storage,
telemetry reporting and actual check/worker execution. Typecheck passes. C13 still needs old telemetry
migration and common decision/provider usage reporting; this checkpoint does not establish measured
benefit or complete telemetry coverage.

### Decision observability checkpoint

`telemetry decisions` reports bounded operational context receipts: delivered/blocked/stale outcomes,
JEV versus baseline selection, fallback reasons, latency and native input/output token samples.
Unknown token observations stay null; stale deliveries still contribute their observed provider cost.
It does not scan frozen evaluations, expose source/receipt contents or infer avoided LLM tokens.
Count/byte limits, scan truncation and nonrepresentative selection are explicit. This diagnostic
supports investigation; it is not a held-out benefit evaluation or a complete common event projection.

Check analytics now read retention counters and records within one SQLite snapshot, validate stored
row identity against payload, and check schema after acquiring the write transaction. Focused tests
cover corrupted row identity, partial token coverage, stale outcomes, disabled-provider observations,
malformed receipts and symlinks. Build/typecheck and focused checks pass. E3 benefit qualification,
common provider telemetry and the remaining migration owners stay open.

### Native KMP graph validator checkpoint

The opt-in `kmp-surface-validation` built-in now has a native implementation for bounded graph/catalog
reading, shared/projection/target composition, shallow versus guarded areas, target-local claims,
explicit implementation/proof gaps, duplicate identities and deterministic capped findings. It consumes
one captured validation subject and does not execute proof or interpret references as successful tests.
No standard pack was enabled automatically.

Six existing Python scenarios were exercised with each live result compared against the native
validator: complete shallow/guarded areas, target-local proof, new targets and declared gaps,
invalid shallow proof gaps/applicability, projection membership and deterministic bounded findings.
All six produced identical envelopes. A native fixture also passed staged-versus-live reference
isolation, missing proof and duplicate-key checks. Build/typecheck pass. Full structured-document
edge-case parity (including YAML alias/merge behavior), doctor wiring and installed opt-in pack proof
remain required before C10 cutover; this is not category completion.

### KMP doctor wiring checkpoint

`doctor --capability kmp-surface-validation` now diagnoses the opt-in capability without claiming a
full installation doctor. It checks exactly one active conventional invocation, graph/catalog path
selection, structural references and route-local KMP skill enablement. Disabled capability produces
no KMP configuration requirement. Structural inspection iterates declared target routes instead of
expanding every missing target obligation; implementation/proof gaps stay in the validation check.

All three existing Python doctor scenarios produced identical native string findings, including
adopter-defined lifecycle stages, default-only skill rejection, absent-target suppression, duplicate
invocations and incomplete selectors. The native integration fixture and typecheck pass. YAML edge-case
parity and final installed opt-in pack proof remain open before C10 acceptance.

### KMP document and installed-package checkpoint

KMP authorities now share a bounded YAML 1.1/JSON decoder. It expands ordinary aliases and merges,
rejects duplicate fields after merge expansion, rejects unsafe key coercion and recursive aliases,
and enforces document byte/depth/string/collection bounds plus an expansion ceiling. Non-string keys
are rejected at decoding rather than later graph-field validation. Native tests cover YAML 1.1 boolean
resolution, ordinary merges, duplicate merges/keys, JSON syntax, recursive/expanding aliases and limits.

The fresh compiled package was installed offline with an empty npm cache into a temporary environment.
Its opt-in KMP pack passed a complete guarded graph, failed after target-local proof was removed, and
its structural doctor continued to pass that graph. Package identity and command outcomes are recorded
externally; temporary installation, workspace and terminal run evidence were cleaned up. Build and
focused tests pass. This qualifies the declared installed KMP flows, not physical device proof or
immutable release-source identity. Full migration acceptance and final independent review remain open.

### Installation diagnostics checkpoint

The preview `doctor --registry <path>` (or `doctor` from a managed invocation) inspects the compiled
project lock, selected artifact/dependency identity, exact launcher and executable mode, hooks,
maintenance state and outstanding reader count. Its result explicitly names the compiled-installation
scope; KMP wiring remains separately available through the capability doctor. It neither executes a
candidate nor releases readers or maintenance, and reports that no execution readback occurred.

Registry access is read-only and does not create or migrate missing/older state. Tests prove that an
interrupted maintenance registry remains byte-for-byte unchanged, that a verified staged/activated
installation with hooks passes, and that subsequent launcher drift fails. Focused tests and build pass.
This adds diagnostics, not forward repair or complete adopter/configuration migration qualification.


### Integrated engine checkpoint

The engine suite passes all 197 tests after the installation, hook, telemetry and KMP additions.
The first run passed 196 tests and exposed a cancellation-fixture cleanup race: terminal command
results precede the detached worker's advisory projection and final reader release. The fixture now
waits for the exact worker process fingerprint to disappear before deleting its temporary directory.
Cancellation and no-second-command assertions remain unchanged. The focused cancellation test and
full engine rerun pass; residue from the failed fixture was checked for open files and removed.
This is engine integration evidence, not a replacement for outstanding adopter/device, migration,
measured-benefit, final review or release acceptance.

### Developer documentation command checkpoint

The existing native exact catalog resolver is now exposed as `docs route --capability ...` or
`docs route --symbol ...`, with `--json` and the established compact text response. It preserves
matched, disabled, not-found, ambiguous and invalid states; invalid configuration fails while an
ordinary absent/ambiguous route remains a reported non-error. Managed invocation treats this routing
command as read-only. It returns authored paths without loading their documents or using a model.

The compiled command resolved this checkout's live test-execution capability and owning references.
Focused CLI/catalog tests cover aliases, symbols, partial-string refusal, disabled research policy,
ambiguous ownership, unsafe paths and mutually exclusive arguments. Typecheck/build pass. Optional
documentation initialization and complete command migration remain open.

### Documentation initialization checkpoint

The compiled `docs init [--dry-run]` creates the neutral human entry, empty catalog, guides and reference
folders, and enables the optional module in the existing profile. It preserves existing authored files,
comments, custom roots, research policy and explicit opt-out. All declared targets are preflighted;
conflicting file types or symlinks prevent writes. Interrupted create-only work can be retried. Profile
replacement rechecks captured bytes and does not reformat existing authored text. Flow-style or
explicitly terminated profile YAML that cannot accept a raw appended section is rejected before writes
rather than producing invalid YAML; an already configured documentation section works normally.

Four focused initialization/routing tests and build pass. The compiled CLI also passed dry-run,
initialization, repeat initialization and empty-catalog lookup in an isolated temporary repository.
Managed invocation marks documentation initialization as a possible write while retaining read-only
routing. This does not close provider/startup migration, complete installation UX or E5 acceptance.

### Declared-scope forward repair checkpoint

`runtime-complete --forward-repair` now requires a verified backup taken after compiled-runtime writes
and an explicitly declared operational SQLite database. It completes the replacement runtime under
owned maintenance, then verifies preserved file bytes/modes and fresh SQLite snapshots against the
current backup before finalizing admission. Cross-input identity/data-version checks detect changes
during readback. It never restores task databases or artifacts. A preservation digest is committed
with finalization so reconnection verifies the same repair rather than substituting an ordinary
activation acknowledgment.

Integration coverage creates post-cutover task evidence, proves rollback refusal, injects concurrent
artifact drift and verifies that repair retains maintenance and the changed evidence. Resetting only
the synthetic test input allows the same repair to resume, preserve task rows and finalize exactly
once. Focused tests, typecheck and build pass. This proves declared-input preservation, not complete
inventory discovery, actual schema-forward compatibility, legacy writer drain, adopter defect soak,
or final installed repair qualification; those requirements remain open.

### Physical functional qualification in progress

The next E4 proof runs the adopter's canonical physical-device two-exercise capture scenario through
the compiled workflow engine in the isolated qualification checkout. An explicit dedicated bundle
identity keeps the existing app installation separate. The canonical runner retains its legacy device
lock and scenario assertions; the engine separately verifies the selected device and exact app process
absence before releasing its claims. The external batch requires cleanup acknowledgment and returns
completion to the initiating task. Runtime identifiers and evidence remain outside this checkout.

This run is pending assessment. It uses an embedded bundle and therefore cannot establish Metro
recovery. Legacy/new lock unification, complete immutable build identity and the remaining E4
failure/recovery flows are still required even if the canonical functional scenario passes.

### Repair admission correction

A failed preservation readback could previously be bypassed by ordinary transition completion or
manual maintenance release. Installation protocol 6 now persists the repair's preservation requirement
before replacement work. Finalization checks it atomically with admission reopening, and direct
maintenance release refuses pending repair requirements. Reconnection must retain the same declared
preservation digest. Existing protocol 5 maintenance ownership survives migration.

The forward-repair integration test now exercises both rejected bypass paths before successful repair
resumption. A focused migration/reconnection test and the existing backup, doctor and launcher tests
pass, as does typechecking. Compiled distribution rebuild and installed proof are deferred until the
ongoing physical batch finishes, keeping that batch's runtime payload stable.

The same durable repair requirement also blocks pre-write recovery and direct generation rollback.
Replacement activation resets the new generation's write flag, so that flag alone cannot distinguish
an ordinary pre-write update from an interrupted post-write repair. Recovery now checks the recorded
repair requirement before restoring installation files. Integration proof verifies unchanged launcher
bytes after refusal, retained maintenance and successful subsequent repair. The focused migration and
recovery tests plus typechecking pass.

### Physical functional qualification assessed

The isolated physical-device canonical capture scenario passed through the compiled workflow engine.
Evidence records three counter events, a completed result, both exercise steps and the intervening
rest transition. The dedicated app process stopped; independent device observation binds its absence
to the exact approved workflow and both released resource claims. Batch evidence and its announced
digest agree. Total batch duration was approximately 159 seconds.

This closes the pending canonical physical capture assertion run above. It remains declared-scope
evidence, without full immutable build identity, Metro recovery, disconnect/lifecycle, UI/acoustic or
measured development-efficiency claims. Runtime-specific receipts remain external.

### Installed forward-repair checkpoint

A freshly packed, dependency-bundled preview passed isolated installed-CLI activation and forward
repair. The fixture first creates post-cutover operational evidence, backs it up under maintenance,
then introduces artifact drift. Repair selects the replacement but refuses finalization; ordinary
completion also refuses and maintenance remains held. After resetting only the synthetic fixture
artifact, repair succeeds, reconnect returns the identical receipt, task evidence remains present,
and the installed launcher returns its expected version. Temporary installations are removed.

The initial verification script expected internal exception text that the CLI deliberately masks;
it was corrected to assert the native failure status and persisted replacement/maintenance state.
No product assertion was weakened. Archive identity and proof receipts remain external. This is
installed fixture proof using the same package in a replacement directory, not schema evolution,
legacy writer drain, complete adopter migration or a released immutable version.

### Decision configuration diagnostics

`doctor --capability decisions` now reports optional JEV configuration without network requests,
credential validation or health-store writes. It distinguishes off, missing token, disabled questions,
disabled sharing and absent source scope from configuration eligible for a bounded provider attempt.
Missing configuration remains a healthy provider-free default; malformed policy fails diagnostics.
Token output is presence-only. Eligibility is explicitly not provider health or measured benefit.

Focused coverage exercises defaults, missing credentials, source restrictions, enabled configuration,
malformed policy, secret omission and a network-call trap. Typecheck, build and compiled-command
readback pass. Representative JEV comparison and the remaining E3 acceptance obligations stay open.

### Integrated repair and decision diagnostics checkpoint

All 204 engine tests pass after the repair-admission, documentation initialization and decision
diagnostics additions. Typechecking passes. Decision configuration now requires a bounded nonempty
text revision and an actual model-name string, rejecting numeric/object revisions and coercible
model arrays before constructing a provider. Focused decision coverage includes these malformed
profiles along with provider-free fallback, sharing restrictions, cooldown, cancellation and usage.
This is core integration proof; it does not close the outstanding E3 comparison or adopter migration
and recovery acceptance requirements.

### Context comparison baseline correction

The context evaluator now reports per-case wins, ties and losses against both deterministic order
and lexical matching, separately for delivered and shadow selections. A selection win over fixed
ordering that merely ties lexical matching is no longer hidden behind aggregate totals. Tests also
exercise a candidate that ties fixed ordering but loses to lexical matching; absent shadow advice
produces zero shadow comparisons. Four focused evaluation tests and typechecking pass. These are
selection counts, not statistical significance or development-benefit claims; representative frozen
cases and workflow cost measurement remain required for E3 qualification.

### Source-derived JEV screen

A frozen eight-case screen used actual excerpts for recurring hook installation, forward repair,
physical cleanup, context selection, telemetry retention, documentation setup, provider fallback and
backup questions. Candidate excerpts, owner-file labels, sharing scope and thresholds were frozen
before live shadow calls. JEV selected all eight labeled owners; lexical matching selected six.
The two incremental selections concerned telemetry retention and provider fallback. No lexical
selection regressed, and maximum observed provider latency was approximately 456 ms.

The screen met its predeclared threshold of two incremental selections, zero losses and request
latency below five seconds. Native usage reports 69,833 input and 1,400 output tokens across eight
requests. This supports further evaluation, not adoption or a token-savings claim: candidates and
labels were curated by the implementer, decisive code may lie beyond initial excerpts, and no host
reading/rework or enabled-workflow cost was measured. Independent task-derived cases and the enabled
comparison remain open. Frozen inputs, provider configuration and receipts remain external.

### Provider-free context baseline improvement

The JEV adapter now uses the evaluated lexical ordering as its deterministic optional-context
fallback, including off, missing-token, shadow and provider-failure paths. Ties preserve discovery
order. Diagnostic and intent advice retain their existing baseline; mandatory policy routing remains
unchanged. Receipts identify lexical-context-1 versus discovery-order-1 so future comparisons can
distinguish the baseline change. Historical frozen evaluation receipts are not rewritten.

All 26 focused decision/context tests pass, including source freshness, routing and packet behavior.
Additional assertions verify stable ties, other-question ordering, zero-network fallback and the
recorded baseline version. Typechecking passes. This makes the provider-free path stronger without
claiming new JEV benefit or changing its opt-in configuration.

Operational decision telemetry now separates lexical-context-1, discovery-order-1, unspecified older
receipts and unrecognized future baselines. It does not infer the baseline of historical receipts or
copy arbitrary version strings into reports. The existing token/latency coverage and evaluation-data
exclusion remain intact. Focused telemetry tests and typechecking pass.

### Host instruction merge parity

The native managed-section merger preserves authored prefix/suffix bytes, replaces only the existing
harness-delegation markers, refuses malformed/duplicate markers and leaves whitespace-only host
overrides inactive. Live comparison fixtures use the current Python merge function as the expected
owner, including mixed newline text and repeat application. Focused parity tests and typechecking
pass. This is the merge primitive only: filesystem alias/ownership checks, transactional installation
and usable native provider targets must be wired before host instruction cutover. No adopter host
instructions were changed.

Host-instruction preflight now resolves local aliases as a group, deduplicates shared destinations,
retains authored bytes/modes in proposed writes and rejects external/managed-state destinations,
dangling aliases, invalid UTF-8 and oversized/non-file entries. An active entry that aliases an empty
override is rejected before any writes. The planner is read-only; focused filesystem tests and
typechecking pass. Applying these plans still requires integration with maintenance/backup and
provider readiness; this does not install unsupported provider entrypoints.

### Provider completion reconciliation parity

Native provider completion reconciliation now validates the structured answer and combines it with
observed tool states, required tool/category evidence and unresolved permission denials. A model's
reported checks cannot substitute for native tool observations. Remaining work, missing tools or
nonterminal operations prevent success. Live Python comparison fixtures preserve the existing
completion semantics, including recovered denials and successful tool/category matching. Focused
parity tests and typechecking pass. Native stream identity, command adapters, lifecycle integration
and host completion delivery remain open; no provider job was launched for this parity work.

The native provider session boundary now checks exact requested model/session/permissions and any
reported effort before tool admission or completion. Missing effort remains unknown rather than
being invented. Duplicate initialization/completion, session/model drift and premature termination
are rejected; completion input is captured rather than retaining a caller-mutable reference. Four
focused session/completion tests and typechecking pass. Wire adapters must propagate these failures
to their shared process owner; this class does not itself launch or supervise provider processes.

### Claude native stream migration checkpoint

The native Claude stream adapter now binds initialization and later model/session observations to
the session gate, tracks tool operations and recovered permission denials, captures native usage and
reconciles structured completion. Invalid identity or wire evidence makes the stream permanently
failed. Public text is bounded; private reasoning deltas are ignored. Unknown tool results, missing
required evidence and premature terminal states cannot produce success. Three focused stream tests
and typechecking pass. Command construction, byte-stream framing, process-owner integration and
Codex/Gemini adapters remain open. No live Claude invocation or final review occurred.

Native provider JSON framing now preserves records across arbitrary pipe/UTF-8 splits, enforces a
16 MB per-frame bound and rejects malformed encoding/JSON or truncated final records permanently.
Coalesced complete records are bounded individually. Byte-split tests, invalid/truncated/oversized
cases and a framed Claude completion-to-evidence-gate test pass, as does typechecking. This remains
a transport component pending shared process-owner integration; it creates no second supervisor.

### Shared process-owner stdin support

The existing detached command owner now accepts an optional bounded 500 KB stdin assignment. It is
bound to submission identity and persisted in the private request directory, not added to argv or
automatically copied to logs. A broken input pipe fails execution and uses the same process-group
cleanup as cancellation/deadline handling. Existing commands keep ignored stdin by default.

The six-command lifecycle suite passes with literal UTF-8 assignment delivery, repeat submission,
changed-input refusal and input-size validation. A focused broken-pipe fixture also confirms failed
status and cleanup despite the child attempting a normal exit. Typechecking passes. This supplies
the common transport needed by provider commands; native stream validation still needs wiring into
the owner before provider execution can be exposed as a migrated capability.

### Provider validation under the existing process owner

The detached command owner now optionally validates Claude JSON stdout with the native stream and
session gates. Native exit zero alone cannot pass: missing completion, identity drift, malformed
frames or missing required tool evidence prevent success. Reconciled provider completion/usage is
saved separately and its digest binds it to the command receipt; observation verifies that binding.
Cancellation, deadlines, output limits and descendant cleanup retain the same process owner.

Eight focused owner tests pass, including a four-case fake native-provider process matrix; typecheck
passes. No real provider or final review was invoked. Public progress persistence, command/profile
selection, native session follow-up, remaining providers and host completion delivery are still open.

Public provider progress is now persisted by the existing process owner as bounded, sequenced JSON
records bound to the command request. Cursor observation reads complete records only, verifies
identity/order and never dispatches work. The public projection excludes private reasoning blocks;
raw native stdout remains a separate private execution artifact. Reconnect pagination, wrong-request
refusal and reasoning exclusion pass in the fake provider process fixture; the eight focused owner
tests and typechecking pass. Host-facing CLI/delivery and remaining provider adapters are still open.

Native Claude command planning now preserves the existing explicit model/effort, default native
tools, permission mode, disabled inherited fallback, structured completion schema, additional roots
and exact resume identity. Assignment text is not part of argv. A live Python adapter comparison
checks every flag and parsed JSON schema/settings; invalid option-like selections and relative roots
are rejected. Two focused tests and typechecking pass. Selection precedence, assignment-authority
prompting and public job submission still need integration before this becomes an exposed adapter.

Native provider binding now resolves explicit model/effort/executable choices over a bounded version-1
configuration, then resolves the selected native executable without invoking a shell or substituting
a model/provider. Missing choices fail; Gemini suffix/effort consistency is preserved. Focused tests
cover precedence, PATH selection, missing executable, missing model, contradictory Gemini effort and
unknown binding keys. Typechecking passes. This POSIX qualification does not claim Windows native
executable discovery, authentication or live provider capability parity.

### Integrated native-provider checkpoint

All 226 engine tests pass after provider binding, native Claude framing/validation and shared-owner
stdin/progress integration. The compiled build also passes. A compiled fake-provider execution
receives its assignment via stdin, emits a verified session/completion, persists public progress and
returns a digest-bound provider result with confirmed cleanup. Its temporary workspace is removed.
This verifies compiled imports and the shared process path, not a live provider, installed release
or complete C04 migration. No final Opus review has been invoked.

### Gemini native event adapter checkpoint

The Antigravity event adapter now shares session/completion validation and the existing process owner
with Claude. It bounds pre-initialization events, validates native identity before replaying them,
tracks tool transitions/recovered denials and preserves native usage. Invalid effort, native terminal
failure, excessive early output and unfinished/denied tools cannot become success. Permission-error
classification is shared rather than copied into each adapter. Seven focused stream/owner tests and
typechecking pass. Gemini command/input construction and end-to-end fake/live provider qualification
remain open, as do Codex app-server integration and public provider job entrypoints.

Gemini command planning now preserves native streaming, schema, roots, exact conversation resume and
zero/fractional timeout mapping. Structured stdin is JSON-escaped and its encoded size is bounded.
Live Python command comparisons pass for disabled, fractional and ordinary native deadlines. A fake
Gemini subprocess receives framed stdin through the common owner and produces validated tool and
completion evidence, public progress and confirmed cleanup. Four focused command/owner tests and
typechecking pass. No real Gemini request occurred; model selection remains explicit. Public jobs,
assignment policy, Codex protocol and host completion delivery remain migration obligations.

### Codex app-server handshake checkpoint

The native handshake now produces initialize/thread-start-or-resume/turn-start messages with explicit
model, effort, workspace, permission and output-schema settings. It checks returned settings and
session identity before allowing the turn, rejects duplicate/out-of-order responses and model
rerouting, and binds subsequent identified events to the known turn. Failures remain terminal for
the handshake. Two focused tests and typechecking pass. Notification ordering against the actual
app-server, callback responses, bidirectional pipe transport and persistent-server shutdown still
require implementation/qualification; this is not a working Codex provider adapter yet.

Codex client-request handling now preserves native refusal replies for command/file approvals,
elicitation, user input and unavailable dynamic tools, plus method-not-found for unknown requests.
Replies retain the server request ID; interruption identifies only a known owned thread/turn. Each
request records an unresolved need for parent assistance rather than granting new authority. Live
Python comparisons cover all six response categories; focused tests and typechecking pass. The
process transport must flush these replies before stopping, which remains part of bidirectional
owner integration rather than a claim of completed callback delivery.

Codex notification handling now binds known turns, projects bounded public answer text, tracks native
command/file/web/tool evidence, preserves token-usage observations and reconciles final structured
answers. Nonzero native command exits do not satisfy required command evidence. Parent-only callbacks
produce refusal/interruption messages and a blocked outcome. Mismatched completed turns or model
rerouting permanently fail validation. Three focused tests and typechecking pass. Bidirectional
transport, actual notification-order qualification and persistent-server cleanup remain outstanding.

### Shared-owner Codex transport checkpoint

The existing process owner now transports Codex initialize/thread/turn messages bidirectionally.
Pending input is bounded; task text remains private stdin data. A verified completed turn closes
stdin and permits bounded shutdown of the owned persistent server. Parent-assistance replies are
flushed before a short bounded stop; their provider outcome remains blocked. Completion still passes
through stream validation and digest-bound evidence, with normal group cleanup required.

A fake app-server process verifies both full handshake/completion shutdown and receipt of a user-input
refusal before termination. The first fixture exposed an aliased temporary workspace mismatch; using
its canonical path made the fixture agree with the native cwd without weakening identity checks.
The Codex integration rerun passes, and the nine existing shared/provider-owner cases pass. Actual
app-server notification ordering, host job admission/follow-up and live provider qualification remain
open; no real model call or final review was launched.

Provider completion receipts now include verified conversation/model/permission identity, requested
effort and separately reported effort. Missing effort remains null. Codex includes its exact turn
and retains initialized identity when parent assistance blocks completion. Before initialization,
no verified identity is invented. Five focused session/process tests pass; Codex's blocked and
completed receipt identities also pass readback. This enables later receipt-bound follow-up design
but does not yet authorize or dispatch a continuation.

### Native invocation planning checkpoint

One pure planner now combines explicit provider binding with canonical workspace paths, native
Claude/Gemini/Codex arguments, private assignment stdin and shared-owner bounds. Aliased roots are
deduplicated. Gemini receives framed input and a rounded native timeout while the process owner
retains the exact deadline. Required tools and native conversation identity survive planning.
Unsupported Codex additional roots fail explicitly rather than silently losing requested scope.

Two focused tests cover all three providers plus invalid deadlines, oversized UTF-8 assignments,
unsupported roots, conversation options and tool limits; typechecking passes. This is transport
planning, not assignment authorization. Public job admission, workspace coordination, receipt-bound
follow-up, zero/unbounded legacy deadline semantics and Codex additional-root qualification remain
open. No native model process was launched by these tests.

### Receipt-bound provider continuation checkpoint

Provider invocation plans now record canonical additional workspace roots. Continuation planning
reads the exact hashed parent request and digest-bound native result, requires terminal state and
confirmed process cleanup, and checks model, requested/reported effort, permission mode and any
previously bound conversation. It preserves executable, workspace scope, required tools, environment,
credential names and process bounds; the new assignment uses the verified native conversation.
A distinct job is required. Blocked native outcomes with verified identity can be continued after
cleanup; missing identity and altered evidence cannot. Parents lacking recorded scope are rejected.

Three focused tests pass across all native providers, including uncertain cleanup, altered result
bytes, identity drift and parent request mismatch; typechecking passes after making the recorded
executable explicitly required. This produces a parent-bound plan only. Fresh job authorization,
workspace/session claims and durable parent linkage at dispatch remain required integration work.

### Workspace admission registry checkpoint

The existing host resource registry now checks workspace claims atomically alongside device and
other claims. Reader jobs may coexist with one cooperating writer; overlapping writers and exclusive
jobs serialize. Filesystem device/inode ancestry catches aliases and parent/child overlap while
allowing unrelated siblings. Exact provider/conversation claims serialize even across disjoint roots.
Unique claim IDs allow multiple readers. Cleanup and generation fencing use the existing registry.

This changes resource protocol semantics to version 2. Existing protocol-1 registries require an
explicit migration option and drained holders; normal opening refuses an upgrade. Host identity,
prior evidence and leases are preserved. No live host registry was migrated. The resource-maintenance
CLI and coordinated legacy-writer drain remain required before adoption.

The full engine suite passed 245 tests. After correcting migration to require explicit invocation,
five focused registry/workspace tests and typechecking passed again. Public provider job admission
still needs to acquire and release these claims around the shared process owner; these primitives
alone do not establish completed provider migration or E5.

### Detached-owner workspace coordination checkpoint

Provider plans now carry explicit access mode (exclusive by default), canonical workspace/session
claims and the host registry location. Follow-up planning preserves access mode and registry rather
than rediscovering either. The detached command owner acquires all declared claims before launching
native code, persists the lease record, and publishes a failed admission receipt without launching
when acquisition fails. Confirmed native cleanup permits release with the terminal receipt digest;
unknown cleanup retains claims. A lease-persistence failure also retains its cleanup uncertainty.

Four focused tests and typechecking pass, including actual detached execution that proves blocked
admission never writes its launch marker, release after confirmed cleanup, idempotent resubmission,
and unchanged continuation binding. Crash recovery between terminal receipt publication and resource
release remains open, as does host assignment authorization and the public provider job entrypoint.
Low-level command requests without coordination retain their existing behavior; public provider
admission must require the coordinated plan. No real model invocation occurred.

### Command-claim reconciliation checkpoint

A reconciliation function now finishes resource release after an owner has published confirmed
cleanup but exited before releasing claims. It binds the exact parent request, published terminal
receipt, registry, complete resource set and lease owner/operation/generation. Missing or unknown
cleanup cannot release anything. Resource release replays are idempotent only for the exact prior
lease set and cleanup observation recorded in the registry; a replay never alters a newer holder.

Six focused recovery, detached-owner and registry cases pass, along with typechecking. The focused
recovery test also passes after requiring request protocol 1 and rejecting unknown terminal state.
Proof covers the persisted interruption boundary rather than a forced real owner crash. Public job
observation/recovery still needs to invoke reconciliation; no process is restarted by this function.

### Native assignment authority checkpoint

Review of the live provider contract clarified that the parent owns delegation authority. The
continuity ledger's read/check/record operations must not be repurposed to authorize a coding agent.
The migrated assignment builder preserves parent restrictions, role, authorized roots, provisional
reader findings, cooperating-writer limits, real tool evidence and structured completion requirements.
These declarations guide native capabilities; they are not a process sandbox.

Invocation planning now accepts a structured assignment and persists its canonical workspace scope
alongside the private native prompt. Receipt-bound follow-up preserves role, constraints and supplied
evidence while replacing the task text. All three access modes match the existing Python prompt in
live pure-function parity tests. Five focused assignment/planning/follow-up tests and typechecking
pass, including composed-input limits. The public provider entrypoint still needs to require this
structured path and validate the installed parent/destination runtime binding before launch.

### Provider destination-runtime identity checkpoint

Provider runtime validation now locates the destination lock, verifies the complete parent generation
reader, compares the staged installed payload and destination lock, and requires the executing helper
to belong to that installed generation. It does not bootstrap or update the destination. Ungoverned
standalone work remains available with no inherited runtime identity. Governed work, partial inherited
identity, or legacy parent identity cannot silently fall back to standalone/source execution.

The real staged-installation fixture verifies a matching binding and rejects a source helper, wrong
reader owner and changed project lock. Its complete test passes with typechecking. A separate focused
test passes for standalone, incomplete generation, legacy parent and malformed destination-lock cases.
This is validation infrastructure; public dispatch still needs to call it and retain the generation
through native completion, then verify the same binding during follow-up.

### Explicit provider submission checkpoint

The provider job API now requires a structured parent assignment, validates its destination runtime,
and dispatches the coordinated plan through the existing detached owner. Follow-up verifies parent
runtime identity, reconciles confirmed parent resource cleanup, and persists the parent request/result
binding in the new command request. It retains native session and assignment restrictions.

Installed jobs reserve a generation reader after their unique job directory/request reservation and
before launch. The worker verifies the retained reader against the destination installation; only
confirmed cleanup releases it. Interrupted dispatch retains its obligation. An exclusive worker claim
also prevents direct duplicate worker invocation. Identical submission retries reserve no new reader
and launch no replacement process.

An actual fake native executable proves initial submission, session continuation, preserved parent
constraints, parent linkage and exactly two launches across repeated submissions. Typechecking passes.
This does not constitute live provider or packaged-provider qualification. The CLI surface, durable
runtime-reader crash reconciliation, legacy timeout parity and native host completion delivery remain
open; no real model agent or final review has been invoked.

### Provider CLI checkpoint

The compiled CLI now exposes file-backed submission and continuation plus status, bounded waiting,
public event paging and explicit cancellation. Every observer/canceller verifies the recorded provider
request identity first. Requests reject unknown top-level fields; follow-up cannot override model,
workspace or parent restrictions. Submission success means accepted dispatch, while status/wait keep
pending/unknown distinct from verified success. Managed runtime invocation classifies these commands
as reads or writes and permits them through the generation launcher.

The fake-native end-to-end test now starts through a separate CLI process, waits and observes through
the command handler, resumes through the public follow-up handler, and verifies duplicate calls do
not rerun either job. Wrong-digest cancellation and excessive wait bounds are rejected. The test and
typechecking pass. Packaged-runtime provider proof, complete cancellation/timeout parity, runtime
reader crash reconciliation and host completion delivery remain open.

### Installed provider dispatch proof checkpoint

An offline-bundled compiled package was staged into a clean temporary installation and exercised
through managed runtime invocation. A fake native Claude executable completed initial assignment
and exact-session follow-up. The proof verified retained runtime readership after the submitting
launcher exited, duplicate submission without another launch, changed destination-lock refusal,
exactly two native invocations and zero retained readers after completion. The isolated workspace
was removed; artifacts and receipts remain external to this checkout.

The first proof exposed an uncaught asynchronous error from the CLI runtime-run branch. Awaiting
that invocation inside the existing error boundary now yields the structured CLI failure. A newly
built archive passed the full proof again and explicitly checked exit 2 plus structured failure for
the changed lock. This is installed fake-provider proof, not live provider, immutable release-source,
legacy runtime drain, timeout parity or host completion-delivery qualification.

### Retained-runtime recovery checkpoint

Command reconciliation now releases confirmed resource obligations before releasing the exact retained
runtime reader. It checks the hashed request, original cleanup receipt, generation sidecar, reader
owner/revision/directory and registry. Missing cleanup never clears an obligation. Runtime release
replay accepts an already absent token but refuses a live token owned by another job. Normal owner
completion uses the same replay-safe release so recovery may race it harmlessly.

The public `provider-reconcile` command exposes this path; follow-up invokes it before dispatch. Three
focused command/resource recovery and full fake-native CLI tests pass with typechecking. The initial
new fixture incorrectly tried to activate an unstaged directory; it now explicitly seeds the persisted
post-activation crash boundary, leaving actual installation verification to the existing packaged
proof. Forced owner crashes before a cleanup receipt and startup enumeration remain separate work.

### Provider timeout semantics checkpoint

Provider deadlines now preserve the legacy zero/disabling behavior and seven-day finite bound.
An independent optional idle timer uses monotonic native stdout/stderr activity; activity never
extends the overall deadline. Follow-up preserves the idle limit, Gemini receives its native disabled
timeout representation for zero, and cancellation remains active with both timers disabled. Ordinary
workflow commands retain their existing finite deadline requirements.

Actual fake subprocess cases prove silence triggers idle timeout, continuing output permits completion
beyond the idle interval, overall timeout still stops an active process, and an unbounded job remains
cancellable. Four focused timeout/planning/follow-up tests and typechecking pass. Full-engine proof
also passes 253 tests. These tests establish timer/cleanup behavior, not live provider qualification.

### Explicit resource maintenance checkpoint

Protocol-1 resource registries can now migrate through the compiled `resource-maintenance` command
with an explicit source protocol, existing registry and operator authority reference. The registry
checks drained holders and records authority plus the version transition atomically. Compatible
replays add no migration event. Empty/unversioned files cannot silently initialize replacement state.
Managed runtime invocation recognizes this as a write; ordinary job opening still refuses migration.

Six focused maintenance, resource and workspace cases pass with typechecking. Tests preserve host
identity, prior cleanup observations and history, reject live holders, and verify single-event replay.
No live host registry was migrated. Coordinated legacy process drain remains an operator/adoption
precondition; absence of recorded resource holders alone does not prove old processes cannot return.

### Native provider diagnostics checkpoint

The compiled `provider-doctor` command now resolves an explicit provider/model/effort/executable
binding without launching it. Stable failure reasons distinguish missing selection, inconsistent
selection, missing executable and invalid configuration. Diagnostics do not echo malformed config
contents or claim authentication, native capability or network qualification. Managed invocation
classifies this as a read.

Two focused binding/diagnostic tests and typechecking pass. A executable-canary fixture confirms the
command is never run. The initial error-class syntax was incompatible with Node's strip-only source
execution; explicit field initialization corrected it before the successful rerun. This diagnostic
is advisory and does not activate provider use or substitute a model.

### Public provider completion readback checkpoint

Provider status and wait now include a bounded public completion projection: verified conversation
and model identity, answer, artifacts, checks, sources and remaining work. The read verifies the exact
provider-result bytes against the command receipt before projecting selected fields. Unknown raw
fields, raw streams and assignment text are not included. Oversized answers/lists are explicitly
marked truncated and retain the full result artifact path and digest for deliberate inspection.

Two focused projection and end-to-end provider CLI tests pass with typechecking. They verify answer
readback without another file-read step, bounded output, exclusion of unknown private fields and
refusal of changed evidence bytes. Execution status remains separate from provider completion claims;
this convenience does not add acceptance authority or establish measured token savings.

### Codex additional-root parity checkpoint

The earlier blanket rejection of Codex additional roots is removed for structured assignments.
The existing Python contract sends these roots in the assignment to a verified full-access native
session; it does not require a separate Codex CLI flag. The compiled path now preserves that behavior
and claims all canonical roots in the shared registry. A raw prompt without the structured assignment
still cannot silently omit extra scope. These are parent authority declarations, not sandbox limits.

The Codex fake app-server test now runs through public provider submission rather than direct command
construction. It checks that the actual turn message carries the additional-root declaration and task,
then verifies both completed persistent-server shutdown and parent-input refusal with cleanup and
reconciliation. All three focused Codex/planning cases and typechecking pass. Actual provider wire
ordering and live native tools remain unqualified.

### Provider telemetry readback checkpoint

A caller-selected provider receipt manifest now produces bounded telemetry through `telemetry
providers`: execution outcomes, duration, cleanup and selected native usage observations. Requests
and provider-result bytes are verified; repeated request identities are counted once. Prompt, answer,
raw stream and unknown usage fields are omitted. Native Claude usage and estimated cost fields and
Codex last/total counters retain their names and accounting boundaries. Gemini mapping remains
explicitly unqualified; absent fields are unknown, never zero-cost work.

The focused telemetry test and typechecking pass. Fixtures cover altered evidence, duplicates,
missing usage, separate cumulative/per-turn observations and exclusion of private text. The read
has job-count and byte limits and reports incomplete coverage. It makes no accepted-work performance
or token-savings claim; those still require paired workflow evaluation.

### Host instruction application checkpoint

E5 audit identified an unwired installation responsibility: host instruction planning had no apply
implementation. The new installer applies the existing owned-block merge after group preflight,
optionally requires an exact reviewed plan digest, rechecks the remaining plan before each atomic
replacement, preserves file modes and safe symlinks, and verifies final readback. Empty overrides
remain empty. Interrupted application retains earlier valid changes for an idempotent retry.
Hard-linked targets are explicitly refused before writing because atomic replacement would sever
untracked authored relationships; this limitation needs category disposition before broad adoption.

Four focused installation/planning cases and typechecking pass. Initial fixtures omitted required
managed markers and assumed append order; they were corrected to the existing prepend contract
without changing merge semantics. No adopter instruction file was modified. The product init/update
orchestrator still needs to supply its verified packaged pointer and include these targets in the
coherent migration backup; this installer alone does not close C05/C12 or E5.

### Packaged host route integration checkpoint

A canonical compiled-provider guide now ships in the existing generated resource payload and is read
through `provider-help`. The thin managed host block points to that installed command and preserves
the requirement to obtain test-execution guidance before substantial proof. The `host-instructions`
command verifies both the destination's pinned generation and exact stable launcher, supports dry-run
and plan-digest application, then calls the preserving installer. It cannot install a source-checkout
or standalone fallback route.

The focused route/installation test, compiled build and typechecking pass. A new clean package proof
exercises dry-run, exact-plan apply, idempotent readback, authored text/mode preservation, a retained
local symlink and inactive empty override. Its installed launcher successfully reads provider help.
The same artifact repeats initial/follow-up fake-provider execution, runtime-reader retention/release,
duplicate prevention and changed-lock refusal. All pass; temporary installation is removed and proof
remains external. Full init/update backup integration and live parent route-selection qualification
remain open; no adopter startup file was changed.

### Host-target backup scope checkpoint

Host instruction planning now derives the complete canonical backup target set, including unchanged
and inactive authored entries and explicitly absent files. Safe aliases share a single target. The
coherent backup supports absent inputs under receipt version 3; existing file/SQLite-only receipts
remain version 2 and readable. Absence is checked against an ordinary canonical parent before and
after capture. A newly created file fails backup and retains maintenance rather than becoming
installer-owned. Historical absence remains inspectable without claiming current absence.

Forward repair preserves declared absence and refuses newly created content instead of deleting it.
Three focused backup/staged-installation cases and typechecking pass, including creation during the
SQLite backup yield. Full-engine proof passes 261 tests. This supplies backup semantics and target
selection; the combined product transition still needs to bind the host plan and apply it under the
same declared recovery operation. No live installation or authored adopter file was changed.

### Backed host-plan verification checkpoint

Host migration now verifies the original plan against the coherent backup before deriving a resumable
remaining plan. Every canonical entry target must be backed. Planned content must equal the owned
merge of those original bytes with the declared block, including mode and original absence identity.
Current targets may contain their original bytes or the exact intended result; unrelated content,
changed entry mappings and unbound writes fail. A previously absent file created empty by another
writer is still a conflict, not permission to overwrite it.

Three focused backup-bound verification/application tests pass; typechecking passed before the final
small absence-condition tightening. Fixtures exercise partial application followed by readback, new
empty authored files, changed text and a modified planned payload. This is validation for the combined
transition, not a replacement for its durable maintenance/finalization obligations. Coherent backup
selection and the preserving installer now have a checked bridge; orchestration remains outstanding.

### Coherent host completion and admission checkpoint

The compiled transition now binds backed host instruction changes to the same maintenance operation
as generation activation. Installation protocol 7 generalizes the existing preservation requirement
into typed completion requirements, preserving protocol 6 repair obligations during migration. A
partial host update blocks maintenance release, lock-only finalization and lock-only recovery. The
combined transition verifies original/partially applied content, resumes remaining owned changes,
reads back all targets, then finalizes through the actual installed launcher. Completed replay must
retain the same host-plan identity. Existing forward repair retains its separate preservation digest.

Typechecking, six focused cases and all 264 engine tests pass. A clean, offline packaged-runtime proof
also passes: partial application resumes, both bypasses fail, replay is identical, authored text and
mode survive, an in-project symlink and empty override survive, launcher help remains available, and
no generation readers remain. The temporary installation was removed. Evidence is external to this
checkout; this is a dirty preview archive, not immutable release or live adopter migration proof.

The combined API closes the host-plan application/finalization gap. The operator command still needs
file-backed plan handoff and absence-input support; full inventory-driven installation, legacy writer
drain and category closure remain open. No active adopter, commit, publication or final review changed.

### Saved host-plan operator command checkpoint

The preview operator entry point now exposes `runtime-host-plan --workspace <root>` as read-only JSON
containing the plan, its digest and required host backup inputs. Save that output outside the adopting
checkout before applying the transition. `runtime-complete --host-plan <saved-json>` consumes the
same plan after coherent backup and supports repeated `--absent <path>` alongside `--file` and
`--sqlite`. The full declared scope must still exactly match the backup. Saved plan structure and
digest are checked before mutations; backup-bound checks remain authoritative for original content.
These maintenance commands run through the verified candidate entry point, not an admitted reader
of the generation whose admission they must close. They do not perform inventory or writer drain.

Preserving forward repair and host instruction changes are mutually exclusive command modes:
`--forward-repair --host-plan` fails before accessing the registry. Repair continues preserving the
backed inputs; changing instruction content uses the declared host transition. This does not permit
combining an evidence-restoring repair with instruction replacement.

Typecheck/build and both focused operator/staged-transition tests pass. The staged test found and
corrected a saved-plan validator mismatch with the canonical `sha256:` prefix. A newly built offline
archive passed installed CLI proof for planning, partial-application completion, identical replay,
blocked lock-only finalization/recovery, authored content/mode/symlink/empty-override preservation,
real launcher readback and zero retained readers. Temporary installation cleanup completed. This
closes the saved-plan command handoff noted above; inventory-driven init/update, drain and remaining
E1–E5 qualification are still open. No live adopter or release was changed.

### Installed adopter dependency audit checkpoint

A read-only audit of the authorized isolated baseline records the actual wheel version/lock/launcher,
Git hook selection and four hook files, host instruction entries, project-native hook declarations,
12 custom packs, 46 policy file identities and selected runner/resource owners. Git status and index
identities were unchanged. Detailed adopter paths and evidence remain external. This is not a global
host-startup audit, clause-level policy audit or proof of drained owners, so E0 audit closure remains
open.

The audit found ten target packs invoking Python from the wheel's runtime directory. Compiled
activation would otherwise leave these checks dependent on the retired installation. The transition
now rejects literal wheel-interpreter references in declared target pack commands before activation,
with the affected pack IDs. This is a focused check for the observed dependency, not a shell resolver
or claim that arbitrary scripts have no hidden dependencies. Preserve those project-owned checks by
moving their interpreter/dependencies to the project's own environment before cutover; do not retain
another governance runtime as a compatibility layer. The selected entry scripts include a third-party
YAML dependency, so substituting an arbitrary system Python is not sufficient qualification.

The audit also confirms shared worktree Git-hook configuration, separate legacy physical-device lock
ownership and legacy simulator exclusion from that device lease. Do not mutate shared Git config or
claim unified resource admission from the preview registry alone. Typecheck and two focused
transition/dependency tests pass. Running the read-only preflight against the actual baseline names
all ten dependent packs and refuses activation. No adopter files, locks or resources were changed.

### Target interpreter separation qualification checkpoint

The authorized isolated baseline's four Python validator entry scripts import successfully in a
separate environment containing the same exact PyYAML version and no governance package. The
candidate environment installs from a locally captured wheel with a SHA256-locked requirement and
no dependency resolution. Three bounded direct checks (root toolchain configuration, static release
preflight and a selected architecture packet path) pass under both interpreters with identical JSON
and exit results. Environment, dependency artifact and source identities remain in external evidence.
This demonstrates the dependency split for those entry paths; it is not a platform-wide environment
pin or complete execution proof for every custom pack.

The standalone interpreter then invoked the canonical complete toolchain checker: 163 of 164 Node
contract tests passed. The remaining authoring watcher test timed out waiting for a file event after
10 seconds. A targeted diagnostic passed the same test alone without changing source. Preserve the
failed full result; neither the isolated pass nor successful Python imports establish broad toolchain
qualification. Watcher timing under concurrent contract execution remains unresolved. No SDK source,
pack command, installed lock or active project was changed, and the separate environment is retained
outside the adopter for follow-up qualification. Direct-test usage was recorded once for this batch.

### Task-derived JEV comparison and observability checkpoint

A frozen four-case follow-up uses defects encountered during this implementation, twelve supplied
source candidates and enabled ranking at the normal one-second deadline. Labels remain author-owned
and the candidate universe curated; this is not independent held-out qualification. The first request
returned the existing undifferentiated unavailable/invalid fallback, and the next three observed shared
cooldown. Delivered packets matched lexical fallback, with two useful owners selected in each mode.
Classify this comparison as inconclusive, not no benefit or qualified savings. Native usage for the
failed request remains unknown. Original receipts were retained, not replaced with later success.

The failure exposed an observability gap. Decision results now distinguish a fixed nonsecret failure
stage: health storage, transport, response body, response JSON, model identity or choice validation.
Operational telemetry aggregates only those allowed stage labels; exception text and raw responses
are not copied. One subsequent diagnostic, outside the frozen comparison, succeeded in about 485 ms
and reported 6,775 input / 240 output tokens. It does not retroactively explain the original failure
or establish net token savings.

An offline replay of the same captured decisions also identifies useful candidates too large to fit
beside required context under the packet budget. One missed owner in this dataset has that property;
no ranking can fix it. The evaluator reports these separately from candidates absent from retrieval,
while retaining total selection misses. Typechecking and 16 focused decision/telemetry/evaluation
tests pass. A source-bound excerpt/fragment delivery improvement and a newly frozen independent
workflow comparison remain needed before a development-benefit claim. No default JEV policy changed.

### Bounded optional source delivery checkpoint

The observed whole-file budget limit now has an explicit delivery option. `context-packet` and
`context-route` accept `--optional-excerpt-bytes` (128–65,536 bytes per optional source). The default
whole-file behavior is unchanged. Deterministic term matching selects one contiguous whole-line
window, preserving the full-source digest and recording first/last/total lines, an exact UTF-8 excerpt
digest and an incomplete marker. Required instructions are never shortened. Packet size includes the
range metadata, and a single oversized line remains undeliverable rather than being silently split.
Provider advice sees the same prepared optional excerpt that delivery uses; it cannot invent text.

Receipts retain the selected range metadata without source contents. Full-file freshness checks still
reject edits outside the delivered range. Focused tests cover exact contiguous/Unicode bytes, bounded
size, unchanged required instructions, stale changes outside the excerpt, invalid budgets and ordinary
no-token/auto/shadow behavior. Typechecking and the consolidated engine suite pass. An offline,
zero-network development replay of the four task-derived cases selects three useful owner files
instead of two and reports no individually oversized useful candidate. This is owner-selection
coverage, not proof that every decisive line was selected, independent evaluation, avoided host tokens
or accepted-work benefit. No additional live JEV requests were made for this replay.

### Context fallback consistency checkpoint

Packet assembly previously used discovery order when a provider threw or returned invalid identity,
while disabled/missing-token assistance used lexical ranking. That discrepancy could omit the useful
source under the same byte budget solely because optional assistance failed. Packet assembly now
starts from the shared lexical baseline, including when excerpts are enabled; valid advice remains
optional. A focused parity case proves identical selected/omitted content for disabled assistance,
provider exceptions and stale advice. Typechecking and twelve affected packet/evaluation/command
tests pass. The test fixture's byte budget was tightened after its first run admitted both candidates;
no production selection rule was weakened. No live provider calls or adopter changes were needed.

### Managed provider job discovery checkpoint

Provider submission and follow-up now default to a private workspace-scoped job directory keyed by
the caller's job ID. Existing durable request reservation supplies idempotency; no second job-state
index or database was added. Explicit directory overrides remain available. A changed request under
the same managed ID conflicts instead of launching another native process.

`provider-list --workspace <root> --limit <1..1000>` reads those managed job directories and returns
ID, directory, request digest, state and cleanup status. It does not create a store, reconcile jobs,
read private prompts into output or dispatch work. Malformed/symlink entries are reported as invalid;
scan/output bounds and truncation are explicit. This is workspace-managed discovery, not complete
legacy or arbitrary explicit-directory inventory. Installed invocation recognizes listing as read-only.

Typechecking and focused tests pass for absent-store observation, stable workspace-separated IDs,
bounded malformed/symlink scans, actual CLI default submission into the store, native fake-provider
completion, same-job replay, managed native-session follow-up and rediscovery of both completed jobs.
No live model was called. Packaged guidance documents the defaults and scope. C04 still requires
host completion delivery, pre-receipt owner-loss recovery and native provider qualification; this
checkpoint does not close those independent lifecycle requirements.

### Opt-in provider completion delivery checkpoint

Compiled provider submission can now opt into native Codex queue delivery through an explicit
`--completion-executable`. Before dispatch it captures the invoking task ID, queue capability,
executable digest and Codex home. Follow-up jobs retain that original target. The process owner
attempts delivery after recording its terminal outcome, including resource-admission failure. The
notice contains only the bound result locator/digest and assessment instructions, never provider
output. Delivery does not alter execution receipts, release unknown resources or resubmit work.

A separate private receipt records sending, queued or failed/uncertain delivery; queued consumption
remains unknown. `provider-deliver` reuses existing delivery state, and explicit `--retry` retries a
failed/uncertain notice without rerunning the command. An interrupted delivery lock is retained as
busy/uncertain rather than stolen. Queue calls and capability probes are time/output bounded. This
first port does not qualify descendant cleanup of a malfunctioning queue executable, host restart
survival or automatic recovery of a delivery owner lost before receipt finalization.

The fake native host proof executes one command, fails the first queue attempt, retries successfully,
retains byte-identical command evidence and proves only two notices and one execution occurred.
Broader proof caught a fixture race between final receipt writing and delivery-lock release; the test
now waits for both recorded terminal delivery and released lock. Packaged guidance's prior no-notice
assertion was updated to the explicit opt-in/unknown-consumption contract. Typechecking and the full
273-engine / 72-continuity test suites pass. No actual Codex task notice or live model call was sent.
Real installed-host delivery qualification and pre-receipt execution-owner recovery remain open.

### Completion evidence retry binding checkpoint

Delivery retry now validates the saved notification's protocol, task, request and exact result bytes
before reusing or retrying it. Previously a queued receipt could be returned after the result file had
changed, making the readback ambiguous. Result parsing and its notice digest now bind the same
captured UTF-8 content, with a second file-digest check after acquiring delivery ownership. A changed
queue executable is refused before invocation; changed result evidence cannot reuse a prior notice.
Typechecking and both completion tests pass, including zero queue calls after executable drift and
no extra queue call after evidence drift. This is local failure-path proof, not real-host delivery
qualification; original execution evidence and unknown consumption semantics remain unchanged.

### Installed discovery and native queue qualification checkpoint

A fresh offline preview archive passed its installed CLI proof for managed submission/listing,
constraint-preserving native-session follow-up, duplicate submission and duplicate notice prevention,
changed-lock refusal, retained/released generation readers, installed host guidance and authored
instruction preservation. The fake native provider executed exactly twice and the fake queue received
exactly two notices. Its temporary project/installation was removed after verification.

The actual initiating host's Codex executable also passed the bounded queue-capability probe with
captured executable digest and task/home identity. One separately retained installed-artifact
qualification ran a deterministic read-only command, completed successfully in 24 ms with confirmed
process cleanup, and recorded successful queue acceptance to the initiating task. No model call,
device action or adopter mutation occurred. Queue acceptance is proven; host consumption/reopening
is still unknown until the actual completion notice is received and assessed. External evidence
retains the job, exact archive, installation, request/result/notification identities and scripts.
Do not relaunch this qualification in response to a later duplicate notice. This is live queue proof,
not host-restart survival, provider-model qualification or complete C04/E5 closure.

### Native completion consumption checkpoint

The actual native queue notice arrived in the initiating task. Consumer assessment verified the
announced result hash, request/result/delivery binding, successful deterministic command output and
confirmed cleanup; the delivery lock is absent. A separate external consumer assessment records
receipt and assessment without rewriting the producer's unknown-consumption receipt. The existing
job was reused and no work was relaunched. This closes the narrow same-task delivery-and-consumption
proof. Host restart survival, live provider qualification and device workflows remain separate gaps.

### Execution-owner loss evidence checkpoint

The command owner now persists launch intent before spawning and records the child PID, process
group and start fingerprint after spawning. Failure to persist the acknowledgment causes the live
owner to kill its freshly created group; it does not manufacture confirmed cleanup. An interrupted
intent remains unresolved. These records supply missing recovery evidence, not authority to signal
a later reused PID or replay a command.

A real process fixture kills the worker after launch acknowledgment, verifies that its child remains
alive, and confirms observation returns unknown with no fabricated result or duplicate execution.
The fixture cleans up only its matching process identities. All eight process-owner tests and
typechecking pass. This reproduces the pre-terminal owner-loss gap rather than closing it: a durable
guardian/reconciliation path is still required before unattended crash-recovery qualification.

### Owner-loss reconciliation checkpoint

`provider-recover` now supports the post-exit recovery case. It requires explicit recovery authority,
the original request and complete local launch/owner identities, and successful bounded process
enumeration showing neither the owner PID nor the child PID/process group present. It refuses live
or reused PIDs, incomplete launch acknowledgment and other-host evidence. It never signals or replays.
The recovered result retains an unknown execution outcome while separately confirming process-group
cleanup. A bound recovery receipt allows reconciliation to release only the original claims/readers;
unknown outcomes without that proof remain ineligible. Native-session follow-up still refuses unknown
execution. Interrupted recovery locks remain unresolved rather than being stolen.

The real worker-loss fixture verifies live-owner/live-group refusal, incomplete/other-host launch
refusal, unknown outcome after fixture-owned child termination, changed cleanup-proof refusal,
idempotent reconciliation and protection of a newer resource holder. Typechecking and the full engine
and continuity suites pass; the expanded negative-path fixture also passes. Packaged guidance exposes
the command and limits. This closes post-exit reconciliation, not autonomous termination of surviving
children: a guardian remains needed for that case, and escaped process groups remain outside this
process-group cleanup proof. Installed-artifact qualification of this new command is still pending.

### Automatic worker-loss guardian checkpoint

Every native command now starts a detached guardian and waits for its bound acknowledgment before
launch. The guardian waits for positive process-enumeration evidence that the worker disappeared,
then terminates the recorded group only while its original leader fingerprint matches. It escalates
from termination to kill with bounded waits. After group absence, it uses the same owner-recovery,
claim/runtime reconciliation and completion-delivery paths. Inspection failure and uncertain identity
preserve unresolved obligations; no command is replayed and recovered execution remains unknown.

The real process fixture waits for a child to install its termination handler, kills the worker,
and proves the guardian cleans a child that ignores termination. Manual-recovery proof explicitly
stops its own guardian to retain that independent recovery case. All 276 engine and 72 continuity
tests pass, as do typechecking and build. A fresh offline archive passed installed CLI submission,
follow-up, discovery, delivery and generation-reader checks plus all nine process-owner tests run
against its compiled modules. No live model or adopter was used. The public recovery-command route
itself still needs an installed CLI failure/recovery proof; installed module proof is narrower.

This covers a surviving original group leader, not every crash window. Loss before launch
acknowledgment, a missing leader with surviving group members, escaped groups, and simultaneous
worker/guardian loss still need qualification or further implementation. These limits prevent broad
unattended-recovery or complete C04/E5 claims. External evidence retains the archive, proof script,
installed test output and summary; temporary proof workspaces were removed.

### Recorded group-member recovery and installed command checkpoint

The guardian now records members while a matching known process still anchors the original group.
It retains those identities after the original leader exits and signals only matching individual
members. A reused/mismatched identity cannot authorize collecting unrelated group members. Tracking
is bounded, and changed membership is persisted without rewriting unchanged observations every poll.
This closes the observed missing-leader case when surviving members were recorded; unobserved or
escaped processes remain outside that proof.

A real fixture lets the guardian observe a child, kills the original leader, verifies the child is
still live, then kills the worker. The guardian cleans the recorded survivor and publishes unknown
execution with confirmed group cleanup. A negative identity fixture verifies no adoption from a
mismatched process fingerprint. Typechecking, build, all 278 engine tests and 72 continuity tests pass.

The fresh offline archive passed ten process-owner tests against compiled modules and the installed
public `provider-recover` route. That CLI proof refuses a live owner and live child, recovers only
after their exit, releases the retained runtime reservation, preserves unknown execution and returns
the same result on replay with exactly one native launch. Its first qualification script attempt
used an undefined registry path and failed before dispatch; the corrected explicit registry path
passed. Installed native submission/follow-up/discovery/delivery proofs also pass. Evidence and
archives remain external; temporary workspaces were removed. All native providers here are fixtures.

Remaining recovery work includes launch interruption before acknowledgment, escaped descendants,
simultaneous worker/guardian loss and existing terminal receipts with uncertain cleanup. No complete
E1–E5 or unattended process-tree containment claim follows from these bounded process-group results.

### Terminal cleanup reconciliation checkpoint

Recovery now handles an already published terminal outcome with uncertain cleanup. After the same
local launch and process-absence checks, it adds a receipt-bound cleanup confirmation without
rewriting the original result. Reconciliation may release the original reservations using that
proof; the CLI reports this separately from the original outcome and cleanup observation. Replayed
recovery preserves both records. Changed result or launch evidence invalidates the proof rather than
silently replacing it. Native-session continuation still follows its original outcome/cleanup gate.

A real failed command supplies the terminal fixture; after its owner and guardian exit, the test
seeds uncertain cleanup and verifies original failure bytes/exit code remain intact through recovery,
replay and result-drift refusal. The full 279-engine and 72-continuity suites, typechecking and build
pass. A new offline archive passes eleven compiled process/recovery tests plus the public recovery
CLI with a deliberately seeded terminal/uncertain-cleanup boundary. The CLI refuses live cleanup,
preserves original bytes and failed outcome, releases the retained runtime reader and does not launch
the native fixture twice. This validates the recovery boundary, not a naturally occurring cleanup
failure. Installed submission/follow-up/discovery/delivery checks also pass; no live provider was used.

The remaining recovery gaps are launch interruption, escaped descendants and simultaneous owner loss.
This checkpoint closes the previously listed terminal-uncertain-cleanup reconciliation gap only.

### Recorded escaped-member refusal checkpoint

Recovery now validates the persisted member ledger and checks every recorded PID against the full
process inventory, including processes outside the original group. An empty original group cannot
confirm cleanup while a recorded member remains elsewhere. Recovery evidence binds the exact member
ledger; changing that ledger invalidates subsequent reconciliation. Malformed, duplicate or oversized
member records are refused. This protects cleanup claims without granting new signaling authority.

The terminal-recovery fixture adds a known live PID outside the finished command's group and verifies
recovery refusal with unchanged result bytes. It then verifies ledger drift invalidates the later
cleanup proof. Typechecking and eleven focused process/recovery tests pass. The ledger addition is a
seeded recovery-boundary fixture, not proof of automatically discovering escaped descendants. Source
changes after the preceding archive still need integrated installed-artifact qualification; automatic
discovery and cleanup of escaped descendants remain open.

### Observed detached-descendant cleanup checkpoint

Process enumeration now includes parent identity relationships. The guardian extends its bounded
recorded set from matching known processes through observed parent/child relationships, including
children launched into separate process groups. Cleanup signals matching recorded individuals and
does not stop merely because the original group is empty. Normal worker completion also checks all
recorded PIDs and cannot declare confirmed cleanup while an observed detached child remains alive.
The guardian can then publish later cleanup evidence without rewriting the original unknown result.

Two real process fixtures launch detached children and wait until the guardian records them. One
kills the worker; the other permits native exit zero while the detached child remains alive. Both
prove guardian cleanup. The latter preserves an unknown execution outcome and original uncertain
cleanup observation even after later cleanup confirmation. These are observed-tree tests, not seeded
membership records. Unobserved rapid detach/reparent sequences still require OS containment or a
stronger launch protocol; no universal descendant-containment claim is made.

Typechecking, build, all 281 engine and 72 continuity tests pass. A fresh offline archive passes
thirteen compiled process/recovery tests, including both detached-child fixtures and the prior
recorded-member drift guard. Installed public recovery preserves terminal bytes, releases the original
runtime reservation and does not replay native work. Installed submission/follow-up/discovery/delivery
proofs pass as well. All provider processes are fixtures, external evidence is retained, and temporary
workspaces are removed. Launch acknowledgment interruption and simultaneous worker/guardian loss
remain open alongside the broader E1–E5 qualification work.

### Cleanup-supervision restart checkpoint

`provider-resume-cleanup` restarts only the guardian after simultaneous worker/guardian loss. It
requires the original bound request, current execution-owner code, matching runtime generation,
local acknowledged launch and recorded process identities. A live worker refuses restart; a live
matching guardian is reused, while a reused guardian PID is refused. A restart lock serializes
dispatch. The replacement guardian loads the validated persisted member ledger, so it can clean an
observed detached child even when the original group leader has also exited. Completed cleanup
reconciles without starting another guardian or replaying the native assignment.

The real process fixture stops worker, guardian and leader while leaving a recorded detached child
alive, then proves cleanup restart and replay. Typechecking, build, all 282 engine tests and 72
continuity tests pass. A fresh offline archive passes fourteen compiled process/recovery tests and
the public restart command through installed runtime invocation. The installed proof preserves a
seeded terminal failure, confirms later cleanup, releases the original runtime reader and records
exactly one native launch. Installed submission/follow-up/discovery/delivery proofs also pass. All
provider processes are fixtures and temporary workspaces were removed; evidence remains external.

This closes recovery from acknowledged simultaneous supervision loss when its launch/member evidence
survives. Interrupted restart locks, launch interruption before acknowledgment and descendants that
escape before observation still remain unresolved. No complete E1–E5 or OS-containment claim follows.

### E3 decisive-evidence scoring checkpoint

Context evaluation now accepts optional source-bound line-span labels and scores delivery of those
spans separately from selecting the owning file. A selected excerpt that omits the decisive lines
does not count as useful-evidence delivery. Invalid source/range labels are refused before provider
calls, and unlabeled evidence quality remains unknown. Existing owner-selection scores remain intact;
neither measure implies token savings or development-speed benefit.

Nine focused context/evaluation tests and typechecking pass, including selected-owner/missing-lines,
full-source delivery, stale labels, missing labels and excerpt identity. An external offline replay
adds author-selected decisive spans to the four frozen task-derived cases. Lexical whole-file
delivery supplies the labeled evidence in two cases; the existing optional 1,400-byte excerpt setting
supplies it in three. This is descriptive source-bound evidence for further excerpt testing, not
independent held-out gold or a new JEV comparison. No paid call was made, no previous evaluation was
overwritten, and E3's enabled-workflow benefit assessment remains inconclusive.

### E3 live evidence comparison and caller cancellation checkpoint

The frozen four-case decisive-span screen completed one live auto-mode JEV comparison using the
same 1,400-byte optional excerpts and lexical reference. All four requests returned usable advice
within 186–512 ms. Both JEV and lexical delivery supplied the labeled evidence in three cases; there
were no additional hits or regressions. Native reported usage was 27,880 input and 966 output tokens.
This screen shows no incremental selection benefit over the lexical reference. Whole-workflow benefit
remains inconclusive because labels/candidate scope are author-selected and host accepted-work/token
measurements are absent. The frozen manifest/result remain external; no prior evaluation was replaced.

The decision interface, packet builder and evaluator now accept caller cancellation. Pre-cancelled
packets make no provider call; active JEV transport is aborted and returns the lexical baseline without
creating provider-outage cooldown or failure-stage telemetry. Late advice cannot replace the packet's
baseline, while any reported native usage remains accountable. Eighteen focused decision/evaluation
tests and typechecking pass, including no-call pre-cancellation, active abort, no cooldown poisoning,
late-advice rejection and usage retention. Cancellation is caller-owned through the API; this is not
a new detached shadow scheduler or a claim that all CLI signal paths have been wired. Installed
artifact qualification of these E3 changes remains at the next integrated boundary.

### E3 command cancellation checkpoint

Context packet, routed-context and evaluation entry points now forward caller cancellation into
optional advice. Their CLI branches scope SIGINT/SIGTERM handlers to that operation, abort advice,
retain deterministic fallback/receipts and return interruption exit codes 130/143. Mandatory routed
context remains governed by its existing policy. Handlers are removed after completion or error.

Typechecking and focused command/routing tests pass. A real subprocess sends itself SIGTERM during
an injected, non-network provider call and verifies cancellation evidence plus exit 143. A routed
pre-cancellation fixture verifies zero provider calls and preservation of mandatory content. The
first local validation caught an import placed before the CLI shebang; it was corrected before the
passing rerun. This proves direct CLI signal handling, not managed-launcher signal forwarding or
installed-artifact qualification, which remain separate integration work.

### Managed cancellation and E3 package checkpoint

Managed invocation now forwards SIGINT/SIGTERM to its live child handle, retains the generation
reader until child close, and preserves interruption exit codes even when a cooperative child exits
zero after cleanup. A failed signal is not treated as proof that a spawned child stopped. Scoped
handlers are removed on completion. Abrupt uncatchable launcher loss still requires reader recovery.

The staging fixture verifies both signals, delayed child cleanup, non-success parent exit and reader
release. Typechecking, build, all 288 engine tests and 72 continuity tests pass. A fresh offline archive
passes 22 compiled decision/context tests and fourteen compiled process/recovery tests. Its actual
managed `context-evaluate` invocation receives SIGTERM during injected transport, forwards cancellation,
writes baseline cancellation evidence, exits 143 and releases the runtime reader. No paid provider
call occurs in this qualification. Existing installed submission/follow-up/discovery/delivery and
instruction preservation checks also pass. This closes the prior managed-forwarding and package
qualification gaps for these E3 changes; it does not establish JEV workflow benefit or full E1–E5
completion. External scripts, archive and results remain retained; temporary workspaces were removed.

### Explicit compiled maintenance and backup commands

The preview CLI now exposes the existing transition prerequisites through `runtime-maintenance`
and `runtime-backup`; operators no longer need internal TypeScript calls for these two steps.
`runtime-maintenance --action begin --registry <path> --owner <identity> --revision <expected>`
obtains an owned token. `runtime-backup` takes the registry, owner, token, new destination directory
and explicit repeated `--file`, `--sqlite` and `--absent` inputs. The same declared-input parser feeds
`runtime-complete`, keeping one backup-scope interpretation. `runtime-maintenance --action end`
requires the matching owner/token and preserves completion-obligation refusals. These commands run
from the verified compiled candidate, not through a retained reader of the installation being drained.
They do not prove that legacy writers stopped.

Typechecking and three focused maintenance/staging tests pass. CLI proof preserves file bytes and
absent paths, produces an inspectable backup and cannot end maintenance with unfinished host work.
Invalid arguments cannot create a registry. An initial fixture used a noncanonical temporary path;
canonicalizing that fixture fixed the expected backup refusal without weakening runtime checks.
This exposes qualified primitives; inventory-driven init/update, legacy drain and record migration
remain required before coordinated product activation. Installed packaging of these new commands
remains pending at the next integrated boundary.

### Maintenance registry refusal and package checkpoint

Ending maintenance and requesting backup now require an existing ordinary registry before opening
the coordination store. Beginning at a nonzero revision also requires one. A mistyped path can no
longer create an empty registry as a side effect of those operations; only explicit revision-zero
begin can initialize it. Focused tests verify missing-registry refusal leaves both registry and
backup destination absent while successful declared backups preserve their inputs.

Typechecking, build, all 290 engine tests and 72 continuity tests pass. A fresh offline archive passes
the two maintenance command tests through its compiled modules and CLI, fourteen process/recovery
tests and 22 decision/context tests. Installed managed cancellation, provider submission/follow-up,
discovery, delivery and host-instruction preservation proofs also pass. This closes the preceding
maintenance-command packaging gap. All native providers remain fixtures; no adopter installation was
changed. External evidence is retained and temporary proof workspaces were removed. Inventory-driven
init/update, legacy-owner draining and preserved-record migration remain open E5 work.

### Project-file migration discovery checkpoint

`runtime-migration-plan --workspace <root>` now discovers host-instruction targets, governance and
validation configuration, project policy files and repository-local hooks. Its deterministic digest
binds source hashes, modes and declared backup inputs. Discovery refuses aliases, unsupported entries,
excessive nesting or an overlarge scope instead of silently truncating. The result explicitly leaves
external stores/artifacts, live legacy owners, effective shared Git hooks and installed runtime/launcher
inventory unresolved. It is read-only discovery, not activation authority or a coherent backup receipt.

The first isolated-adopter scan revealed that policy files live outside the governance/validation
directories. Including `config/policies` produces 75 declared inputs, including 51 policy files, with
unchanged Git status. The initial narrower inventory is retained separately from the corrected one.
This real scope exceeds the prior 64-input backup bound, so writer, inspector and planner now share
one 256-input bound; oversized scope still refuses rather than splitting a coherent backup silently.
Typechecking and three focused tests pass, including deterministic discovery, policy inclusion,
content drift, alias refusal and an inspected 67-input CLI backup. Adopter evidence remains external;
no adopter file or installation was changed. Integrated package qualification and complete migration
orchestration remain pending.

### Saved migration plan to backup checkpoint

`runtime-backup --project-plan <saved-json>` now consumes the discovered project-file scope alongside
explicit external inputs. It recomputes discovery before creating output and again before issuing a
verified backup receipt. Both checks bind to the initially selected plan digest, so replacing the
saved plan together with changed source files cannot silently change the selected scope. Stale plans
fail before destination creation; scope invalidation after copying leaves a failed receipt and retains
owned maintenance. This is optimistic drift detection under the existing drained-writer prerequisite,
not a replacement for legacy-owner draining or an atomic filesystem snapshot.

Typechecking, build and six focused tests pass. The same six tests pass against a freshly staged,
offline bundled preview archive, including the compiled CLI's 67-input backup. Evidence and the
archive remain external; temporary proof installations were removed. This closes package qualification
for project-file discovery, the shared 256-input limit and saved-plan consumption. External inventory,
legacy drain, preserved-record migration and coordinated adopter activation remain open E5 work.

### Legacy ownership inventory checkpoint

`runtime-legacy-jobs --workspace <root> --store <legacy-store>` provides a read-only inventory of
legacy agent records with exact recorded-workspace matching. It binds selected status bytes to their
SHA256 identities, reports unsupported/unreadable entries and bounded-scan truncation, and excludes
prompts, response tails and native logs from output. It never invokes the old status/recovery API,
which can mutate ownership, and explicitly retains legacy ownership with drain unverified even when
all selected records show terminal states. Historical workspace strings are not resolved into another
worktree's ownership. Missing or inaccessible stores fail instead of becoming empty inventories.

Typechecking and focused tests pass for exact matching, content exclusion, byte preservation,
truncation, unsupported protocols and symlink refusal. An external read-only baseline inventory found
14 matching records among 507 inspected records, with eight succeeded and six failed, no unreadable
entries and no truncation. The broader location inventory also found two repository-local telemetry
files and no continuity database or startup directory at their expected locations. These observations
are not a coherent backup or legacy-drain proof. No adopter state was changed. Installed-package
qualification of this new command, artifact preservation and old-owner reconciliation remain pending.

### Legacy receipt readback and installed inventory proof

Legacy inventory now reads selected result receipts, verifies job/protocol/state agreement with the
status record, hashes the original bytes and exposes only the reported cleanup disposition. It rereads
status before returning a selected entry; mismatched or changing records become explicit issues.
Missing receipts remain visible as missing, and reported cleanup never becomes a claim of present-day
drain. Tests cover missing results, matching cleanup receipts, private-answer exclusion and conflicting
result identity in addition to the previous inventory cases.

Typechecking, build and seven migration/maintenance/inventory tests pass against a fresh offline
installed preview archive. Temporary proof installations were removed. The isolated adopter's 14
selected receipts all report confirmed cleanup and match their status identities. A separate read-only
process-table observation found none of the 56 recorded worker/launch/guardian/provider PID references
present. This is point-in-time evidence only: dispatch exclusion, shared resource ownership and coherent
artifact preservation remain required before activation. Evidence is external and original receipts
were left unchanged. No installed adopter runtime was replaced.

### Legacy history preservation experiment

The isolated baseline's fourteen legacy batches declare no external output roots. Their selected job
directories were copied into a separate external history archive: 472 ordinary files, 29,375,805 bytes.
The archival procedure checked original status/result hashes against the prior inventory, rejected
aliases and nonordinary entries, bounded file count and individual size, then compared complete file
membership, raw SHA256 hashes, byte counts and modes between source-before, archive and source-after.
All comparisons passed. Original job directories and runtime ownership were left unchanged.

This qualifies preservation/readback of those selected directory contents, not automatic discovery of
all indirectly referenced artifacts, a coherent installation backup, imported execution authority or
legacy dispatch exclusion. The archive retains original receipt bytes and original path references;
no historical success or input-validity claim was upgraded. The external archive receipt records each
member identity for later migration readback. Product-level archive/import orchestration and referenced
artifact closure remain open.

### Reusable legacy history archive command

`runtime-archive-legacy --workspace <root> --store <legacy-store> --destination <new-directory>` now
implements the prior archival experiment in TypeScript. It requires a complete, nonempty inventory
with terminal receipts reporting cleanup, verifies initial receipt identities, captures bounded ordinary
members and empty directories, copies bytes privately, verifies archive readback and rechecks original
membership, content and inventory. Aliases, special files, conflicting destinations and oversized scopes
refuse. Source modes are retained as metadata; archive copies are private non-executable files. Files
and created directories are flushed before the verified receipt is published. An interrupted copy
retains partial evidence with a failed receipt rather than deleting original records or claiming success.

Typechecking and two focused history/inventory tests pass. Source-CLI proof archived the baseline's
fourteen jobs and 472 files (29,375,805 bytes) with verified readback. This is the reusable TS command,
not yet installed-archive qualification; the external destination name does not change that distinction.
The earlier independently created archival copy remains retained. Neither archive imports execution
authority, proves dispatch exclusion or claims indirect external-reference completeness. Installed
qualification and migration/activation integration remain open.

### Installed history archive qualification

Archive discovery now applies one shared byte/entry budget across all selected job directories,
refusing excess during scanning rather than only after collecting every directory. Destination-parent
aliases are rejected before creating output; a focused regression confirms refusal leaves the real
aliased destination absent. These corrections keep preservation bounded and avoid writing through an
unintended destination alias.

Typechecking and build pass. Eight migration, maintenance, legacy-inventory and history-archive tests
pass against the newly staged offline preview package, including binary evidence, empty directories,
private archive permissions, original-mode metadata, unchanged source receipts and destination refusal.
External package evidence is retained and temporary installation cleanup completed. This qualifies
the installed archive module; full activation integration, dispatch exclusion, indirectly referenced
artifact closure and final installed device workflow qualification remain outstanding.

### Independent historical archive readback

`runtime-inspect-legacy --directory <archive> --digest <expected-receipt-sha256>` verifies a retained
archive without reading original job directories. It binds the receipt to the caller's saved digest,
checks its version/disposition, verifies each job's exact nested file/directory membership, raw hashes,
byte counts and private archive modes, and rejects unexpected archive-root entries. Historical modes
remain metadata, not executable permissions on archived content. Archive writing now refuses receipts
larger than the reader's bound rather than publishing evidence that its own reader cannot consume.

Typechecking and the focused archive test pass. The test removes the original fixture store before
successful readback, then checks mismatched receipt identity, changed evidence bytes and unexpected
files. Source-CLI readback of the retained real baseline archive verifies all fourteen jobs and
29,375,805 bytes. Evidence remains external. Installed qualification of this readback command and its
integration into activation completion obligations remain pending; archive verification does not
prove legacy dispatch exclusion or indirect external-artifact closure.

### Retained-history completion requirement

`runtime-complete` accepts paired `--history-archive` and `--history-digest` inputs. Normal activation,
host-instruction transition and forward repair bind that archive to the exact workspace and register a
`legacyHistoryDigest` completion requirement before installation writes. Finalization independently
reads the archive, checks replay identity and repeats readback after the launcher probe before releasing
maintenance. Existing maintenance-end and finalization paths cannot omit a registered history proof.
This reuses the existing typed completion table without introducing another state store or schema.

Typechecking and seven focused archive/maintenance/admission tests pass, including requirement survival
across reopening, missing-proof refusal, wrong-workspace refusal and corrupt archive refusal. The
existing staging/activation regression also passes. Full successful activation with a selected history
archive and packaged CLI qualification of the new options remain pending; these lower-level tests are
not that broader proof. Dispatch exclusion and indirect artifact closure remain separate unfinished
migration requirements. No adopter installation was activated.

### History-bound activation and package proof

The activation fixture now creates a retained legacy archive and completes installation through the
command parser with `--history-archive` and `--history-digest`. Successful completion releases maintenance;
retry with the same history returns the same result. Retry without the required history or after
corrupting archived result bytes refuses. Restoring the original bytes permits readback again. The
fixture also retains the existing installation, launcher, backup, recovery and identity checks.

Typechecking, build and the focused staging test pass. Eleven history/inventory/migration/maintenance/
admission/staging tests pass against a fresh offline installed preview package. This closes the prior
normal-activation success and packaged completion-parser/readback gaps. The activated candidate inside
the staging test is synthetic; no live adopter activation or device qualification is inferred. Temporary
proof installations were removed and evidence remains external. History combined with host-instruction
updates and forward repair still needs explicit combined-path proof; legacy dispatch/resource exclusion,
external-artifact closure and final installed RN workflow qualification remain open.

### Combined history, repair and host-update proof

The staged activation fixture now carries the same retained history through forward repair after new
writes and through host-instruction migration. It verifies repeat completion, retained SQLite evidence,
authored instruction content/modes and refusal to omit history on completed repair readback. The first
combined run exposed an ambiguous negative assertion: both history and repair proof were absent. The
fixture now supplies valid history while testing missing repair proof, so it tests the intended guard
without depending on completion-requirement iteration order. No runtime change was needed.

The broad engine run passed 295 of 296 tests with that single assertion failure; the corrected staging
test then passed. All 72 continuity tests pass. Eleven installed-package tests, including the corrected
combined fixture, pass against the unchanged prior archive; no rebuild was needed for a test-only
change. Separate external evidence retains the failed run and corrected qualification. These proofs
close combined-path behavior in isolated fixtures, not live adoption, complete artifact-reference
closure, legacy dispatch exclusion or final installed device-workflow qualification.

### Legacy dispatch exclusion prerequisite proof

Source inspection confirms installed legacy readers and provider entry points share the repository's
OS `flock` on `.governance/runtime-use.lock`; the new generation registry does not acquire that lock.
A real isolated-baseline experiment acquired the existing lock exclusively and verified the installed
legacy `runtime_reader` refused admission, then admitted it after release. The reverse experiment held
a real installed reader, verified exclusive acquisition refused, released that reader and acquired the
exclusive lock successfully. Both experiments closed descriptors and left no worker running.

This establishes the existing installation-reader exclusion primitive and its drain behavior. It does
not qualify arbitrary external Python installations, project device locks or an atomic TS cutover.
Migration orchestration still must hold this legacy lock continuously through inventory revalidation,
backup and replacement while respecting the new engine's maintenance boundary. Lock-file existence
alone is not proof, and SQLite maintenance alone must not be reported as legacy dispatch exclusion.
The experiments changed no installed runtime or job record; external receipts retain their narrow scope.

### Legacy lock handoff experiment

A separate isolated process experiment acquired the existing `flock` style of exclusive lock in a
short-lived launcher and inherited the open descriptor into a Node child. After forcibly terminating
the launcher, a competing exclusive acquisition still refused while the child lived. Closing the
controlled child's input let it exit, after which exclusive acquisition succeeded. The descriptor,
child and temporary directory were cleaned up; no adopter lock or runtime was changed by this test.

This proves the descriptor handoff needed for a one-time legacy cutover on the current host, including
launcher loss. It does not yet implement the cutover coordinator, prove Linux behavior or make a plain
inherited descriptor an authenticated assertion of exclusive ownership. The final migration path must
acquire the actual old owner's lock and keep the same open descriptor alive through replacement;
legacy compatibility behavior must not become a permanent alternate runtime.

### One-time legacy migration invocation

`legacyMigrationInvocation` now constructs a transition-only invocation using the old installation's
Python in isolated mode to acquire its existing exclusive OS lock, validate the lock inode and refuse
an unfinished legacy update. It replaces that process with the selected Node script, retaining the open
lock descriptor. There is no persistent Python supervisor or ordinary-runtime Python path. The helper
validates the legacy package lock and canonical governance location before constructing the invocation;
this is a migration primitive, not yet a complete user-facing cutover command.

Typechecking passes. A read-only source-level experiment used the isolated baseline's real old
interpreter, transferred its lock into Node and verified a competing shared reader was refused. Node
exited and a separate exclusive acquisition then succeeded, proving release. No installation files or
job records changed. This qualifies the actual handoff primitive on this host; installed packaging,
coordinated inventory/backup/activation execution, interruption recovery and Linux qualification remain
pending. Ordinary runtime operation continues to use the selected compiled engine directly.

### Locked legacy identity and packaged launcher proof

The one-time launcher now binds the selected legacy package-lock bytes and rechecks their SHA256
while holding the old OS lock, before executing Node. A changed lock or pending old update refuses
without running the migration script. Focused proof creates a temporary old-style interpreter environment,
checks both refusals, executes Node with the inherited descriptor and confirms exclusive reacquisition
after process exit. This closes the select-before-lock identity gap in the launcher primitive.

Typechecking, build and twelve installed-package migration tests pass, including the new launcher test
and combined history/host/forward-repair fixture. Temporary interpreter and package environments were
removed. This is packaged migration-primitive proof, not a complete cutover coordinator or live SDK
activation. Linux, shared project-resource exclusion, full artifact closure and final installed RN
workflow qualification remain open alongside the remaining E1–E5 acceptance requirements.

### Legacy entrypoint retirement scope correction

Reviewing the actual installed entry surface found that replacing only `project-governance` leaves
`harness-agent` available from the old wheel. Project-file migration discovery now includes both
existing entrypoints as ordinary canonical backup inputs, names the main launcher for replacement
and the old provider entry for retirement after qualified cutover. This records scope; it does not
unlink executables or claim that retirement/recovery is implemented. Alias entrypoints refuse.

Typechecking and six focused discovery/maintenance tests pass. The isolated baseline's refreshed
external plan contains 77 inputs, including both entrypoints, with unchanged Git status. Earlier plans
remain retained and will fail freshness comparison against this expanded discovery contract. Coordinated
cutover must handle old-entry retirement with pre-write rollback and post-write forward repair; the
current launcher-only activation primitive does not satisfy whole-runtime retirement by itself.

### Backed legacy provider entry retirement and restoration

Wheel-to-engine preparation now retires the backed `harness-agent` entry after replacing the main
launcher. Retirement requires owned drained pre-write activation, exact backup ownership/revision and
unchanged original bytes/mode; an unbacked or edited entry refuses. Repetition after removal is inert.
Final activation readback refuses while any old provider entry remains. Compiled-to-compiled updates
do not automatically delete similarly named files.

Pre-write wheel recovery now includes the backed retired entry, restoring its original bytes and mode
when absent or already restored. A recreated unrelated entry refuses before restoration begins. The
existing completed-recovery replay checks include this restored entry. Typechecking and the focused
staging/activation test pass for unchanged retirement, changed-file refusal, absence, repeat retirement,
unrelated replacement refusal and exact restoration/replay. This test exercises the retirement primitive
under synthetic activation; installed packaging and the full coordinator path remain pending. No live
adopter entrypoint was removed. This does not claim removal of the entire old environment or resolution
of the separate legacy dispatch/resource and artifact-reference requirements.

### Automatic entrypoint retirement package checkpoint

The wheel-migration fixture now follows successful pre-write restoration with a fresh backed normal
transition. The normal transition automatically removes the backed old provider entry, completes native
launcher readback, releases maintenance and permits identical completion replay without restoring the
old entry. This exercises orchestration through `completeRuntimeTransition`, beyond the prior direct
retirement helper test.

Typechecking, build and the focused transition fixture pass. Twelve installed-package migration tests
pass in a fresh offline preview archive, including expanded entrypoint discovery, retirement/refusal/
restoration and automatic cutover replay. Temporary proof installations were removed. The installed
fixture uses synthetic legacy entrypoints; no live adopter cutover occurred. The coordinator still must
join the legacy OS lock, new maintenance state, complete inventory, artifact preservation and accepted
workflow qualification in one recoverable operation before product activation.

### E4 Metro reuse defect reproduced

Revisited the actual RN runner rather than treating its current behavior as the target. Its reuse path
accepts a listening TCP port plus a live recorded PID and client-log flag without proving listener,
Metro service or workspace identity. A controlled local non-Metro TCP server with a synthetic ownership
record reproduced `ensureMetro` returning successful reuse. The experiment used only its own server
and process, took the reuse branch without signalling anything and closed the server afterward. The
source and external reproduction receipt are retained; no SDK source or installed app changed.

This is a concrete gap in stale/wrong-workspace service acceptance, not proof of the cause of every
reported Metro incident. E2/E4 qualification must repair or replace this reuse decision before claiming
the required service-identity matrix. A fresh device listing also confirms the selected iPhone remains
connected; that is availability evidence only, not app/device workflow qualification. The next adapter
change must verify the owned process/listener/workspace identity and preserve unrelated services.

### Isolated RN adapter ownership correction

The authorized isolated adopter adapter now verifies persisted Metro ownership against process-start
identity, canonical working directory, expected local CLI path and the actual listening socket before
reuse or restart. Unverifiable old records and different-workspace listeners are preserved rather than
signalled; explicit occupied ports report conflict. New launches must pass owned-listener verification
before their identity is recorded. This is project-owned adapter behavior, not product-specific code
inside the neutral engine checkout.

Twenty-five focused tests pass: controlled unrelated listeners survive both ordinary and fresh requests,
a controlled CLI listener matches its correct process/workspace/port, mismatched identities refuse, and
existing Metro state/diagnostic tests remain green. An initial fixture socket emitted ECONNRESET when
the probe disconnected; handling that fixture event corrected the test without changing runtime code.
The SDK context router had no matching route; its documented index fallback supplied owner guidance.
External evidence binds changed file hashes. Real Metro/device proof remains pending. `ps` identity
resolution and the dependency on available `ps`/`lsof` observations remain explicit limits; this does not
yet bind bundle/configuration identity or complete the Metro recovery matrix.

### Metro restart wait and real service proof

The isolated runner's restart path incorrectly waited for port presence and then inverted the result,
which could fail immediately while a signalled listener was still shutting down. The wait now accepts
the required socket state, uses a monotonic deadline and evaluates the observed state before rejecting
an expired deadline. Restart callers explicitly await absence; startup callers await presence.

All 26 focused Metro tests pass. A bounded real-Metro experiment then passed cold startup, warm reuse
and restart to restore the declared client-log capability without cache reset. Both recorded Metro
processes exited and the temporary port had no listener on independent readback. The experiment ran
in its own process group with a bounded outer cleanup owner; unrelated services were untouched.
External evidence records logs, process identities, source hash and cleanup observations. This qualifies
real service orchestration on the current host, not delivered bundle identity, app readiness or device
scenario assertions. Those remain part of the open E2/E4 matrix.

### Failed Metro startup process cleanup

The isolated adapter previously sent TERM and threw immediately when owned listener verification failed,
leaving the caller without proof that the child had exited. It now awaits the live child handle, escalates
to KILL after a bounded grace period and distinguishes startup failure with observed exit from unresolved
cleanup. Repeated cleanup of an exited child is inert. This only establishes the directly owned process
exit; descendant/resource cleanup remains the enclosing workflow owner's responsibility.

All 27 focused Metro tests pass, including a real controlled child that acknowledges readiness, ignores
TERM, exits on escalation and permits repeated cleanup observation. No unrelated process is signalled.
The earlier real-Metro cold/warm/restart evidence remains unchanged; this turn qualifies its separate
failed-start child-cleanup path. Full bundle/device and shared-resource qualification remain open.

### Separate native and embedded-JavaScript artifact observations

The isolated RN certification resource proof now includes bounded SHA256/byte identities for its native
executable, optional native debug library and embedded `main.jsbundle`. Missing, nonordinary, aliased,
oversized or changing required artifact files refuse. The proof explicitly records app consumption as
not observed; it does not equate file presence with delivered execution. Frameworks, resources and
source/toolchain provenance still require their own complete identity coverage.

Focused tests pass for independent JavaScript/native changes, debug-library capture, missing files,
symlink refusal and invalid executable names. The existing real simulator artifact passes resource
validation and identity capture: its debug library is 15,339,776 bytes and JavaScript bundle 7,903,479
bytes. External receipts retain the initial entrypoint-only observation and corrected debug-library
observation separately. No app was launched or rebuilt for this readback. Device consumption and full
artifact closure remain open E2/E4 requirements.


### Complete packaged-app tree identity

The isolated certification adapter now captures all ordinary packaged files and directories, including
frameworks, resources and signatures. It bounds tree depth, entries and bytes, refuses aliases and special
files, and checks tree metadata again after hashing. Selected executable, debug-library and JavaScript
identities come from that same snapshot rather than subsequent independent file reads. This closes the
previous entrypoint-only gap without treating packaged bytes as source provenance or app consumption.

The focused fixture passes independent native/JavaScript/framework changes, empty resources, missing or
empty required artifacts, and alias refusal. Readback of the existing simulator package captured 402 files
and 489,657,000 bytes; the complete manifest is retained outside the checkout. No rebuild or device launch
occurred. Installed consumption, source/dependency/toolchain binding and the remaining E4 matrix are open.


### Simulator installation identity gate

The isolated simulator runner now resolves the installed application container immediately after install
and refuses launch if its paths, member kinds or file bytes differ from the qualified build. Permission
modes remain in the complete package manifest but are excluded from content comparison, because install
permissions can differ. This is installed-byte readback, not runtime consumption or source provenance.

Two focused tests pass, including a stale framework with unchanged executable/JavaScript and permission
normalization. Runner syntax validation passes. Offline readback of the previously installed simulator
app found different native content and a smaller tree than the current build; that previous installation
cannot qualify the current package. Full manifests are retained externally. The simulator stayed shut
down, no installation was replaced and no old scenario result was promoted to new evidence. A fresh
owned install-and-scenario run is still required to qualify this gate on the live workflow.


### Migration handoff script identity

The transition-only legacy lock handoff now pins the selected migration script's bounded UTF-8 bytes
and rechecks its SHA256 after acquiring the exclusive OS lock, before replacing the interpreter with
Node. A script edited between invocation construction and handoff refuses execution. The focused real
venv/lock test passes changed package lock, pending transaction, changed migration script, successful
handoff and post-exit lock release; TypeScript checking passes. This protects cooperating migration
input selection, not an adversarial path swap after validation. Full coordinated E5 cutover remains open.

A separate simulator installation/scenario batch was submitted with 485 manifest-bound inputs and native
completion delivery. Its outcome must be assessed on receipt; submission establishes no passing proof.


### Integrated source regression after migration and lifecycle changes

The complete `npm test` command passes against the current source, including the engine suite and all
72 continuity tests. This supersedes the earlier broad-run failure followed only by focused correction.
It verifies the repository's automated source contracts; it does not qualify final installed-artifact
behavior, adopter cutover, device execution, provider-host restart or measured workflow benefit.
The separate simulator batch is assessed through its existing completion result, without duplicate work.


### Resource-protocol refusal before workflow dispatch

The new simulator qualification uncovered a real pre-execution failure: the persisted host resource
registry remained at protocol 1, and the protocol-2 worker exited during registry construction before
claiming its queued run. No simulator adapter ran. The original submission was reconciled to blocked
after observing its worker absent, no command directory, no held host resource claims and the dedicated
simulator shut down. External recovery evidence supports the cleanup acknowledgment; no test was relaunched.

Workflow dispatch now checks resource-registry compatibility synchronously before creating its worker
reservation or retaining a runtime reader. The integration fixture verifies protocol-1 refusal leaves
no reservation or new reader, then an explicitly drained migration permits exactly one successful
workflow. Focused worker proof and TypeScript checks pass. A live host-registry migration and fresh
installation qualification remain necessary. Legacy handoff coverage also now verifies an active shared
reader prevents migration and handoff succeeds only after that reader exits.


### Drained host resource-registry migration

The persisted preview host registry was backed up through SQLite's backup API, then migrated using
`ResourceRegistry`'s explicit drained protocol-1-to-2 path. Before/after readback preserved all six
resource records, seven request records and the host identity; no held leases existed. The original
registry backup and migration receipt remain external. This qualifies this host's registry transition,
not the full legacy-runtime cutover.

The preview was rebuilt and a fresh simulator installation/scenario batch submitted with 670 hashed
inputs covering adapter scripts, app package files and compiled runtime JavaScript. The first request
was rejected before submission because including every packaged documentation asset exceeded the
256-KiB request limit; the accepted manifest excludes non-JavaScript runtime assets and remains a
declared scope, not full environment provenance. The adapter now shuts down the simulator only after
its own successful boot. The new batch outcome remains unproven until completion assessment.


### Durable worker startup refusal

A registry failure after dispatch preflight now records a bounded startup-failure receipt and atomically
blocks only an untouched queued run with the exact request binding. Its pending stages become blocked;
no command is started, and the retained generation reader is released. Runs that already have an owner,
changed binding or started stages cannot be closed through this path. Diagnostic codes do not include
arbitrary exception text. This closes the observed silent queued-run failure for registry initialization.

Nine focused workflow tests pass, including a real worker launched against protocol-1 state after request
creation, no command directory, a bound failure receipt, and rejection of changed or already-started runs.
TypeScript checking passes. The compiled payload was left unchanged during the in-flight simulator batch;
this source change still needs the next package build and installed proof.


### Live installed-package proof and cleanup timing failure

The fresh simulator run passed the canonical two-exercise scenario, three-rep observation and
background/foreground assertions. Installed readback matched all 402 package files and 489,657,000 bytes;
the declared 670-input batch manifest remained valid. This is meaningful installed-byte and scenario
proof, while fresh-build/source/toolchain provenance remains separate.

The enclosing workflow nevertheless recorded unknown cleanup: the command exited zero, but its guardian
published bound process-absence recovery shortly after the workflow consumed the original unknown
receipt. The wrapper then failed because the workflow resource-cleanup receipt was absent. Recovery
reused this submission, verified all recorded process IDs/group absent, observed the simulator shut down,
reconciled the command and released exactly its held simulator lease with a bound external receipt.
The outer cleanup acknowledgment is evidence-backed; the original unknown workflow and failed test
outcomes remain preserved. No scenario was relaunched. Consuming late cleanup proof in workflow
reconciliation is an open implementation defect exposed by this run.


### Consume late command cleanup without replay

Workflow execution now gives a terminal command's independent guardian cleanup proof a bounded
five-second observation window. A matching proof reconciles retained command obligations and records
both the original receipt digest and recovery-file digest in the derived stage result. Only a known
native `exit` with a recorded exit code can resolve an unknown execution state this way; the recipe's
accepted exit codes determine success versus failure. Missing proof and lost execution remain unknown.
The original command receipt is never rewritten, and resource-specific readback is still required.

Five focused recovery/executor tests and TypeScript checking pass, including delayed proof, accepted
and rejected native exit codes, retained unresolved resource claims and original-byte preservation.
The new resolver also assessed the existing simulator command as successful with confirmed cleanup
without reexecution; its original receipt and previously unknown workflow remain unchanged. The external
derived assessment is separate from the failed outer batch. A rebuilt workflow qualification is still
needed before claiming the timing defect closed in the installed execution path.


### Integrated regression for worker-startup and late-cleanup fixes

The full source suite passes after the new startup-refusal and late-cleanup handling, including all
continuity tests. The rebuilt preview is separately under simulator qualification with 671 declared
hashed inputs. Source regression does not replace that workflow result or final installed-package proof.
No release/cutover readiness is inferred; coordinated migration/recovery, the remaining device matrix
and measured accepted-work benefit remain requirements under the original E5 gate.


### Rebuilt simulator workflow succeeds end to end

The rebuilt compiled preview completed fresh installation, exact packaged-content readback, the canonical
two-exercise/three-rep scenario, background/foreground recovery, simulator shutdown and bound resource
lease release without manual intervention. The batch succeeded in 95.096 seconds, with all 671 declared
hashed inputs manifest-verified and cleanup confirmed. Installed readback matched 402 files. External
batch, scenario, workflow and resource receipts retain their separate evidence identities.

This run used a directly confirmed command-cleanup receipt; it therefore verifies the rebuilt normal
path, not a newly forced late-cleanup race. The late-proof branch remains covered by its focused real
command fixture and readback of the earlier failed batch. Prepared-build provenance, physical-device
qualification of these changes and final installed-product proof remain open. This timing is one sample,
not a measured improvement claim or release acceptance.


### Physical requalification preflight

The selected physical target is connected and its prepared package passes complete identity capture.
However, current installed-app/process readback finds the dedicated qualification bundle already running,
with no current owned launch receipt for that PID; other development applications are also running.
The physical cleanup observer correctly refuses an idle observation. No process was signalled and no
installation or test was started. External evidence records the exact device/app/process observation.
Requalification of the rebuilt engine on physical hardware remains pending ownership reconciliation;
this does not prevent independent source, migration or packaging work.


### Installed package proof for startup and cleanup fixes

The current compiled archive was built with bundled pinned dependencies, staged offline in a clean
temporary installation and tested through its installed JavaScript modules. All 15 selected tests pass:
workflow authority/state, startup protocol refusal, detached execution, native failure/cancellation,
resource cleanup, late bound recovery and legacy migration lock/script checks. The temporary test
installation was removed; archive, logs and proof remain external. This is a dirty preview artifact,
not an immutable release source or publication.

A separate retained installation of that exact archive now drives a fresh simulator workflow through
its installed entrypoint. Its batch binds 672 input files including the archive and installed runtime
JavaScript. The old wheel only provides the external batch completion envelope; adopter hooks and the
active product lock remain unchanged. Assess this installed-path workflow on completion before accepting
its claim. Physical ownership reconciliation and full product activation remain open.


### Installed simulator attempt: adapter failure preserved, cleanup passed

The first installed-archive simulator batch failed before scenario launch, after successful install and
container lookup. All declared inputs remained valid. The engine preserved exit-one failure, independently
observed simulator shutdown and released the owned lease; outer cleanup is confirmed. This qualifies
failure propagation/cleanup in the installed path, not successful installed-product scenario execution.

The adapter printed only an AggregateError's outer stack, discarding both the primary and cleanup error
members. Its logger now prints the Error object so subsequent evidence preserves those causes; syntax
validation passes. Post-run installed readback matches all 402 source files exactly. That later match
does not identify the transient failure or justify changing the identity gate. No test was relaunched
from the completion notice; successful installed-path qualification remains open pending diagnosis.


### Reuse backed project-file scope at completion

`runtime-complete --project-plan` now accepts the saved discovery scope already used by backup, alongside
explicit extra file/database inputs. It verifies the saved plan digest, workspace containment, unique
paths and every selected kind/hash/mode against the verified backup. It does not rediscover live files
after transition writes, so completion/recovery can retain the original scope. Existing transition checks
still require the complete declared input set to match the backup and do not infer external-store coverage.
Host-instruction changes continue through their explicit host-plan boundary.

Three focused migration-plan tests and TypeScript checking pass. The new case permits resumed use after
live source changes but rejects another workspace or altered source identity even with a recomputed plan
digest. Full completion/installed-package integration of this new CLI option remains to be qualified.
The installed simulator diagnostic remains bound to its existing immutable preview archive.


### Installed simulator success exercises late cleanup recovery

The diagnostic installed-archive batch succeeded in 94.062 seconds with manifest-verified inputs and
confirmed cleanup. Exact installed content matched 402 package files; the two-exercise/three-rep scenario
and background/foreground assertions passed. This run actually exercised the late-cleanup branch:
the original command receipt retained unknown cleanup, while the workflow recorded successful native
exit plus confirmed cleanup with matching original/recovery digests. The simulator lease was released
without manual recovery. This supplies installed-path evidence for the previously observed cleanup race.

The earlier pre-launch adapter failure did not recur and remains unexplained; a successful diagnostic
run does not erase it or establish a reliability improvement. No further rerun was triggered by the
completion notice. These claims cover the retained preview archive and prepared simulator package,
not later source changes, a fresh build, physical hardware or full E5 activation.


### Backed-plan completion integration and package proof

The activation integration fixture now uses a real discovered project plan for backup and passes that
saved plan to `runtime-complete`. It verifies maintenance closes, the backed legacy provider entrypoint
is retired, and identical completion replay succeeds after live lock/launcher changes. Source integration
and TypeScript checks pass. A fresh offline-installed archive then passes all 13 selected migration tests,
including that completion/replay path, stale-plan refusal, history preservation, legacy lock handoff,
retirement/restoration and forward-repair admission. The temporary installation was removed and archive,
logs and proof retained externally. No adopter activation, publication or broader workflow requalification
is inferred from these migration fixtures.


### Physical continuation through a separate qualification bundle

The existing project runner supports explicit target-scoped bundle identity and signing overrides;
it skips installed-bundle inference when that identity is supplied. A second dedicated qualification
bundle allows physical testing without terminating or replacing the already-running qualification app.
The authorized isolated worktree now has a fresh build/install/scenario batch using the retained
installed engine archive and exact new app/device claims. Existing device locks and process-specific
cleanup remain effective. The running unrelated applications were not signalled.

The batch binds 270 adapter/runtime/archive files. Fresh build outputs are produced during execution
and captured by the package-identity helper; this declaration does not certify complete source,
dependency or toolchain provenance. Outcome, physical assertions and cleanup require completion
assessment. No physical pass or source-wide qualification is inferred from submission.


### Schema-independent resource protocol status

`resource-status [--registry PATH]` now reads SQLite's version field without initializing, migrating or
interpreting resource tables. It distinguishes missing, protocol-compatible, migration-required and
incompatible state, explicitly limited to protocol observation. Canonical ordinary-file checks refuse
aliases. This supplies the E1a diagnostic needed when a pinned client cannot open another protocol.

The focused test preserves exact bytes of legacy/current/future-version stores and leaves a missing
registry absent; TypeScript checking passes. Source CLI readback reports the actual host at protocol 2.
This is not resource integrity, holder/drain or execution-readiness proof. The concurrent physical batch
continues using its unchanged installed archive; no SDK or installed-package input was modified.

### Legacy handoff exclusion and migration checkpoint

The legacy handoff fixture now verifies the inherited descriptor names the original lock inode and
attempts independent shared-reader and exclusive-migrator acquisition from the Node process. Both
are refused while Node owns the inherited lock; acquisition succeeds after it exits. This closes a
proof gap where the fixture previously checked only that an open regular-file descriptor survived.

The focused handoff test passes. Current TypeScript checking, compilation, resource-status,
migration-plan and staging/completion tests also pass (five tests in the combined run, plus the
handoff test). The complete migration coordinator, adopter activation and final installed-artifact
qualification remain outstanding. These checks do not authorize or claim cutover.

### Installed physical diagnostic passed; earlier instability remains explicit

The fresh-build physical diagnostic passed in 121.120 seconds through the retained installed preview
archive. The receipt hash was verified, declared inputs remained manifest-valid, the scenario observed
both exercise transitions and three counted repetitions, and command plus physical-resource cleanup
were confirmed. Both resource generations were released automatically against the same observation.
The runner's launch-bound cleanup no longer confuses other installations sharing its executable name.

The earlier package-tree mutation did not recur; its cause remains unresolved. This pass does not
establish reliability improvement or full installed-byte/source/toolchain provenance on the device.
The separate cleanup-only workflow recovery helper now verifies historical resource-release events,
so later resource acquisitions cannot erase earlier release evidence. It refuses stale bindings and
unresolved command effects and preserves failed assertions. TypeScript checking and nine focused
workflow-store tests pass; the prior cleanup-recovered run now records failed instead of unknown.
The helper still needs CLI/installed-package integration; the successful device archive predates it.

### Cleanup-only recovery CLI

`workflow-reconcile-cleanup --database PATH --run ID --revision N --receipt PATH` now exposes
receipt-bound recovery through the normal command entrypoint. An optional `--registry PATH` selects
an existing resource registry. Recovery requires an exact historical release event for every declared
resource and the same workflow binding/owner; it does not release resources itself or dispatch work.
Completed failed assertions remain failed (shell exit 1). Unknown command outcomes remain unknown,
even if the device is clean. Later acquisitions stay held and do not invalidate prior release history.

TypeScript checking and ten focused workflow tests pass, including CLI recovery, stale/binding refusal,
newer-holder preservation and rejection of unknown commands without a state/revision change. Installed
archive proof of this command and generation-reader retirement after recovery remain to be integrated.

### Installed recovery and retained-reader qualification

A fresh offline-installed archive passes all 19 selected workflow, resource-status, migration-handoff
and staging tests. This includes cleanup-only CLI-handler recovery, unknown-command refusal, historical
release verification after later resource acquisition, and generation-reader release/replay. A wrong
reader revision is refused; another retained worker remains protected. Managed invocation now admits
`resource-status` as read-only and `workflow-reconcile-cleanup` as potentially writing state.

Recovery accepts optional `--worker-directory` to verify the saved request/owner binding and confirmed
worker absence before releasing its retained generation claim. Terminal replay rechecks the cleanup
receipt and outcome, allowing interrupted reader retirement to finish without repeating commands.
The temporary archive installation was removed; archive, logs and proof remain outside the checkout.
This does not qualify adopter activation or transfer earlier device results to the newer archive.

### Isolated adopter target-check interpreter migration

The authorized isolated adopter now declares its own pinned Python validation dependency and explicit
setup command. Ten project validation packs invoke that project-owned environment instead of the old
wheel interpreter. The environment was installed offline from the previously qualified dependency;
it contains no governance runtime. Four validator imports and three bounded direct checks pass, with
identical result JSON and exit codes under the old and new interpreters. The compiled transition's
wheel-interpreter dependency guard now passes for those packs.

The checker scripts and delegated Gradle/Node operations are unchanged. This does not establish full
pack execution or resolve the earlier independent toolchain watcher timeout. The active adopter and
its current operational runtime were not changed. Further cutover scope, backup and activation proof
remain required.

### Remaining product-lifecycle integration priority

Current-source audit confirms the migration dependency guard and both pack-loading paths select only
`.yaml` target pack files. The isolated adopter's migrated validator entrypoints do not independently
invoke the wheel interpreter; the toolchain checker delegates Python work through `sys.executable`.
The full project-owned-interpreter toolchain batch is pending assessment.

The next E5 implementation priority is the unified product lifecycle coordinator. Current CLI routing
exposes staging, maintenance, backup and completion primitives but does not expose `init`, `update` or
`repair`. Existing staging/completion fixtures qualify constituent operations, not those missing user
flows. Integrate preflight and candidate staging, legacy/new admission ownership, declared history and
backup coverage, transition and readback, and interrupted-operation recovery under one durable operation
identity before claiming product lifecycle completion. Preserve the selected forward-repair policy and
avoid activating the isolated adopter before the existing acceptance gates are met.

### Project-owned Python toolchain pack qualified

The isolated adopter's full toolchain pack now passes all 164 contract tests through its project-owned
Python environment. The batch completed in 8.088 seconds with verified receipt hash, manifest-valid
inputs and confirmed cleanup. The prior failure was the obsolete contract requiring the wheel
interpreter; that contract now requires the project interpreter. Both relevant CI workflows explicitly
prepare the environment before checks, and environment setup changes select the toolchain pack.

This validates the local toolchain pack and static workflow contracts, not execution on GitHub or all
other target packs. The earlier watcher timeout did not recur in this batch but remains historical
instability, not a diagnosed repair. No additional batch was launched from the completion notice.

### Durable staging operation for lifecycle coordination

`runtime-stage` now accepts exactly one of the existing `--destination` or the new
`--operation-directory`. The latter exclusively reserves a staging operation, binds its archive bytes
and compiled lock, and reuses the one verified candidate on identical replay. Changed identity,
missing request, no candidate, or a still-preparing candidate remains unresolved without creating
another installation. This is a staging receipt, not a second runtime activation authority.

TypeScript checking and the staging integration fixture pass, including actual CLI replay and the
request-only/empty-candidate/preparing-candidate interruption cases. The full init/update/repair
coordinator must still own admission, backups, history, completion and recovery; candidate staging
alone does not implement those flows or authorize adopter activation.

### Composed installation preparation

The lifecycle preparation function now binds workspace, registry, archive/lock, expected revision and
backup scope in one saved operation. It stages one verified candidate, acquires maintenance using a
persisted UUID token, and creates or verifies the original backup. Identical replay retains the same
candidate, maintenance token and backup digest. Changed operation identity is refused. Preparation
records explicitly state that activation has not occurred.

TypeScript checking and the staging integration fixture pass for this composed path. Legacy writer
exclusion is still a caller obligation; the eventual lifecycle entrypoint must own it across preparation
and transition. No public init/update/repair completion or adopter cutover is claimed here.

### Saved-operation activation and host obligation binding

Completion of a saved preparation now checks the operation digest, candidate identity, backup digest
and maintenance identity before invoking existing activation/readback. It supports a host-instruction
plan and legacy-history archive requirement bound inside the original operation identity; those use
the existing mandatory completion gates. Successful completion records a receipt and remains
replayable through the authoritative generation finalization record.

TypeScript checking and the staging integration fixture pass for a composed compiled update with a
bound host plan, candidate-substitution refusal and completed replay. This does not yet qualify a
legacy-history requirement through the composed path, public lifecycle commands, legacy exclusion
across the whole operation, or a real adopter activation.

### Composed history requirement qualification

The composed-update fixture now archives a terminal failed legacy job, binds its archive receipt into
the saved operation, and includes host-instruction completion. Altering the archived result refuses
completion before generation state changes. Restoring the original fixture bytes permits activation
and finalization; identical completion replay succeeds with history verification still enforced.
TypeScript checking and the integration fixture pass. This closes the composed-path history test gap,
not external-reference coverage, legacy writer drain, or real adopter migration completeness.

### Legacy exclusion persists across lock-format transition

Composed preparation and completion now require the cooperating migration launcher's inherited
workspace-lock descriptor when the source installation uses the wheel. The saved operation retains
that requirement after the package lock changes to the compiled format, so interrupted completion
cannot infer that old-runtime exclusion is no longer needed. Descriptor identity checks name the exact
canonical workspace lock; the launcher remains the OS-lock owner, not an adversarial-user boundary.

The locked-launch fixture verifies normal direct legacy entry is refused and a persisted requirement
still refuses missing ownership after lock-format change. TypeScript checking and composed staging
integration also pass. The public lifecycle entrypoint must still dispatch through this handoff and
preserve it across the complete migration rather than asking callers to assemble the steps.

### Public update/repair composition and request admission

The CLI now composes update and forward repair from an explicit installation request and discovered
project plan. Updates bind the discovered host-instruction plan. Legacy updates acquire the old
runtime's workspace exclusion through its Python handoff and retain that requirement for replay after
the package lock changes format. The handoff carries the original request digest; rereading different
request contents refuses before creating operation state. Backup input shape, absolute paths,
duplicates, bounded count and nonnegative revision are checked before preparation.

TypeScript checking and the staging integration fixture pass. The fixture exercises a fresh legacy
update through the real CLI (including lock handoff, activation, provider retirement and replay),
compiled forward repair preserving SQLite evidence, and malformed or changed requests leaving no
operation directory or registry. These are temporary-project source tests. Public init, the broader
interruption matrix, current installed-artifact qualification and actual adopter cutover remain open.

### First-installation lock and launcher writes

The existing backed transition now accepts verified absent records for the product lock and launcher
when the backup has no previous compiled generation. It creates those files through the same
activation, readback and finalization path used by updates. Existing parent directories must be
canonical; symlinks and non-file targets are refused. A different file appearing after backup is
preserved and blocks completion. Matching installed content remains replayable.

TypeScript checking and the staging integration fixture pass for first creation, refusal and
preservation of newly authored lock/launcher contents, resumed completion, executable permissions and
completed replay. This qualifies the write primitives with prepared directories; it does not yet
provide public init, directory preparation/discovery, or real adopter installation.

### Public init through the shared lifecycle

The CLI now accepts init with an explicit pinned archive request and discovered project plan. Discovery
includes absent product lock and launcher targets. Init requires revision zero, absent targets, no
legacy-history migration, and a new registry unless replaying its saved operation. It creates the
canonical product directory layout and uses the same staging, maintenance, backup, activation,
host-instruction and finalization machinery as update. Existing installations are refused.

TypeScript checking and the actual CLI integration fixture pass for an empty temporary project,
managed host instructions, finalized activation, identical replay and second-init refusal. All three
migration-plan tests pass; the backup fixture now prepares the product directories as init does.
This is source-level lifecycle proof, not complete project-profile onboarding, installed-package
qualification, interruption-matrix completion or an adopter cutover. Those remain required work.

### Lifecycle checkpoint and missing-directory backup repair

The grouped lifecycle checkpoint exposed a real regression: discovery includes absent product files,
but the backup primitive required their immediate parent directories to exist. Backup now binds
absence to the nearest existing canonical directory and rechecks that ancestor identity, any newly
present intermediate directories, and the missing target. It does not create project directories.

All 29 grouped runtime, host-instruction and legacy-launch tests passed after the repair. A subsequent
intermediate-directory alias guard received focused backup/maintenance tests and TypeScript checking.
Managed launcher update/repair dispatch remains open: ordinary invocation holds a reader, so adding
maintenance commands to its normal allowlist would prevent their own drain. The current explicit
lifecycle entrypoint remains separate until an owned maintenance dispatch path is implemented.

### Installed maintenance dispatch

The runtime-run entrypoint now sends update and repair directly to the shared lifecycle coordinator,
without acquiring an ordinary invocation reader. It binds the request to the launcher's canonical
workspace and registry, and refuses maintenance-probe use. Other commands retain existing reader
admission. The coordinator still owns maintenance, backup, activation and finalization.

TypeScript checking and the staging integration fixture pass. A mismatched launcher workspace is
refused before operation creation. An existing project reader blocks repair and remains owned;
after that reader releases, the actual runtime-run CLI completes and replays repair with no retained
readers and unchanged task evidence. This is source-entrypoint proof; current archive installation,
launcher script consumption and interrupted-process qualification remain open.

### Installed lifecycle package checkpoint

A newly built offline archive passed 13 lifecycle tests against its installed JavaScript. An additional
empty-project scenario consumed that archive to initialize, replay initialization, execute the actual
installed shell launcher for version readback, and perform/replay forward repair through that launcher.
Repair used a declared operational SQLite database and marked post-write generation, as its contract
requires. An earlier fixture without those prerequisites was correctly refused and was corrected;
no runtime relaxation was made. Temporary installations were removed, and external logs, archive and
proof receipt were retained.

This verifies current packaged init and launcher repair consumption, including the maintenance path.
The archive is a dirty preview rather than an immutable release. It does not establish real adopter
cutover, complete onboarding, the remaining interruption matrix or overall E0–E5 acceptance.

### Backup-to-preparation interruption checkpoint

The lifecycle fixture now removes the preparation receipt after the backup has committed, modeling
that persistence boundary. A new policy file then invalidates discovery: completion refuses while
retaining the same maintenance token, unchanged backup receipt and missing preparation receipt.
After restoring the fixture's original project scope, the public update command reconstructs exactly
the prior preparation receipt, uses the same candidate and backup, and finalizes successfully.

TypeScript checking and the integration fixture pass. This is deterministic boundary-state injection,
not a killed-process or power-loss test. It establishes that this valid committed backup is reused
without silently accepting scope drift; process interruption and other persistence boundaries still
need qualification.

### Fresh-project configuration defaults

Comparison with the existing installer identified three missing onboarding outputs: the minimal
profile, empty facts document and ignored runtime directory policy. Migration discovery now includes
these files or their absent records. Init creates absent defaults under owned maintenance from the
initial backup, preserves pre-existing files, refuses conflicting new content, and verifies generated
content on replay. File and parent writes are synced; no Python bootstrap is introduced.

TypeScript checking, migration-plan tests and the lifecycle integration fixture pass, including exact
default contents and init replay. This closes these missing outputs, not complete hook/skill onboarding.
The prior installed-package checkpoint predates this change; current packaged qualification remains
necessary at the next coherent boundary.

### Default-file publication and authored-content proof

Default creation now writes and syncs a temporary file, then atomically links it into the absent target.
The publish cannot replace a file created by another writer. Ordinary success/failure removes its
own temporary file; abrupt process loss can leave an unreferenced temporary file, but cannot expose
partial target contents. The parent directory is synced after publication and temporary unlink.

TypeScript checking and focused defaults/lifecycle tests pass. They cover authored profile retention,
new conflicting facts preservation, dangling-link refusal, repeat installation, complete default
contents, retained maintenance and no temporary residue after normal completion. Abrupt-process
residue cleanup remains a separate qualification item.

### Fresh hook-file onboarding

Init now includes the four native hook launchers in its discovered backup/default scope and creates
missing launchers with explicit executable permissions. Existing hook files remain authored input;
init does not overwrite them or change Git configuration. The explicit hooks command retains its
existing reconciliation and worktree/shared-configuration rules. Generated hook mode drift refuses
init replay rather than silently changing permissions.

The new absent hook paths exposed another immediate-parent assumption in forward-repair readback.
That readback now verifies the nearest existing canonical directory when an absent target's directory
is also missing. TypeScript checking and five focused default, discovery and lifecycle tests pass,
including executable hook contents and repair with absent hook directories. Hook activation remains
an explicit separate command; no claim of configured hooks follows from file creation alone.

### Compiled test-execution guidance owner

Inspection found that the compiled package still routed its test-execution skill to wheel-era
harness-agent commands, despite retiring that entrypoint. The compiled build now selects an authored
native test-execution skill for that catalog entry. It describes focused checks, detached check IDs,
authorized workflow submission, bounded same-run observation, cancellation and evidence assessment.
The operational wheel's skill remains intact while that installation is still supported.

The build passes. Loading the generated canonical skill catalog verifies the compiled entry contains
native workflow observation and no retired harness-agent invocation (2,878 bytes). Command flags were
checked against CLI parsing. This qualifies the generated test-execution entry, not every remaining
packaged skill/resource or a complete host-context workflow; the remaining catalog migration audit is
still required before cutover.

### Compiled provider skill routing

The compiled Claude, Codex and Gemini catalog leaves now derive from one thin authored template.
Each binds its provider and routes command details to the installed provider-help guide, preserving
explicit model/effort, authorization, native tool boundaries, same-job observation and cleanup rules.
The wheel's provider leaves remain unchanged for the currently operational installation.

Build and generated catalog loading pass for all three leaves. Verification confirms provider-help
routing, no unresolved template tokens, and no legacy operation-guide references in those leaves.
No provider invocation was necessary for this routing-only change. Other legacy resource entries and
installation guidance still require migration audit; this is not whole-catalog cutover acceptance.

### Compiled installation guidance

The compiled install skill now documents the actual explicit-request lifecycle, exact archive/lock
identity, project and external inventory, legacy exclusion, same-operation recovery, post-write repair,
readback and separate hook configuration. The generated file matches its authored source and no longer
routes users to Python bootstrap or unsupported wheel update flags. Build passes.

The audit also confirms automatic compatible startup update commands are absent from the compiled
CLI. The compiled install guidance states that limitation explicitly. This is a remaining capability
migration requirement, not an accepted deferral or proof of completed C12/C13 parity. The wheel's
operational installation guidance remains intact until cutover.

### Startup policy migration begins

A native startup policy reader now preserves the existing manual default, explicit compatible opt-in,
cache/discovery/install budget defaults and limits. Parent work assessment defers substantial-plan,
review and read-only work; minor/not-started work still requires repository opt-in. An eligible
assessment explicitly requires subsequent host, release and worktree validation and cannot activate
anything by itself. Invalid fields, null policy, nonfinite/boolean budgets and unknown work states
are refused.

TypeScript checking and two focused tests pass. This is the policy component only. Native startup
identity, bounded immutable-release discovery, reservations, Git isolation and startup lifecycle
integration remain unimplemented or unqualified; automatic startup adoption is not available yet.

### Compiled startup candidate compatibility

Native candidate validation now binds the literal candidate lock bytes to compatibility metadata,
checks stable numeric version ordering and explicit source range, requires the same major and GitHub
distribution, and preserves configuration/Node contracts. Compiled startup contract 2 cannot consume
wheel startup contract 1. The owning startup specification records this format distinction.

TypeScript checking and four startup tests pass, including exact-byte drift, excluded source bounds,
major/downgrade/preview refusal, changed distribution and unsupported declarations. This read-only
component neither fetches releases nor activates a runtime. Immutable publication validation, native
host identity, reservations and isolated activation remain required work.

### Complete bounded startup release inventory

The native release inventory consumes at most four pages of 100 records and requires a shorter final
page before returning any usable inventory. It ranks stable current-major candidates numerically,
limits candidate inspection to four, records a newer-major notice separately, and refuses malformed
pages or duplicate stable release identities. Draft/prerelease records are excluded. Transport errors
propagate without manufacturing a current-version result.

TypeScript checking and six startup tests pass, including shuffled ordering, pagination, incomplete
listing, malformed data and transport failure. This is transport-independent inventory logic;
response-byte/deadline enforcement, immutable asset validation and startup lifecycle integration
remain required before any automatic adoption.

### Bounded startup HTTP transport

A native transport now scopes initial requests to one GitHub repository, shares one monotonic deadline
across requests and redirects, limits redirect hops and streamed response bytes, and rejects unsafe
redirect hosts and unsuccessful responses. An explicitly supplied token is removed permanently after
an authority change. Responses are data; errors do not imply that the installed version is current.

TypeScript checking and nine startup tests pass, including credential stripping, request scope,
oversized bodies, unsupported redirects, HTTP errors and deadline abort. These tests use injected
transport responses; no release service was contacted. Native credential discovery, immutable asset
composition, host receipts and activation integration remain open.

### Startup immutable candidate assets

Candidate composition now requires an immutable non-draft stable release, unambiguous uploaded
metadata/lock/archive assets, repository-scoped asset API URLs and published SHA256 identities.
Downloaded metadata and lock bytes must match declared sizes and hashes before compatibility is
assessed. The release tag must match the compiled lock, and the archive download URL must match that
lock's exact artifact. The result explicitly leaves archive-byte verification pending installation.

TypeScript checking and ten startup tests pass. Fixtures cover immutable-release refusal, altered
bytes, duplicate assets, tag mismatch and archive substitution. No network release or installation
was exercised; discovery orchestration, actual archive verification, host identity and startup
activation remain open.

The first ten-test run exposed fractional timer truncation in the HTTP deadline test. The transport
now rounds its remaining timer delay upward, preventing a timeout scheduled before the shared
monotonic deadline. TypeScript checking and the complete ten-test startup group then passed.

### Composed startup discovery

Read-only discovery now connects repository policy, the bounded HTTP client, complete release inventory
and immutable candidate validation. Manual policy makes no network request. A complete empty candidate
set returns current or a distinct major-version approval result. A validated candidate returns its
bound metadata and pending archive-verification obligation; transport or candidate failure propagates
rather than claiming current. Discovery does not download the package archive or mutate installation.

TypeScript checking and twelve startup tests pass, including a composed candidate through injected
HTTP responses, manual/current/major outcomes and failure propagation. The current composition
inspects the newest stable candidate only; the existing bounded fallback across compatibility-refused
candidates still needs implementation. Host admission, credential lookup/cache, startup receipts and
activation remain open. No automatic update capability is exposed yet.

### Bounded compatible-candidate fallback

Discovery now distinguishes explicit approval-required compatibility refusals from transport and
integrity failures. It examines up to four stable current-major candidates in descending order,
retains refusal reasons, and may select a lower compatible candidate. If all examined candidates
require approval, discovery returns that outcome rather than current. Altered asset bytes and
transport errors still terminate discovery.

TypeScript checking and thirteen startup tests pass, including an immutable-policy refusal followed
by a valid lower candidate and a six-release list limited to four refusals without asset requests.
This completes the bounded selection composition, not native startup admission, caching or activation.

### Native startup event normalization

The native event component now permits discovery only for an unseen Codex SessionStart with source
startup. Other supported task events reserve or close the same workspace/session identity without
discovery. Resumes, forks, clear/compact sources, prompts, worker ancestry, child events, unsupported
hosts and malformed identities cannot open an update opportunity. Worker and invalid-event refusal
happens before filesystem access. This preserves the existing Codex-only adapter boundary; it does
not newly certify another host.

TypeScript checking and fifteen startup tests pass. The seen-task input must still be supplied by a
durable reservation owner; the normalizer is not exposed as independent activation authority. Task
store integration, native host conformance and safe activation remain open.

### Durable startup task receipts

A versioned SQLite receipt store now atomically records whether a workspace/session has consumed its
startup discovery opportunity. Reconnects, duplicate starts, close/reopen and even an end event seen
before startup cannot rediscover. Changed lock identity requests context refresh. Discovery results
attach only once to the same open pending task and lock; activation authority remains in the existing
generation registry. Environment ancestry remains checked by default.

TypeScript checking and sixteen startup tests pass, with focused follow-up verification for end-first
receipt handling. The receipt store is not yet a runtime-reader reservation: live parent ownership,
admission integration, abandoned-task recovery and automatic update execution remain required work.

### Startup observation under generation ownership

Startup observation now verifies the project lock against the active installed generation, holds a
runtime reader throughout receipt registration and discovery, and releases it after completion or
failure. The first eligible event records its discovery outcome durably; duplicate events return that
outcome. Failure receipts contain a bounded generic status rather than potentially sensitive transport
exception text. Worker/unsupported-event refusal occurs before opening runtime or receipt stores.

TypeScript checking and the lifecycle fixture pass for a real staged generation with manual-policy
observation/replay, incompatible local-release discovery failure/replay and zero remaining readers.
This reader protects the observation interval only. Parent-lifetime reservations, native hook wiring,
process-loss cleanup, caching and automatic activation remain open.

### Parent-lifetime generation reservations

Startup observation now reserves one durable generation reader per canonical task identity. Repeated
observations reuse that reader transactionally. The short observation reader still protects discovery,
while the task reader remains after success or failure until its close event. Closing releases only
that task's reader and remains permitted while maintenance blocks new admission, so owners can drain
without admitting new work. Ambiguous duplicate ownership requires reconciliation.

TypeScript checking and the lifecycle fixture pass for two parent tasks, duplicate observation,
maintenance admission refusal and separate closes reducing readers from two to one to zero. Native
host wiring, process-identity-backed abandoned-task recovery and startup's own update reservation
handoff remain open; these persistent readers must not be abandoned or stolen by a timeout.

### Startup host identity and absence primitive

Startup owner capture now walks at most eight native ancestors, records a Codex host only when its
process-start fingerprint remains stable across executable inspection, and retains the local host
identity. The absence guard requires that recorded identity and successful full process enumeration;
a present PID, including reuse, unknown owner, different host or inspection failure refuses recovery.
It never signals a process or uses elapsed time as evidence of termination.

TypeScript checking and seventeen startup tests pass. The new fixture verifies the absence guard,
not live native-host conformance. Persisting this owner with the task reservation and binding recovery
to that record remain necessary before exposing automatic reservation recovery.

### Persisted startup owner bindings

Startup receipt protocol 2 adds an owner binding without changing installation activation authority.
Observation records a positively captured native host together with the exact registry, reader token,
revision and directory. Identical binding replay succeeds; changed ownership refuses silent replacement.
Unidentified hosts retain their task reader without fabricated recovery evidence.

TypeScript checking and task/lifecycle tests pass, including binding persistence after reconnect and
changed-PID refusal. Recovery execution and explicit rebinding after a closed task reopens still need
implementation; current refusal preserves the prior binding rather than guessing which reader to
release. Protocol migration and live native-host conformance require further qualification.

### Closed-task owner retirement and rebind

Startup receipt protocol 3 retains prior owner bindings in history. Task close now verifies the
recorded registry/token/revision against the held reader, releases that reader, and retires the binding
while holding the receipt transaction. A failed release retains the binding. Reopened tasks can bind
a new reservation without deleting prior evidence or creating another discovery opportunity.

TypeScript checking and receipt/lifecycle tests pass for failed-retirement preservation, successful
retirement, new-reservation binding, and close during maintenance. These tests do not establish full
native event-order concurrency or abandoned-process recovery; those remain qualification work.

### Recorded lost-parent recovery

Recovery now requires the exact persisted owner-binding digest, verifies positive local host absence,
and releases only its recorded reader token/owner/revision. Under the receipt transaction it closes
the task and retains the binding plus absence evidence, authority and observation time in history.
Identical recovery replays the existing evidence; changed ownership refuses. Recovery reports unknown
task outcome and never replays work. Failed absence checks preserve the binding.

TypeScript checking, seventeen startup tests and the lifecycle fixture pass. The fixture uses an
actually exited subprocess with synthetic host metadata and full process enumeration, then verifies
reader release and evidence replay. This is recovery-mechanism proof, not live Codex host conformance.
Native hook/CLI integration, observation-process-loss recovery and automatic update execution remain
open.

### Startup observation CLI

The CLI now exposes startup observe with an explicit provider, bounded event file and receipt store.
Standalone invocation requires workspace/registry; runtime-run binds those to its launcher scope and
refuses conflicting overrides. Observation owns its reader lifecycle directly, including close during
maintenance, rather than acquiring an outer reader that would block closure. Maintenance probes cannot
invoke it. GitHub tokens may come from the existing environment without being written to receipts.

TypeScript checking and the lifecycle fixture pass with startup observation through the real runtime-run
CLI, followed by existing replay, failure and close checks. This command only observes; recovery CLI,
automatic apply, canonical native hook setup/receipt location and live host conformance remain open.

### Scoped startup recovery CLI

Startup recover now accepts an existing receipt store, task identity, exact binding digest and recovery
authority. Standalone and managed invocations bind recovery to workspace/registry; cross-workspace
requests refuse before releasing a reader, and replayed evidence must name the selected registry.
Delegated workers and maintenance probes cannot invoke recovery. Observation rejects recovery-only
arguments, and recovery rejects observation arguments.

TypeScript checking and the lifecycle fixture pass through the real runtime-run recovery CLI with an
exited subprocess fixture, plus wrong-workspace refusal preserving the reservation and identical
recovery readback. This exposes the tested recovery mechanism; native hook setup, startup apply and
live host conformance remain unfinished.

### Native startup event input

Startup observation now accepts either an explicit event file or native standard input. The latter
has a 64 KiB byte limit, strict UTF-8 decoding and a three-second input deadline. Interrupted,
oversized and malformed input refuses before observation; recovery cannot accept event input.
This lets native hooks invoke the compiled command without a temporary event file.

TypeScript checking and four focused tests pass, including the lifecycle fixture using standard
input through runtime-run and refusal of conflicting input sources. The first fixture assertion
expected an internal error string; it was corrected to the CLI's existing sanitized failure response.
Hook configuration, automatic application and live native-host qualification remain open.

### Native hook configuration proposal

The startup hooks command now reads existing Codex settings and returns a configuration proposal.
It preserves authored settings and handlers, repeats without duplication, and refuses malformed,
legacy or conflicting governance handlers. Generated commands invoke the installed launcher with
native standard input and an explicitly selected external receipt store. Configuration reads refuse
symbolic links. No hook settings, receipt database or update opt-in are written by this command.

TypeScript checking and four focused tests pass, including shell execution with quoted paths, stdin
preservation, repeat proposals, conflict refusal and read-only CLI behavior. Native hook trust,
deliberate installation/migration, automatic application and live host qualification remain open.

### Read-only startup recovery inspection

Startup status now exposes a known task's saved owner and exact binding digest through the installed
launcher. The command opens an existing canonical receipt database read-only, checks workspace and
recorded registry scope, and reads supported older protocols without migrating them. Missing stores
are not created. The returned owner is evidence for recovery input, never proof of process absence.

Focused proof covers CLI readback, exact binding digest, unchanged database bytes, absent-store
preservation, scope refusal and protocol-one inspection without migration. This closes the manual
SQLite-inspection gap in the existing recovery path; automatic application and native qualification
are still unfinished.

### Verified startup archive staging

The startup download path now revalidates immutable release metadata, bounds archive bytes, and
checks both the published SHA-256 and pinned SHA-512 before returning package data. Staging publishes
the complete archive under its digest using a synced temporary file and non-overwriting link.
Existing identical content can be reused; conflicting files remain untouched. Readback uses a bounded
ordinary-file descriptor without following final symlinks. This supplies installation input only;
it does not authorize or perform activation.

TypeScript checking and focused discovery/download/staging tests pass. Proof covers metadata-only
discovery, both archive hashes, size limits, identical staging replay and conflict preservation.
This uses injected release responses, not a live published release. The automatic application
coordinator, Git transaction, native qualification and final installed-package proof remain open.

### Startup Git admission

The compiled startup path now has a read-only Git admission check. It requires the canonical worktree
root and a committed branch; refuses conflicts, active operation markers, redirected Git environments
and runtime source checkouts; and compares tracked governance authority against both HEAD and index.
Unrelated staged, unstaged and untracked files remain allowed and unchanged. Literal filenames use
NUL-delimited Git output, and undecodable filenames fail rather than producing ambiguous identities.

TypeScript checking and a real temporary Git-repository test pass for unrelated-work preservation,
authority overlap, active index locks, redirected index and detached HEAD. This is preflight only:
the mutation snapshot, effective hook/configuration fingerprint, isolated commit and recovery still
need integration before startup application is complete.

### Startup preparation snapshot

The Git preparation snapshot now binds unrelated changed-file contents, modes, symlink destinations,
deleted paths and the complete staged index. File reads are bounded and reject changes observed
during collection. It also hashes effective Git configuration without retaining credential-bearing
values, resolves the effective hook directory through Git, and binds ordinary hook contents and modes.
Ambiguous hook directories or non-file entries require reconciliation. Revalidation refuses any
captured change and requires a fresh assessment.

TypeScript checking and the temporary-repository test pass for changed working bytes with unchanged
status names, changed staged bytes, changed Git configuration and added hooks. Restoring those exact
states restores snapshot validity. This is preparation evidence, not a transaction lock or proof of
isolated commit/rollback. Those remaining startup operations are still required.

### Parent-bound startup assessment

The startup assess command now requires the current native session to match the requested open task,
rejects delegated ancestry, and verifies that the saved lock identity is still current. Manual policy,
protected work states and missing native owner evidence defer preparation. An eligible parent assessment
captures the work state, explanation, profile/owner identities and Git snapshot. It grants no activation
authority and does not claim the recorded host is still live or that release discovery succeeded.

TypeScript checking and five focused assessment/policy/status tests pass, including wrong-session,
delegated-worker, protected-work, missing-owner, changed-lock and closed-task cases. A temporary Git
fixture reaches preparation-required with synthetic owner metadata. Live owner validation, reservation
handoff, candidate preparation, isolated commit and recovery still belong to the unfinished coordinator.

### Atomic startup reservation handoff

The generation registry can now exchange the sole exact parent reservation for maintenance in one
SQLite transaction. Its maintenance owner binds the full reader identity. Other readers, changed
generation or conflicting ownership refuse without releasing the parent. Exact acquisition replays
after reconnect, while new readers remain excluded. Pre-activation cancellation atomically restores
the original reservation and removes maintenance, provided no completion obligations remain. A repeated
cancellation refuses rather than claiming an unrecorded cancellation receipt.

TypeScript checking and focused ownership/protocol migration tests pass. Registry fixtures cover
competing readers, wrong tokens, reconnect/replay, admission exclusion, cancellation and pending
completion preservation. The coordinator must still journal the handoff, reconcile task receipts,
validate the live parent and connect this transition to candidate activation and Git recovery.

### Installed startup and lifecycle checkpoint

A fresh compiled preview archive passed 44 installed-JavaScript startup and lifecycle tests. The
external proof also initialized a clean project through that archive, repeated initialization,
invoked its actual installed launcher, and completed/replayed forward repair while preserving an
operational SQLite fixture. Temporary installations were removed. The combined source checkpoint
passed 34 startup, lifecycle and protocol-migration tests before packaging.

This verifies the packaged components and deliberate lifecycle at this checkpoint. It is a dirty
preview with local artifact identity, not immutable release provenance. No adopter was activated,
no live release was downloaded, and no native host/device qualification or automatic update transaction
was established by these checks. Coordinator, parity, benefit and final review obligations remain open.

### Git lock-transition verification

Snapshot verification now has an explicit transition path for exact original and candidate lock
contents. It normalizes only that lock change while preserving unrelated index entries, working
files, configuration, hook content and lock modes. Before commit, HEAD must remain unchanged. After
commit, readback requires one exact parent, only the lock path in the commit, candidate bytes in HEAD
and index, and preservation of every unrelated snapshot field.

TypeScript checking and Git/assessment tests pass. The temporary Git fixture performs a real isolated
lock commit and verifies that unrelated staged and unstaged work remains. Wrong candidate bytes,
parallel edits and an unexpected HEAD change refuse. No source or adopter commit was made. This
supplies transaction verification; production commit execution and recovery coordination remain open.

### Durable isolated lock commit

The startup commit operation now uses the existing durable command owner for a path-specific Git
commit, retaining normal hooks and configured signing. A successful process result still requires
confirmed cleanup and exact Git transition readback. Failed or unresolved execution returns
recovery-required with the original command identity. Reconnect reuses that command rather than
launching another commit. The caller still must hold maintenance and journal the operation before
invocation; no public automatic-update command invokes this helper yet.

TypeScript checking and the real Git fixture pass for isolated commit, identical reconnect without
a second commit, unrelated-index preservation and a rejecting native pre-commit hook. The rejecting
hook runs once, leaves HEAD unchanged and is not repeated on reconnect. Signing configuration is
preserved by implementation; live signing and maintenance-aware governance hooks remain unqualified.
Only temporary test repositories were committed. Coordinator and recovery integration remain open.

### Maintenance-scoped commit hook admission

Managed invocation now recognizes an explicit startup maintenance identity for only native pre-commit
and commit-msg hooks. The token, owner, workspace and registry must match current maintenance; partial,
stale, cross-scope and version-probe identities refuse. Other commands retain ordinary admission.
The durable commit helper can carry this identity into Git's hook environment. Hook execution retains
the existing possible-write marker, so its evidence cannot justify a pre-write rollback.

TypeScript checking, focused admission tests and the durable Git fixture pass. This establishes scoped
admission logic, not installed governance-hook conformance under a full update. The coordinator still
must supply verified maintenance ownership and account for hook writes during completion or recovery.

### Startup completion preserves hook evidence

The registry now has a separate startup completion transition that keeps the possible-write marker,
records the exact completion identity and atomically restores the parent's reader on the successor
generation. It requires owned drained maintenance and no unresolved completion obligations. Exact
completion replay does not add another reader; conflicting receipts refuse. Existing pre-write
finalization and rollback rules remain unchanged.

The startup finalization coordinator now checks the isolated Git commit, installed generation,
launcher, lock and native version readback before invoking that transition. TypeScript checking and
four focused registry/protocol tests pass, including hook-write preservation, active-reader refusal,
single-reader replay and rollback refusal. These registry fixtures use synthetic installation state;
the new finalization coordinator still requires an integrated installed-runtime/Git proof before
automatic startup application is qualified.

### Installed startup finalization proof

An external integration fixture now exercises the source finalizer against a real installed preview
launcher and temporary Git repository. It refuses completion before the lock commit, retains
maintenance on that refusal, then verifies committed installation readback, preservation of the
hook-write marker, one restored parent reservation across replay, and unrelated staged/unstaged
content. The fixture and installed generations were removed after the successful run.

The fixture deliberately uses two synthetic source identities for the same local preview archive
and a synthetic hook-write marker. It proves finalization composition, not published-release
compatibility, actual governance-hook execution, native host conformance or adopter activation.
Task-receipt rebinding, full coordinator journaling and recovery remain required.

### Startup task completion across both ledgers

Task completion now holds its receipt transaction while the replayable generation finalizer restores
the successor reservation. It preserves prior owner/discovery evidence, records the new lock and owner,
and prevents task closure from retiring the old binding midway. Identical receipt completion replays
without finalizing again; different identities refuse. The composition checks the matching generation
finalization receipt before returning completion.

TypeScript checking and four focused receipt/registry tests pass. The external installed-launcher/Git
fixture also passes with generation completion deliberately performed before task completion, proving
that this interrupted ordering can resume. A subsequent native close releases the successor reader.
The fixture retains its synthetic source-identity/hook-marker limits. Full preparation journaling,
live host conformance and update failure recovery remain open.

### Live parent identity at assessment

Eligible startup assessment now checks the recorded native host against actual process ancestry
before and after collecting the Git snapshot. Provider, host, PID and process-start fingerprint must
all match. A copied session environment, reused PID, missing ancestor or changed process identity
cannot establish preparation eligibility. Protected work still defers before this preparation work.

TypeScript checking and three focused assessment/host tests pass. A read-only probe in the current
native host captured and revalidated an ancestor successfully. This is one live ancestry observation,
not complete native lifecycle conformance; parent/child, reconnect, close and updater recovery still
need the declared integration proof.

### Exact startup operation journal

Preparation can now reserve a canonical external journal binding task, owner, original/candidate
lock bytes, installed candidate, Git snapshot and one durable maintenance token. Exact requests reuse
the token. Changed requests, redirected destinations, an incomplete prior reservation and oversized
readback payloads refuse without replacing the original operation. The journal is preparation data;
it does not independently authorize discovery, installation or activation.

TypeScript checking and the external installed-completion fixture pass with journal replay, changed
binding refusal and incomplete-directory preservation. The fixture uses the reserved token through
generation handoff and task completion. Published compatibility, full preparation orchestration and
failure recovery are still pending; the synthetic fixture limits remain unchanged.

### Evidence-based startup recovery inspection

Recovery inspection now binds the journal to its commit command, requires confirmed process cleanup,
and checks actual Git and generation state before identifying a recovery action. Unconfirmed cleanup
retains maintenance. A matching committed lock selects completion only for the expected successor
and ownership; an uncommitted successor with writes selects forward repair. The original generation
and an untouched successor are distinguished for cancellation and pre-write restoration. Inspection
does not execute those actions or repeat the commit.

TypeScript checking and an external integration fixture pass with a real durable Git command rejected
by a pre-commit hook. The fixture selects forward repair while HEAD is unchanged and the synthetic
write marker is present, then selects completion after an exact fixture lock commit. Existing installed
launcher/task-completion checks also pass. Recovery execution and full startup orchestration remain open.

### Committed startup recovery command

Startup recover-update now binds the requested workspace, registry and receipt store to the saved
journal and original commit command. Confirmed committed transitions complete through the installed
readback/task-rebinding path and persist a recovery receipt. Exact replay preserves that receipt;
conflicting evidence refuses. Uncommitted or unconfirmed outcomes remain recovery-required without
releasing maintenance. Delegated workers cannot invoke this recovery entrypoint.

TypeScript checking, six focused CLI-adjacent tests and the external committed-recovery fixture pass.
The fixture exercises the actual runtime-run CLI, wrong-scope refusal, repeated completion without
another commit/reader, and native task closure. It retains the documented synthetic release and
write-marker limits. Pre-commit restoration, forward-repair execution and full update orchestration
remain unfinished.

### Composed inactive startup preparation

Startup prepare now joins native parent assessment, saved completed discovery, retained runtime
identity, release revalidation, verified download, inactive installation and the exact operation
journal. It rechecks assessment and reservation after installation. A completed identical preparation
reuses its local archive and installed candidate without network calls; changed requests refuse.
No generation is activated and no parent reader is released by this operation.

TypeScript checking and focused preparation/status/assessment tests pass. The preparation fixture
uses injected immutable-release responses, synthetic native ownership and real installations of
minimal fixture archives; it proves four initial download requests, zero replay requests, unchanged
active generation and retained parent ownership. This is not live publication or native CLI conformance.
End-to-end installation-budget enforcement, handoff/backup/activation orchestration and the remaining
recovery branches are still required.

### Prepared startup application composition

The internal application coordinator now revalidates preparation, exchanges the parent reservation
for maintenance, backs up the lock and launcher, activates the prepared generation, commits only the
verified lock and completes both ledgers. Lock replacement can preserve exact verified text while
checking its semantic installed identity. Failures retain their journal and maintenance; successful
application replay follows committed recovery rather than dispatching another update.

TypeScript checking, the lifecycle fixture and the composed preparation/application fixture pass.
The latter performs real inactive archive installs and a temporary Git commit, verifies exact lock
bytes, successor task ownership and identical application replay with one reader and no additional
network work. Its release responses, host identity and version-only archive payload are fixtures.
The application helper is not yet exposed as a public automatic-update command. Real governance-hook
execution, complete failure recovery and overall deadline enforcement remain qualification work.

### Startup commit hooks with the full compiled runtime

The full-runtime fixture exposed a missing maintenance handoff: the native commit hook could enter
its generation, but its detached validation worker could not retain a reader. Check dispatch now
revalidates the scoped startup hook identity and passes its maintenance token into retention. Normal
worker retention remains excluded during maintenance; incorrect tokens and parent identities refuse.
The retained validation reader still blocks activation and finalization until cleanup releases it.

The installed preview fixture now passes preparation, application, real pre-commit and commit-message
hooks, lock-only commit, task rebinding and identical replay. Its release versions, HTTP metadata and
native host identity remain synthetic; this is not release publication or native lifecycle qualification.
The fixture archive excludes macOS metadata sidecars, which had caused an earlier pack-loading failure.
Focused runtime retention and hook-admission tests pass. Recovery-required results now return exit 2
through both direct and managed startup CLI routes; the committed recovery integration proof verifies
both unresolved routes and successful completion/replay. Overall startup deadline enforcement,
uncommitted recovery execution and automatic application exposure remain unfinished.

### Task closure during startup maintenance

Native close now preserves the task binding when its reservation has transferred into startup
maintenance. The registry checks that handoff atomically with retirement, so a missing reader is
not mistaken for a completed ownership obligation. Absent-host owner recovery follows the same
boundary and cannot erase an in-progress update binding. Committed completion accepts a closed task
only with successor retirement; task state remains closed, and replay does not recreate a reader.

TypeScript checking and five focused task/maintenance tests pass. An external integration proof
closes the task after a failed real Git command under maintenance, verifies the retained binding,
then completes an exact fixture commit through the recovery CLI. It confirms no remaining parent
reader, replay, and preservation of unrelated staged/unstaged work. The fixture uses real installed
version readback with synthetic source identities and hook-write marking; it does not qualify
publication or the full native host lifecycle. Pre-commit recovery still requires updater-liveness
proof before taking over mutations; the current inspection does not establish that proof.

### Startup updater process ownership

Prepared application now claims a single durable coordinator identity before the maintenance handoff.
A duplicate or incomplete claim cannot dispatch another mutation attempt. Normal return and exceptions
record coordinator quiescence; abrupt process loss requires complete local ownership and successful
process enumeration proving the recorded PID absent. A live or reused PID, foreign host, mismatched
request or malformed stop receipt refuses. This evidence does not substitute for child-process cleanup.
Committed recovery checks the coordinator when that claim exists; older committed journals retain the
existing exact-commit recovery path. Future uncommitted takeover must require the ownership proof.

TypeScript checking and four focused ownership/application tests pass, including a real child that
exits before recording quiescence, duplicate admission, live-owner refusal and unchanged successful
application/replay. The helper establishes the prerequisite, not an implemented uncommitted takeover
or restoration path. Deadline enforcement and remaining recovery execution remain open.

### Stopped pre-activation cancellation

Startup cancel-update now restores the parent reservation only for a stopped coordinator with no
commit dispatch, the original generation, unchanged Git snapshot and exact original launcher. It
rewrites no project files. Registry cancellation has a durable finalization identity; the task ledger
preserves prior discovery evidence and records a deferred outcome without starting another attempt.
Closed tasks retire the restored reservation, while open tasks retain it. Replay does not create a
second reader and a cancelled operation token cannot reacquire startup maintenance.

TypeScript checking and focused task/maintenance tests pass. External installed-preview fixtures pass
for open and closed tasks through the managed source CLI, including live-coordinator refusal, changed
Git-state refusal, original-lock preservation and replay. Release metadata and host identity remain
synthetic; this is not native startup qualification. Activated/pre-write restoration, post-write
forward recovery, overall deadlines and final installed-package consolidation remain open.

### Startup restoration before runtime writes

Startup restore-update now reuses the shared pre-write restoration owner after proving coordinator
quiescence and checking command cleanup when a commit was dispatched. It accepts only the journaled
lock transition and refuses independently edited installation files. The shared helper supports the
candidate lock's exact verified text rather than requiring canonical whitespace. Restoration verifies
the original Git snapshot and installed launcher/version before releasing maintenance. The task ledger
rebinds an open task to the restored generation revision, or retires that reservation for a closed task;
the registry restoration identity and task outcome make replay non-dispatching.

TypeScript checking, the existing runtime-staging fixture and focused task/maintenance checks pass.
External real-installation fixtures pass open/closed restoration, changed Git and launcher refusal,
managed source-CLI replay and original lock preservation. A focused task test verifies restored-reader
readback and later close. Those fixtures retain synthetic release metadata/native owner identities and
use installed preview readback, not final-product qualification. Post-write forward recovery, overall
startup deadline enforcement and final installed-package/lifecycle consolidation remain unfinished.

### Explicit post-write commit retry

Startup retry-update now provides a bounded forward commit path for an unchanged valid candidate.
It requires the original updater stopped, the original command cleanup confirmed, exact drained
startup maintenance and preserved source/installation identity. One separately owned retry keeps
normal hooks and signing enabled. Replay inspects the same retry; a repeated failure retains
maintenance. Committed recovery checks cleanup of both original and retry commands, including when
called with the original command identity, so old evidence cannot bypass a live retry.

TypeScript checking and four preparation/owner tests pass. Two external fixtures use actual packaged
governance hooks: a transient rejection followed by success, and a persistent rejection. They verify
post-write marking, preserved original command receipt, isolated lock commit, successful completion,
retained maintenance on failure and no new command on replay. The fixtures invoke the source retry
coordinator with synthetic release/host identities; final installed-CLI consolidation is still pending.
This does not repair a broken candidate or override a failed check. General deliberate runtime repair,
startup deadline enforcement and final product qualification remain separate work.

### Shared startup installation deadline

Preparation now persists one request-bound installation deadline. Download, inactive staging,
application admission, isolated commit and final readback consume its remaining allowance rather
than restarting the profile budget at each phase. Staging and readback subprocess timeouts are capped
by the remaining time; phase-boundary checks prevent later activation after expiry. Reopened
preparation preserves its original expiry. In-process monotonic timing prevents clock rollback from
renewing an active allowance. Cleanup observation retains its separate responsibility and cannot be
inferred from an elapsed deadline.

TypeScript checking and four focused staging/preparation/deadline tests pass. An external slow-
transport fixture verifies that expiry leaves the original generation and reservation intact, creates
no staging operation, and makes no additional network request on replay. Normal application and
replay continue to pass. Hard interruption of arbitrary synchronous filesystem work is not claimed;
checks occur at boundaries and native subprocess deadlines. Consolidated current-package qualification,
automatic startup application wiring and the remaining migration/acceptance gates remain open.

### Consolidated startup recovery package qualification

The complete current engine source suite passes 349 tests. A fresh offline-bundled preview archive
passes 57 tests imported from its installed JavaScript modules, real installed-launcher init and
post-write repair with replay, and eight additional installed integration scenarios. Those scenarios
cover real governance commit hooks, application/replay, open/closed pre-activation cancellation,
open/closed pre-write restoration, successful/persistently failed forward commit retry, and deadline
expiry without renewed network work. The archive hash was read back against the proof record and
all temporary fixture installations were removed after completion.

This is one current package checkpoint, not publication or adopter cutover. Synthetic stable release
metadata and native-parent identities remain fixture inputs, and earlier device claims still need
final-artifact applicability/requalification. Native startup integration, remaining category/adopter
parity and retirement, declared CI scope, measured benefit and final review remain acceptance work.
The active status summary above replaces the stale initial E1a paragraph; historical checkpoints keep
their original evidence limits.

### Public parent-assessed startup application

The startup CLI now exposes apply through the same parent assessment and preparation arguments.
It invokes the qualified coordinator; observation hooks remain passive. Tracked compatible-update
policy, recorded task identity, live native ancestry, safe Git state and retained runtime ownership
remain required. Delegated invocation cannot use the parent's application path.

TypeScript checking and package build pass. A fresh installed-preview fixture invokes startup apply
through the actual managed launcher, first refusing delegated invocation without changing the runtime,
then completing real commit hooks, activation, task rebinding and identical application replay.
Native ancestor inspection uses the live Codex process; release metadata and the session identifier
are fixture inputs, so this does not certify actual native hook delivery or host opt-in installation.
Hook installation/guidance and the remaining migration, device, CI and benefit acceptance work remain
open. No adopter runtime or native host configuration was changed by this proof.

### Explicit project startup observer installation

`startup install-hooks` now installs the existing observer proposal into project-local Codex hooks.
It preserves authored handlers and ordinary file permissions, leaves semantically current files
byte-identical, refuses legacy/conflicting handlers and redirected/shared files, and verifies the
configuration again before atomic replacement and after installation. It does not change update
policy, global Codex settings or project trust. The result explicitly reports outstanding host
requirements instead of claiming that installed files establish native hook delivery.

TypeScript checking and six focused installation/proposal tests pass. Tests cover authored handler
preservation, repeat installation, permissions, legacy/symlink refusal, quoted command paths and
proposal-only behavior. Actual native lifecycle delivery and installed-package integration remain
qualification work; no adopter or live host configuration was changed by these tests.

### Native hook protocol correction

Checking the current Codex Hooks reference exposed a generated SessionEnd timeout above the host's
three-second maximum and incomplete trust guidance. The proposal now uses three seconds. Installation
reports exact-definition review through `/hooks` in addition to project trust, managed restrictions
and enabled hooks. It does not bypass that review or change user configuration.

Native stdin observation now returns the documented hook-specific output shape rather than internal
receipt JSON. Only completed initial discovery with an available or approval-required candidate adds
brief parent context. Continuation, child and close events emit an empty object; candidate metadata
is never copied into instructions. File-driven observation retains its structured diagnostic result.
Eight focused tests, TypeScript checking and build pass. A compiled CLI stdin child-event probe returns
an empty object without accessing nonexistent registry/receipt paths. These are protocol and executable
boundary checks, not proof of actual host event delivery or trusted-host installation.

### Bounded startup metadata reuse

The configured cache duration now controls actual metadata reuse across top-level tasks. Observation
keeps a disposable cache alongside its external receipt store. Cache scope binds the current lock,
startup settings and authenticated/anonymous mode. A successful bounded discovery publishes the
whole response snapshot atomically; failed discovery does not publish partial pages. Responses are
replayed through release inventory, compatibility and asset identity validation, rather than treating
a cached selection as activation authority. Cache files above the bounded storage allowance are not
written. Credentials, worktree judgments and archive bytes are not cached.

Eight focused discovery/cache/HTTP tests and TypeScript checking pass. They verify zero repeated
network calls while fresh, expiry, changed-lock and changed-policy invalidation, manual-policy silence,
failed-discovery nonpublication, corrupted-response validation and redirected-file refusal. Native
host delivery, final installed proof and the remaining acceptance work remain open.

### Installed hook/cache integration checkpoint

The complete engine suite passes 355 tests after correcting an obsolete native-stdout assertion.
That case now verifies the host receives an empty object for manual policy while the durable task
receipt still records the manual result. A fresh compiled archive passes 63 installed-module tests,
real-launcher init/repair replay, explicit hook installation/replay and generated child-observer
invocation. Eight installed lifecycle scenarios also pass, including open/closed cancellation and
restoration, successful/failed commit retry, deadline expiry and real commit hooks. Archive digest
readback matches the external proof record and temporary installations were removed.

These checks establish the packaged executable boundary and lifecycle behavior under controlled
inputs. They do not establish actual Codex hook delivery/trust, adopter activation, final device
qualification, CI acceptance, measured benefit or completion of E5.

### Existing startup instruction route migration

The adopter audit identified a remaining cutover dependency: the wheel's managed startup block points
to its installed skill directory. Compiled host planning and backup readback now migrate the exact
shipped block to `startup-help`, preserving authored surroundings. Customized, duplicate or malformed
startup blocks require deliberate reconciliation before any host entry is written. The recognized
legacy text is compared directly with the Python owner's template in the focused test; it is not a
second operational implementation. The compiled guide covers truthful work assessment, retained
receipts, application, recovery, observer installation and native trust without changing update policy.

Eight host merge/installation/backup tests, TypeScript checking and build pass. The backup cases also
cover the actual legacy startup block through partial application and readback. Compiled CLI guide
readback passes. This fixes instruction routing, not legacy hook migration or real adopter cutover.
The read-only adopter audit also found wheel-interpreter dependencies in CI and adapter contract tests;
those need owner-specific treatment before activation. Existing adopter changes remain untouched.

### Adopter CI and contract interpreter separation

The authorized adopter's local CI deadline wrapper, adapter coverage entrypoint and four Python-backed
contract suites now use its existing project-owned validation interpreter and pinned requirements.
The remote contract setup already prepares that environment. The local CI guide now states the
one-time setup requirement explicitly. The coverage entrypoint retains its re-execution behavior and
fails with project setup guidance when the interpreter is absent; there is no wheel/ambient fallback.
The legacy startup launcher remains unchanged until its qualified cutover.

Focused contract execution produced 134 passes and one failure in an existing historical coverage
inventory expectation. The same single case fails with the original HEAD test and wheel interpreter
against current adopter inputs; this is retained as an open qualification issue, not counted as a
passing suite. Three real-process deadline tests, shell syntax and whitespace checks pass. Edited and
exercised input hashes were read back unchanged. No full CI, runtime activation or device build was
performed, and parallel adopter changes were preserved. External evidence records exact paths and logs.

### Historical adopter coverage fixture reconciled

The remaining interpreter-migration contract failure came from a retired-waiver fixture that removed
some newer proof registrations but left later Fabric, GetInView and native-iOS registrations active.
The fixture now removes those explicit replacements while retaining the immutable historical inventory
and all 119 finding expectations. The existing positive replacement checks and negative removal checks
remain. Production coverage policy and enforcement were not changed.

The formerly failing case passes in a focused rerun, with clean whitespace validation. The prior 134
passing cases were unchanged and not repeated. This resolves that known contract failure; it does not
establish a new full-CI run, source-wide qualification or physical-device proof. External evidence binds
the corrected test and current policy identities to the targeted result.

### Native integration added to migration backup scope

Project migration discovery now binds the selected Codex, Claude and Gemini hook/settings files and
the legacy startup launcher, including explicit absence records. It does not recursively sweep provider
directories or include unrelated authentication/session files. Redirected integration paths are refused,
and adding a hook after planning invalidates the saved plan before backup. Effective hook trust and
legacy-handler transition remain explicitly unresolved rather than inferred from file capture.

Four focused migration-plan tests, TypeScript checking and the actual source-CLI installation/repair
integration case pass. The backup/replay tests cover missing native configuration directories. This
closes a backup-scope omission; it does not itself migrate executable startup handlers or qualify their
native delivery. No adopter native settings were changed.

### Backed executable startup hook transition

The coordinated host transition now accepts an explicit startup receipt store and replaces the exact
shipped four-event Codex legacy hook set from its verified backup. It preserves authored handlers and
mode, binds original/intended bytes and receipt scope into host completion identity, and requires drained
pre-write activation. Partial/customized legacy sets, changed files and changed receipt identities are
refused. Replay accepts only original or intended configuration. Lock-only finalization refuses an active
legacy Codex observer instead of silently completing a broken cutover. Native trust remains external.

The actual source-CLI legacy update/replay case now includes native hooks and passes. Four focused
migration/backup/admission tests pass, including comparison with the Python owner's emitted handler set,
changed-input refusal and maintenance retention. TypeScript/build verification is consolidated with this
boundary. Installed-package and actual native delivery qualification still follow; no adopter native hook
was changed. The old unreferenced script is not retired merely because configuration has transitioned.

The consolidated engine suite after the backed native hook transition passes all 360 tests.
TypeScript checking and package build also pass. The earlier installed-package checkpoint predates
this transition and is not treated as final-package qualification.

### Installed backed-hook transition qualification

A fresh package check caught that `startup-help` was available directly but omitted from managed
invocation admission. It is now a read-only managed command, with a regression assertion that it does
not mark the generation written. The rebuilt archive passes 76 installed-module tests, real-launcher
startup-guide readback, hook installation/replay, init/repair replay and eight installed lifecycle
integration scenarios. Those module tests include backed legacy hook transition, host instruction
migration and the CLI legacy update/replay path. Archive SHA256 readback matches the external proof
record; temporary installations were removed after the completed run.

The earlier failing package is retained as diagnostic evidence. The passing package uses controlled
release metadata and host identities; native event delivery/trust, orphan observation ownership,
remaining adopter/category retirement, final device/CI qualification, measured benefit and final Opus
review remain open. No live adopter cutover or public release was performed.

### Short startup observer crash recovery

Short reader acquisition now records the local hook process identity directly in its registry owner.
Explicit `startup recover-observation` checks workspace/host identity and requires the recorded PID
to be absent before releasing the exact token. Live or reused PIDs refuse recovery, and parent task
reservations are never released through this route. Repeated recovery reports not-held without
inferring discovery success. The packaged command guide and startup contract describe this boundary.

A real child-process abrupt-exit test passes against source and compiled JavaScript, preserving both
the live observer and parent reader. TypeScript checking and build pass; temporary test directories
were removed. This is compiled-module proof, not a new installed archive qualification. The separate
parent-reservation-before-native-binding interruption window and actual native event delivery remain
open. The preceding 360-test and installed-package checkpoints predate this change.

### Parent startup reservation write ordering

The observer now persists native owner identity and an exact planned reader token before acquiring
the persistent parent reservation. Registry acquisition verifies generation, directory and token;
retry cannot silently substitute a different reader. Missing native owner identity defers observation
without persistent reservation. Existing unbound legacy reservations still require investigation.

Three focused tests pass, including real process exit after durable binding but before acquisition,
replayed recovery, changed-token/generation refusal and short-reader crash recovery. The existing
source lifecycle integration also passes. These checks close the new-reservation write-order gap;
they do not establish actual native hook delivery or final installed-package qualification.

The follow-up closure check found an interleaving between durable intent and registry acquisition.
Acquisition now holds the task-store transaction and checks the exact open binding, using the same
task-store-to-registry lock order as owner retirement. SessionEnd cannot retire intent and then leave
a newly acquired reader behind. Four focused recovery/ordering tests and the source lifecycle test
pass; TypeScript checking and build pass. The package proof started before this follow-up and does
not qualify this additional change.

The preceding installed artifact completed successfully: 79 installed-module tests and eight
lifecycle scenarios, plus real-launcher init/repair replay. Archive digest readback matched the
external receipt and temporary installations were cleaned up. This proves the short-observer and
durable-parent-intent changes before the closure serialization follow-up, not the latest source.

### Native project trust boundary observed

A real Codex app-server inspection of the temporary installed project returned no hooks. Effective
configuration explained why: its project layer was disabled until persisted project trust exists.
A command-line project trust override did not enable that layer. The retained qualification now
asserts this exclusion rather than treating an empty list as successful hook discovery. No global
trust setting was changed, no trust bypass was used, and the owned app-server process exited.
Positive native delivery and exact hook-definition trust remain unqualified.

The rebuilt archive containing closure serialization passes 80 installed-module tests and real
launcher init/repair replay, startup-guide readback, hook installation/replay and generated child
observer invocation. Temporary installations were removed and the native inspection process exited
with code zero. The earlier eight installed lifecycle scenarios were not repeated because this
follow-up changes reservation ordering; its directly affected installed lifecycle test is included.

### Adopter bootstrap dependency audit

A read-only tracked-file audit identified remaining wheel bootstrap invocations in CI and their
contract tests, the legacy startup interpreter, and host skill routes. The old bootstrap requires
wheel-specific lock fields, so leaving those CI invocations after a schema-2 cutover would fail.
External evidence binds the 12 matched files and 20 references to current bytes. This is a targeted
dependency audit, not completed category parity. Adopter files were not changed in this checkpoint.

Migration backup discovery now includes the legacy bootstrap script, recording both presence and
absence and rejecting saved plans after its bytes change. Four migration tests and the source
lifecycle integration pass; TypeScript checking/build and packaged install-guide byte comparison
pass. The install guide now routes startup operations to the current packaged startup-help contract
instead of repeating obsolete claims that implemented recovery/application paths are missing.
CI bootstrap replacement and authored skill-route migration remain required before adopter cutover.

### Fresh locked-checkout installation

The compiled init command now accepts explicit `lockedCheckout: true` for a fresh checkout whose
tracked compiled lock already exists. The candidate must match that lock exactly, the backup must
include its bytes, and launcher/registry absence is still required. Existing installation replacement
remains update/repair territory. The ordinary backed installer performs activation and replay.

Source CLI integration passes for locked checkout initialization, unchanged lock bytes, identical
operation replay and mismatched-candidate refusal before registry creation. TypeScript checking passes.
This establishes the installer path needed by CI; acquisition of the trusted bootstrap package and
replacement of adopter CI invocations remain unfinished. No adopter runtime was activated.

### Pinned local bootstrap entrypoint

A standalone Node entrypoint now seeds the exact local archive selected by the compiled JSON lock.
It verifies SHA-512 before loading package code, installs with scripts disabled and offline dependency
resolution, and calls the packaged migration planner and init command. It retains a write-once
bootstrap identity and archive in external state and reuses the recorded operation. There is no
second activation implementation. The entrypoint ships as a generated package asset from one source.

A fresh full archive passes actual bootstrap installation, identical replay, exact lock-byte
preservation and managed-launcher version readback. A wrong archive is refused before state creation.
Temporary seed installations and the qualification checkout were removed. Build and whitespace checks
pass. This proof uses a supplied local archive and dirty preview identity; remote/private artifact
acquisition, adopter CI replacement and immutable release qualification remain open.

### Bootstrap artifact acquisition

The bootstrap entrypoint now accepts the lock's HTTPS or local-file URL when no archive argument is
supplied. Private GitHub release lookup selects the exact named asset from the exact locked tag and
checks its browser URL. Credentials are restricted to initial GitHub API calls and dropped on every
redirect. Downloads share a 60-second deadline, bounded redirects and a 32 MiB archive limit; the
locked SHA-512 still gates all package execution. A retained archive takes precedence on replay.

Three controlled-transport tests pass for credential confinement, signed redirects, identity refusal,
unsafe URLs and response bounds. TypeScript/build checks pass. A fresh archive also passes real local
bootstrap installation and replay without an archive argument, preserving lock bytes and returning
the managed launcher version. These are source transport and packaged local-installation proofs;
live private-release download and adopter CI invocation replacement remain unqualified.

### CI cutover refusal and backup scope

The adopter already pins the compiled engine's Node version. Its two direct wheel-bootstrap CI
callers remain incompatible with the compiled lock. The existing activation dependency check now
rejects those executable GitHub workflow steps, including projects without custom validation packs.
Labels and reusable-workflow references are not mistaken for executable calls. This is direct-step
inspection; indirect scripts and composite/reusable workflow graphs still require the declared audit.

GitHub workflow files now join the migration backup scope. Live read-only preflight finds the two
expected incompatible jobs and captures 15 workflow files within a 100-input migration plan.
Focused dependency/migration tests and TypeScript checks pass. No adopter workflow was changed;
bootstrap and related lane-contract replacement must accompany the actual coordinated cutover.

### Coordinated adopter CI migration candidate

An external, unapplied patch now changes both CI callers, build-platform command/entrypoint ownership
and relevant contract expectations together. Its project-owned shell wrapper only acquires the exact
GitHub release asset, verifies locked SHA-512, seeds the package with scripts disabled/offline
dependencies and invokes the packaged bootstrap. It contains no copied installer or activation code.
Current adopter bytes are hashed, and patch applicability and compiled CI dependency preflight pass.

The first wrapper execution exposed a bootstrap entrypoint bug: a temporary-directory path alias
caused a zero exit without executing installation. Canonical entrypoint comparison fixes this silent
no-op. The failed fixture was cleaned up; rebuilt-package wrapper qualification follows.
This does not qualify live GitHub download or full adopter contract suites. The patch remains
unapplied because the adopter still uses the wheel lock; applying only its new CI commands would
break that existing installation. Bootstrap retirement, compiled lock activation and this candidate
belong to the same cutover batch.

After rebuilding, the wrapper passes with controlled GitHub transport and actual npm/package
installation, preserving lock bytes and reading the managed launcher version. Four bootstrap tests
pass, including the explicit alias regression; build and TypeScript checking pass. Temporary
qualification state was removed. The external patch is concrete and applicable but still unapplied;
its full adopter contract checks and live remote transport are not claimed by this fixture.

### Candidate CI contracts and consolidated source checkpoint

Four focused adopter CI contracts pass in an isolated copy with the candidate applied: command/lane
mapping, static PR job ownership, local/CI route ownership and project-owned Python. Candidate input
hashes were checked before execution and the temporary tree was removed. This is not the full SDK
contract suite or live adopter activation.

Consolidated source execution initially produced 368 passes and one test cleanup failure. The native
checker test removed evidence after receiving its terminal result, racing the supervisor's final
writes. It now waits for the recorded worker and guardian fingerprints to disappear before deleting
the fixture; an unresolved writer preserves evidence. The focused regression passes. Failed-run
residue was retained externally and the stopped fixture was removed after process inspection.

The consolidated rerun passes all 369 engine tests. TypeScript and whitespace checks pass.
This is current-source proof; final installed/device/CI qualification and native hook delivery remain
separate obligations. The candidate CI patch remains unapplied until coordinated adopter cutover.

### Packaged skill discovery without a copied corpus

`skill-read --path` now reads one Markdown/YAML/JSON guidance file from the selected package, returning
its content, SHA-256 and byte count. It refuses traversal, aliases and files above 64 KiB. Managed
invocation classifies it as read-only. The provider guide and thin managed instructions route catalog
discovery and referenced-resource reads through this command; required task context still uses
context-route. The external adopter migration candidate also replaces its authored wheel catalog
reference, preserving other instructions.

Focused path/content tests, host instruction tests, TypeScript/build and patch applicability pass.
A fresh package passes bootstrap/replay and actual managed-launcher skill readback with matching
content digest and unchanged generation-written state. Temporary qualification state was removed.
This closes the physical catalog-read route; complete corpus/command migration and adopter cutover
remain distinct qualification obligations.

### Compiled resource command authority

Four wheel-specific resource bodies now have compiled package owners: delegation, provider job
operation, deterministic batches and startup updates. They route to the compiled provider-help,
test-execution and startup-help contracts, retaining authority, cleanup and evidence limits instead
of teaching retired wheel commands. Existing wheel assets remain unchanged. All 73 catalog skills
load from the generated package, and the four resource bodies match their canonical source bytes.
Build and focused guidance tests pass.

Actual compiled context routing against the authorized adopter produces a ready packet without
blockers, but selects architecture because unrelated dirty application paths dominate the scores.
This demonstrates corpus availability, not correct migration-task routing. The compiled command
currently lacks an explicit changed-path filter; adding bounded task-path selection is the next
identified routing gap. No optional JEV candidates were supplied and no provider call was needed.

### Explicit task paths for context routing

The context-route command now accepts up to 64 explicit repository-relative changed paths, validates
their syntax, and includes the sorted selection and selection mode in receipt identity. Only route
and attached-skill scoring use this scope; the captured Git subject and governance check scope remain
unchanged. This also permits guidance for planned paths without claiming they already exist.

The focused routing tests pass for parallel unrelated edits, unchanged subject identity, changed
receipt identity and traversal refusal. TypeScript/build pass. Actual adopter readback with the
bootstrap and CI paths now selects sdk-build-release and returns a ready packet without blockers,
instead of the architecture route selected from the whole dirty checkout. No provider call was used.

### CI evidence-path migration audit

A fresh bounded adopter reference audit found a remaining lane declaration pointing at the wheel's
in-runtime receipt location. The compiled checker writes run-identified terminal evidence outside the
checkout and returns its location. Command migration alone therefore does not establish CI evidence
readback. The coordinated candidate must capture the returned receipt and align the lane's declared
evidence before cutover; launcher-path compatibility does not preserve old receipt locations.
The external audit retains file hashes and individual references. Historical operation environment
paths are retained evidence, not executable dependencies to rewrite. No adopter files changed.

The external coordinated CI candidate now captures the compiled check's terminal JSON into the
project's declared build-report location, preserves check failures through shell pipefail, and
uploads the receipt on success or failure. Its lane declaration and focused contract agree on that
location. Four affected CI contracts pass; controlled successful and failed checker executions of
the actual candidate shell both retain exact receipt contents and their original exit status.
Patch applicability passes. This proves capture behavior, not remote artifact upload or full command
log retention; authoritative detailed run evidence remains in the engine's external run directory.
The candidate remains unapplied until the coordinated runtime cutover.


### Cross-provider legacy startup admission check

Activation now inspects executable hook commands in every provider settings file included in the
migration backup: Codex, Claude shared/local settings and Gemini. Previously the final admission
check only inspected Codex. A legacy startup observer in another provider could therefore survive
cutover despite the single-owner requirement. The replacement itself remains the exact qualified
Codex migration; other provider configurations are refused rather than silently rewritten.
Regression coverage checks all four files and ignores historical prose outside executable commands.
Two transition tests, seven installed-staging/hook tests and TypeScript checks pass. The authorized
adopter currently has legacy startup commands only in its Codex hooks. Full native delivery proof
and retirement of the unreferenced legacy installation remain outstanding.

### Consolidated installed migration checkpoint

A newly built preview archive incorporates task-path context routing, packaged skill/resource reads,
and cross-provider legacy hook refusal. Eighty-four installed-module tests pass against the extracted
compiled modules, including startup, migration, host instructions, context routing and skill reads.
The same fixture exercises actual managed-launcher init and post-write repair with replay, plus hook
installation and bounded child invocation. A separate bootstrap fixture rejects an incorrect archive
before state creation, preserves exact lock bytes, reuses the retained archive and reads digest-bound
packaged guidance without marking the generation written. Temporary installations were removed.

Archive SHA-256: `771d6a3c443962b0ad3232eb8b7e9222f27afdd4c286e67c444c804dff9c1a9a`.
This is a dirty-source preview qualification, not immutable release provenance. It does not establish
native event delivery, live remote CI, device requalification or adopter activation. Earlier native
trust observations remain unchanged; no live host probe was repeated in this checkpoint.

The physical RN qualification was resubmitted against this exact archive through the installed
preview's workflow modules. Device discovery confirms the selected iPhone connected; the previous
successful physical batch reports manifest-verified inputs and confirmed cleanup. The new batch
binds 320 input files, including all compiled JavaScript modules, and uses a distinct workflow
operation while retaining the canonical project scenario and cleanup assertions. Native completion
notification is attached to the initiating task. The new result is pending; submission is not device
qualification. A preparation-only path calculation error was corrected before submission, reusing
the already-staged verified archive; no device operation was launched by that failed preparation.

The new physical batch passed in 128.126 seconds with manifest-verified inputs and confirmed cleanup.
Canonical assertions observed both exercise transitions, three repetitions and completed results;
the launched app process was stopped and both engine resource claims were released with matching
cleanup evidence. This qualifies physical capture playback for this preview archive, not live camera,
acoustic or remote-upload behavior. Against the retained 82.239-second baseline the elapsed ratio is
1.558, above the declared 1.25 investigation threshold. Correctness passed; speed benefit is not
established and the timing difference requires phase-level reconciliation before benefit acceptance.

Timing reconciliation found the retained physical baseline measured dev-loop launch health and app
process presence only; it explicitly excluded functional scenario proof. The 1.558 elapsed ratio is
therefore not an equivalent-work regression comparison. The new run adds full capture playback and
assertions. The prior full-scenario package run took 121.120 seconds versus 128.126 now (1.058 ratio);
result waiting was 74.807 versus 74.369 seconds. The remaining 7.443-second difference lies outside
that wait but is not attributable to a specific phase from these totals alone. No repeat run was
launched to seek a better timing. Accepted-work benefit still requires a comparable workload or a
separately justified measured improvement. The completion notice hash matches the assessed result;
its duplicate delivery was reused without rerunning work.

### Simulator package requalification failure

The same preview's simulator scenario failed before launch: installed-tree identity capture detected
67 metadata changes during its bounded read. The batch's 722 declared input files remained valid;
the engine independently confirmed cleanup, released the resource and the simulator is shut down.
Reading the installed tree after shutdown is stable (402 files). This narrows the failure to installed
artifact observation but does not establish its cause or authorize ignoring metadata changes.
The canonical runner also attempted evidence collection before error-path termination; missing
pre-launch evidence prevented its termination call. The outer adapter shutdown still proved cleanup.
Investigate that ordering and retain detailed changed-field diagnostics before another qualification
attempt. The physical pass remains valid; this simulator checkpoint is failed, not qualified.

The authorized adopter's simulator runner now always attempts termination after diagnostic collection,
even when that collection fails. It retains the primary error, reports cleanup uncertainty separately,
and prints nested identity-change fields. Four behavioral cleanup combinations and four existing
bootstrap/permission contracts pass. A single changed-input diagnostic retry is running against the
same preview package; it is intended to expose the installation observation failure rather than
relax identity checks. No package source or identity acceptance rules changed.

The diagnostic retry passed in 102.121 seconds with manifest-verified inputs. Installed package
identity matched without relaxing checks; the two-exercise scenario completed with three repetitions,
and background/foreground evidence preserved the process and observed post-recovery events. App
termination and released simulator ownership are recorded. This reused the prepared simulator app
with full installed readback, not a fresh simulator build. The prior metadata instability did not
recur and its cause remains unproven; the successful retry must not be represented as fixing that
reliability issue. The cleanup fix has behavioral test coverage, while this live run exercised the
success path. Both run outcomes remain retained for the qualification assessment.

### Live simulator cancellation and retained-owner recovery

A deliberate cancellation after native app launch stopped the stage with confirmed command cleanup,
but the immediate device observer produced no release receipt. The workflow correctly remained
unknown and retained its simulator claim; the qualification driver failed rather than acknowledging
unproved cleanup. Later native readback showed the simulator shut down. Recovery verified the worker
fingerprint was absent, the stage was cancelled with confirmed cleanup, and the exact resource scope
was shut down; it persisted a bound receipt, released that lease generation and reconciled the same
workflow to cancelled. The enclosing batch cleanup acknowledgment followed those observations.
No command or scenario was replayed. Automatic cancellation-to-device-release remains a failing
qualification: investigate shutdown settling and the observer boundary before rerunning. This proves
explicit same-run recovery, not automatic cancellation completion.

### Project-declared graceful termination window

The live cancellation investigation exposed a fixed one-second TERM-to-KILL window, shorter than
some native device adapters need to finish their cleanup. Project operation definitions now accept
`terminationGraceMs` bounded to 1–30000 ms; omission preserves 1000 ms. The command owner and orphan
guardian honor the same declared value, and workflow observation includes that bounded interval.
This is captured operation authority, not a recipe-supplied override or inferred device cleanup.
A real child needing 1.5 seconds to clean up survives cancellation with a declared 3-second window,
produces its cleanup evidence and remains cancelled. Invalid values are refused before dispatch and
by catalog parsing. Sixteen relevant process/workflow tests and TypeScript checks pass. Installed
package and live simulator cancellation requalification remain required; this source result alone
does not close the failed device cancellation checkpoint.

The rebuilt package passed live simulator cancellation after confirmed native app launch with an
operation-declared 15000-ms termination grace. The workflow and stage ended cancelled, command cleanup
was confirmed, device shutdown was independently observed, and the exact resource lease was released
automatically. The enclosing qualification passed in 12.700 seconds with manifest-verified inputs.
No manual reconciliation or command replay was needed. This closes the observed automatic cancellation
cleanup case for the declared simulator operation; it does not qualify physical disconnect, every
project adapter or all grace values on native devices. The external assessment binds the archive,
result digest, launch PID, stage outcome and release receipt.

### Live simulator contention qualification

Two separately authorized task workflows selected the same simulator while the first app was
running. The contender became blocked before dispatch: all its stages were blocked without results,
and its worker command directory contained no command submissions. The original workflow then
cancelled and released the simulator automatically using the declared grace period. Inputs remained
manifest-verified and cleanup was confirmed. This qualifies live contention between distinct tasks
on the same host/package; cross-repository/version protocol cases retain their separate fixture proof.

### High-volume runner reliability scope

The operator explicitly prioritizes refactoring project runners where repeated dev-loop failures
justify the work. Include RN iOS launch readiness, fatal diagnostic observation, bounded known-fault
recovery, owned Metro replacement and phase evidence in the remaining adapter qualification. Reuse
existing project owners; do not create a second supervisor or assume embedded-bundle certification
qualifies interactive Metro/Fast Refresh behavior. Existing project code already contains Metro
classifiers and bounded restart/relaunch policies; qualify and repair those rather than describe
these capabilities as absent or rebuild them from scratch.

An initial recovery audit found persisted Metro PIDs could be signalled without the existing identity
check, and ownership could be cleared despite an unsuccessful exit wait. The authorized adopter now
uses its existing process/app/listener identity verifier before signalling a persisted owner, and
retains ownership when exit is unconfirmed. Live child handles retain their existing signal path.
Four ownership combinations and twelve existing recovery/orchestration tests pass. The existing
fixture now uses a controlled child instead of a fabricated persisted OS PID. This is focused source
proof, not live Metro recovery qualification.

Three installed process tests also pass for graceful cancellation, forced orphan cleanup and graceful
orphan cleanup. A recovered owner's successful child cleanup remains an unknown execution result;
it is never promoted to a successful command merely because resources stopped.

### Metro recovery ordering checkpoint

The existing launch lifecycle already probes post-launch health and checks recorded launch failures
before marking a launch successful; recovery awaits that lifecycle. The next runner correction moves
Watchman reset after verified owned-Metro shutdown. Previously an unconfirmed shutdown could still
alter project watch state before recovery stopped. Regression cases now prove that failed exit stops
before watch reset/replacement/relaunch and preserves the original failure, while confirmed exit
allows the ordered recovery. Eighteen focused recovery/orchestration tests pass. Direct execution was
chosen for this small deterministic Node batch; no device or model execution was needed. Interactive
Metro/Fast Refresh live qualification remains outstanding. Startup recrawl handling also has its own
one-shot watch reset; this checkpoint does not claim to have unified or qualified that separate path.

### Pre-launch Metro recovery convergence

The pre-launch health path independently signalled Metro and cleared ownership without verifying
exit. It now calls the same owned-process shutdown routine as runtime recovery. Thirty focused
health/recovery tests pass, including refusal to replace a process whose exit is unconfirmed.
A direct live qualification then started the actual sample Metro server on a dedicated port,
verified its HTTP health response, deliberately terminated that owned server, and exercised the
pre-launch recovery path. A different replacement PID answered the real health endpoint. Final
shutdown was verified and the isolated ownership map was empty. External evidence retains the
runner, log, result and source manifest. This short direct proof exercised real server process loss
and replacement without device mutation; it does not qualify bundle delivery, app rendering or
Fast Refresh. Those remain required interactive-runner proof.

### Canonical interactive RN iOS simulator proof

The canonical customer-sample dev-loop completed incremental dependency preflight, simulator
build/install/launch, fresh profile startup acknowledgment, and post-launch health checks. A captured
screenshot showed the rendered landing screen. A temporary JS-only module marker then arrived in
Metro's forwarded client log with the native app PID unchanged. The supervisor selected only
`js-refresh`, reusing Metro and native iOS. Restoring the exact original source generated a second
JS-only cycle. This proves runtime delivery without native reinstall; it does not assert React
component-state preservation or fault recovery. No sample product edit remains.

The interactive owner exited 130 after requested interruption. Separate cleanup terminated the
sample app and shut down the dedicated simulator; native readback confirmed shutdown, all recorded
Metro/app PIDs absent, and the dedicated port without a listener. External evidence contains the
startup receipt, before/after session logs, screenshot, temporary-source hashes, and assessment.
This was direct supervised execution because the canonical dev loop is persistent and terminal
controlled. No model worker was used. It reuses incremental SDK artifacts and is not a fresh
whole-toolchain build. Doubled terminal echo was observed, but two runtime-control registrations
are an intentional detach/reinstall around device selection; no unsupported duplicate-command
claim or speculative control change was made.

### Live JavaScript exception detection repair

A deliberate generic Error during simulator Fast Refresh exposed a false-green path: the sample's
fatal RN_CRASH marker and plain Error redbox were present in both captured streams, but the Metro
classifier missed them. The session retained no incident and the watch cycle reported completion.
The classifier now recognizes these two concrete client-log shapes after more specific transport
and watch signatures. A JS refresh with an already observed launch failure records a failed action
and rejects the cycle instead of emitting successful completion. Twenty-nine focused tests pass,
including informational-log negatives and specific-signature precedence from existing tests.

The same live fault was repeated through the canonical customer-sample simulator loop. Both exception
shapes produced retained incidents; the JS action failed, with no automatic recovery or successful
cycle emitted. Source was restored byte-for-byte. Session interruption, app termination, simulator
shutdown, absent owned PIDs and absent dedicated listener were verified. External evidence preserves
both the missed and detected incident sessions plus the assessment. The supervisor remains available
for diagnosis and explicit relaunch; it does not automatically rewrite code or restart the app.
This known-failure guard does not establish an acknowledgment for every asynchronous JS refresh,
and a retained exception requires a fresh verified launch before claiming recovery. Native crash
and physical-device fault qualification remain distinct.

### Consolidated runner checkpoint

All 315 dev-loop module tests pass after the ownership, shutdown-order, pre-launch health and
exception-classification changes. This consolidates the coherent runner batch; it is not broad
repository proof or a substitute for installed-engine qualification. The external checkpoint binds
the test log and current module source hashes. Its live evidence includes real Metro process-loss
replacement, canonical simulator app launch/readback, JS-only execution without native replacement,
and a repeated fatal-exception case that now records failure instead of a green watch cycle.
All temporary sample changes were restored and test-owned device/process cleanup was verified.
Remaining activation requirements still include C01–C18 clause closure, actual host hook activation,
coordinated legacy retirement/CI cutover and final installed-artifact proof. This checkpoint does
not close E5 or trigger the final independent review.

### Activation dependency recheck

The finalization entry point now independently rejects target checks and CI steps that still invoke
the retired wheel interpreter/bootstrap. Preparation already enforced this, but a resumed/direct
finalization must also enforce the current project boundary before reopening admission. A regression
introduces a wheel-dependent check after an initially clean dependency assessment and proves final
activation refuses it. Three focused dependency tests, the existing staged activation/recovery
integration test and TypeScript typecheck pass. Installed package proof predating this source change
must not be described as qualification of the new finalization guard.

### Compiled activation-recheck package proof

A newly built offline-dependency archive includes the finalization dependency recheck. Its SHA256 is
`4faaf4e7de3d213e7a67f567aea6b4af24670254d7395542ddc97a352ceaeb11`.
Eighty-seven tests run against the extracted compiled module paths pass, including the new
finalization dependency regression and startup/migration/backup lifecycle cases. Real installed
launcher initialization, replay and post-write repair/readback also pass. The isolated installation
was removed in finally; external evidence retains the archive, verifier, test log and proof record.
This dirty preview is not immutable release source. It does not activate the adopter or establish
native host event delivery, and earlier device proof needs an explicit applicability assessment
before being attached to this artifact.

### Device-evidence applicability for activation-recheck candidate

A complete archive-member content/mode comparison against the termination-grace package found only
`runtime-finalization.js` and its source map changed. Workflow/process/guardian/resource modules are
byte-identical. The original live cancellation and contention result hashes were reverified, so
those scoped claims remain applicable to this candidate's unchanged execution path. Their claim
remains cancellation/resource exclusion, not activation or full app acceptance. The earlier complete
simulator/physical scenarios predate the termination-grace execution changes and are not promoted
by this comparison. External package-delta and applicability records retain that distinction.

### Certification dependency-selection failure and early refusal

The current-package full simulator scenario failed; its input manifest became invalid when lifecycle
Xcode testing rebuilt a prepared app. Cleanup was confirmed, but no scenario proof is accepted.
Investigation confirmed the customer-sample preflight had selected a different shared React Native
Apple package mount than certification requires. The canonical read-only dependency checker rejected
that mismatch with exit 2. Certification `--skip-build` now calls that checker before installation,
preventing reuse from silently reaching an incompatible lifecycle build. Nine focused dependency and
cleanup tests pass. Next qualification must restore the certification-owned selection and rebuild
before binding prepared app bytes; changing hashes alone is not a fix. The failing job and full logs
are retained externally, and no duplicate was launched in response to its completion.

### Lifecycle build/execution separation

After restoring certification dependencies and rebuilding, scenario assertions passed but manifest
validation still rejected the run: the lifecycle `xcodebuild test` rebuilt/re-signed the prepared
app while it was under test. The failure remains invalid proof with confirmed cleanup. The project
runner now prepares lifecycle products with build-for-testing before installation on fresh builds,
and uses test-without-building for background/foreground execution. Skip-build retains its new
read-only dependency check and requires previously prepared test products. Syntax and nine focused
helper tests pass. A separate live batch now checks the unchanged-artifact scenario; success is not
claimed until manifest validation and cleanup also pass.

### Current-package simulator scenario accepted

The prepared-app simulator run with lifecycle test-without-building succeeded. All 724 declared
input hashes verified and cleanup was confirmed. The capture-playback scenario observed three reps,
both exercise transitions and rest; background/foreground preserved the native PID and subsequent
counter events were observed. The workflow completed and released its simulator resource claim.
The external assessment binds the current activation-recheck archive to the job/result identity and
lifecycle evidence. This closes the observed mid-scenario rebuild drift for that prepared-app lane;
it is not fresh-build-in-batch, live-camera, acoustic, physical-device or complete release proof.

### Current-package physical qualification: identity refusal

The current compiled candidate reached a fresh physical-device build but refused the packaged app
before installation because signature-file metadata changed during full-tree identity capture.
Inputs remained manifest-verified. This is a failed qualification, not device-scenario evidence.
The existing qualification app process was stopped by exact installed-app identity; the compiled
physical observer verified absence, resource claims were released, and the same workflow/job was
reconciled to failed with confirmed cleanup. Other app processes were left untouched.

A subsequent full-tree read was stable, which does not establish why the earlier read changed.
The project runner now retains exact before/after metadata values and prints nested error details.
Three identity tests pass, including mutation refusal. One diagnostic device batch is running with
newly bound inputs. No check was relaxed and no automatic identity retry was introduced. Physical
qualification remains open until scenario assertions and cleanup pass against the current artifact.

### Current-package physical scenario completed

The diagnostic physical run passed a fresh build, install, launch, two-exercise capture-playback
scenario and verified process cleanup through the current compiled archive. All 322 declared inputs
remained manifest-verified. Both resource claims were released with the compiled physical observer's
receipt. Scenario evidence includes both exercise transitions, the rest transition and three counter
events. End-to-end batch duration was 126.161 seconds; this is not an accepted-work speedup claim.

The prior metadata mutation did not recur. Its cause remains unproven; retain the refusal and improved
diagnostics rather than treating a later pass as a reliability fix. This closes current-package happy-path
physical scenario requalification only. It does not close the interruption/disconnect matrix, native
host activation, category migration, selected CI qualification, measured benefit or final review.

### Physical cleanup checks identity before signaling

Inspection of the physical runner found that its initial termination used a saved PID before
checking current identity. The project-owned cleanup path now reads the device process inventory
first, requires the live PID's executable to match its launch receipt, and refuses ambiguous or
changed identity without signaling. An already-absent original PID is not signaled; existing
identity-bound replacement-process cleanup and final absence verification remain in force.

Five focused tests pass, including stale-PID refusal with no termination call, absent original
process, verified replacement cleanup and unreadable inventory. A current-package physical batch
is running against the changed adapter. This is a pre-signal identity guard, not an atomic Apple
process handle: a device-side exit/reuse race between observation and signal is not ruled out.
The previous physical pass predates this adapter change and does not qualify its live behavior.

### Physical cleanup identity qualified; cancellation stage connected

The changed physical runner passed the complete capture-playback scenario in 128.206 seconds.
The before-signal inventory matched the actual launch PID and executable; final app absence and both
resource releases were confirmed. Manifest inputs remained valid. This qualifies the normal cleanup
path, not cancellation or device disconnection.

The physical adapter's host-process termination alone cannot close device effects. A project-owned
cleanup command now binds the launch receipt to the exact device, bundle and scenario run, then uses
the existing identity-checked app operations. Six focused tests pass, including mismatched receipt
refusal before any command and replay after process absence. A live workflow cancellation batch now
uses this operation as a declared cleanup stage under the existing engine lifecycle. No additional
supervisor or model-driven recovery loop was added. Its result remains pending.

### Known cancellation settlement preserves cleanup-stage execution

Live physical cancellation exposed an execution gap: a command with a recorded cancellation and
known native exit initially had uncertain descendant cleanup. The guardian later confirmed cleanup,
but settlement retained the unknown state and skipped the declared device cleanup stage. The failed
job was recovered separately through the receipt-bound project cleanup command and independent
physical readback. Resources were released; its unknown workflow evidence was not rewritten as a pass.

Settlement now preserves cancellation once independent cleanup is proven and native exit or signal
is known. It never turns cancellation into success or resolves a lost execution outcome merely from
process absence. Two source tests and the same two tests against an offline-staged compiled archive
pass, including original-receipt preservation and drift refusal. Typechecking/build pass. A fresh
physical cancellation test is running against that archive; installed live qualification is pending.

### Physical cancellation qualified on rebuilt archive

The rebuilt compiled archive passed cancellation after a real physical app launch. The main stage
reported cancelled, the declared device cleanup stage succeeded, and independent observation released
both app/device claims. Manifest inputs stayed valid; elapsed batch time was 52.303 seconds. No model
or manual device recovery was needed. This particular execution received direct cleanup confirmation;
the late-guardian settlement regression remains separately proven by packaged tests.

A bounded contention test is now running: while the first workflow owns the physical target, submit a
second authorized task against the same resources and require refusal before any command starts, then
cancel the first and verify cleanup. This qualifies same-host physical exclusion only, not a new
cross-version or cross-repository claim. Disconnect and owner-interruption proof remain open.

### Signature metadata stability and bounded identity confirmation

The first physical-contention attempt stopped before launch: the complete metadata diagnostic showed
only ctime changed on the signature resource file. Cleanup was confirmed and inputs stayed valid; no
contention claim was established. The file carries macOS provenance metadata, but the writer/cause
is not proven. Do not label it a verified operating-system defect.

The project identity reader now permits one complete confirmation capture for ctime-only drift.
The confirmation must itself be stable and have identical paths, kinds, bytes and permissions.
Content/topology changes, changed modes and repeated instability still fail. The evidence records
when confirmation was used. Six focused identity tests pass, including changed bytes between captures
and repeated metadata drift. This replaces the earlier no-recapture behavior with a bounded,
evidence-checked confirmation; it is not a build retry or an unconditional metadata exemption.
A new physical contention batch is running against the changed adapter.

### Physical contention qualified

The physical contention batch passed with manifest-verified inputs and confirmed cleanup. A second
authorized task targeting the occupied device/app was blocked with no command requests and no stage
results. The owning task then cancelled; its declared device cleanup stage succeeded and both claims
were released. Batch duration was 47.488 seconds. This proves two-task same-host/current-package
physical exclusion. It does not replace cross-repository/version protocol fixtures or prove device
disconnect, host loss or worker-interruption recovery. Those matrix cases remain open, alongside E5
category cutover, native host activation, selected CI qualification, measured benefit and final review.

### Worker-loss recovery gap: required next implementation

Source inspection found a missing E2/E4 recovery boundary. `observeWorkflow` can identify a stopped
worker, but workflow status reports the ledger and no public operation settles stages left running
when that worker dies. `workflow-reconcile-cleanup` requires already-settled stage results; command
owner recovery alone cannot fill that gap. Do not qualify worker-interruption recovery from process
guardian tests or normal cancellation.

Implement one bounded same-workflow recovery operation using the existing durable request, owner
acknowledgment, stage command directories and command reconciliation. Before any state change, verify
workspace/database/run/binding identity and original worker absence; refuse a live or ambiguous owner.
Bind each observed command request to its recipe stage and preserve original receipt bytes. Record
known terminal results without re-executing them. Missing reservation/receipt or unresolved process
effects remain unknown and retain the relevant resource obligations. Recovery must not infer success
from resource release or worker absence.

Separate observation recovery from dispatch of still-pending cleanup: any resumed cleanup must retain
exclusive workflow/resource ownership and prove the original command cannot still run. Never reuse
the normal queued-workflow submission path to replay an interrupted stage. Acceptance includes live
owner refusal, stale revision/binding refusal, known-result reconciliation, missing-receipt retention,
repeated recovery, concurrent observers and physical app cleanup after worker loss. Package proof and
the real physical lane follow focused source tests. This is unfinished implementation, not an extra
release gate or a change to the approved scope.

### Worker-recovery ledger transaction

The store now has an atomic observation-commit boundary for a stopped-worker verifier. It binds run
revision, exact stage snapshot, action/recipe binding and evidence digest. It refuses replacing
settled/unstarted stages, duplicate observations and unverified successful results. A partial invalid
observation set cannot modify earlier rows. Concurrent observers with stale snapshots cannot amend
the result. Recovery leaves the workflow unknown and grants no execution or resource-release authority.

Eleven workflow-store tests and TypeScript checking pass. This is an internal storage primitive, not
a public recovery command: the worker-absence/command-receipt verifier, safe pending-cleanup recovery,
CLI wiring, packaged qualification and physical interruption proof remain required. Do not invoke the
primitive as a substitute for those checks or claim end-to-end recovery from its fixture tests.

### Stopped-worker observation verifier and CLI

`workflow-recover-observation` now verifies worker absence from a complete process inventory, binds
the original worker request to database/run/recipe identity, and matches each command request to its
exact stage operation. It accepts only terminal command evidence with confirmed cleanup, preserves
receipt bytes, rechecks evidence before the atomic ledger update, and leaves unresolved stages and
resources unknown. It does not launch a replacement worker or command, signal a process or release
a device. Managed runtime invocation classifies it as a write because it records ledger evidence.

Thirteen store/recovery tests pass, including live-worker refusal, changed command binding, stale
revision, repeated current-revision observation, preserved receipts, concurrent stale observers and
missing command reservation without dispatch. TypeScript checking passes. An initial test exposed
canonical temporary-path aliases; comparison now uses the canonical command directory rather than
treating the macOS temporary-directory alias as a different worker. Public CLI/package execution and
pending-cleanup continuation remain unqualified; no live worker-interruption acceptance claim yet.

### Packaged stopped-worker observation command proof

A rebuilt archive was staged offline and passed thirteen recovery/store tests against compiled
modules, including real CLI invocation. The CLI refuses a live worker (invalid-operation exit 2),
records a known original native result after worker absence, and returns the unresolved workflow
(exit 1). Original receipt bytes remain unchanged. Missing reservations do not launch commands.
The same thirteen source tests pass. Temporary fixture installations were removed.

The owner-loss boundary in this proof uses a recorded exited fixture process, not a killed real
workflow supervising a physical app. Pending-cleanup continuation and physical worker interruption
remain required. No final artifact/device applicability or whole-runtime migration claim follows
from this focused package proof.

### Cleanup-only continuation execution seam

The ledger can reserve cleanup continuation against an exact unknown-run revision and stage snapshot.
It refuses running/unknown command stages or unconfirmed process cleanup. Ordinary pending stages
become blocked, settled results remain immutable, and only pending cleanup stages can execute. The
executor requires the original resource generations still be held by the original workflow owner; it
cannot reacquire released or replaced claims. Device release still requires independent readback.

Six executor tests and TypeScript checking pass, including preserved failed output, skipped ordinary
work, no original-command replay, stale continuation refusal and unresolved-command refusal before
dispatch. This is an internal seam. Public cleanup recovery still needs a durable recovery-worker
identity/fence so observation recovery cannot mistake an active continuation for the absent original
worker. Do not expose continuation or qualify live interruption until that ownership path is proven.

### Cleanup recovery worker identity and public command

Cleanup reservation now atomically records the recovery process identity in the existing event
ledger. Observation recovery consults the latest reservation and refuses any still-present PID,
including reuse, before recording another observation. This closes the gap between the absent original
worker and an active cleanup continuation without adding a second supervisor or store.

`workflow-resume-cleanup` connects observation recovery to the existing executor under original held
resource generations, then uses the same simulator/physical observers and retained-reader release.
The public source CLI test proves pending cleanup runs, failed work remains failed, ordinary work
stays blocked and the new worker identity is durable. Thirteen store tests, seven executor tests and
TypeScript checking pass. Compiled-package qualification and real physical worker interruption remain
required; fixture results do not establish those boundaries.

### Packaged cleanup continuation and live worker-loss submission

A newly built archive passed twenty tests against extracted compiled modules, including real CLI
observation and cleanup continuation. The public command preserves failed/cancelled outcomes, refuses
live original/recovery workers and skips ordinary pending work. Temporary fixture state was removed.

A physical batch now targets the real workflow worker after app launch: verify its fingerprint, stop
that exact worker, cancel/observe the original command, and invoke same-workflow cleanup continuation.
The batch requires confirmed app absence and release of the original resource generations. Its 725
manifest inputs include compiled runtime/asset files and current project runner inputs; source maps
were omitted to keep the request within the existing size bound. Result remains pending. This does
not establish host reboot, transport disconnect or automatic host recovery discovery.

### Physical workflow-worker interruption qualified

The live physical worker-loss batch passed in 50.242 seconds with all 725 declared inputs verified
and cleanup confirmed. After the app launched, the exact workflow worker was fingerprint-checked
and killed. Recovery cancelled and observed the original host command, then invoked the public
cleanup-continuation command against the same workflow. The main stage remained cancelled, pending
device cleanup succeeded, and both original resource generations were released. Exactly two command
requests exist: the original scenario and its cleanup; the original native receipt remains unchanged.

This run used delayed guardian cleanup recovery, providing live evidence for the cancellation
settlement fix in addition to fixture proof. It qualifies explicit same-host recovery while the wired
device remains connected. It does not prove transport disconnect, host reboot, automatic recovery
discovery, recovery-worker loss during cleanup or adopted-runtime generation handoff. Remaining E5
category cutover, native host activation, selected CI/benefit acceptance and final review stay open.

### Recovery-worker result reconciliation without replay

A focused public-CLI regression now covers an exited cleanup-worker identity with its cleanup stage
still marked running but an independently completed native command receipt. Recovery observes that
receipt, preserves the original failed assertion and native receipt bytes, and settles without
replaying cleanup or ordinary work. A post-cleanup sentinel and exact command-directory count detect
re-execution. All eight executor tests pass against source and the existing extracted compiled
archive; TypeScript checking passes. No runtime implementation or archive replacement was needed.

This is a process/ledger fixture with real command and CLI execution, not a second physical-device
interruption trial. Device transport disconnect/reconnect, adopted-generation handoff and the open E5
cutover/CI/benefit requirements remain separate. The previous goal turn made progress by qualifying
physical original-worker interruption; this checkpoint adds recovery-worker receipt handling coverage.

### Retired command dependencies checked before cutover

Cutover dependency inspection now uses one matcher for declared target-pack commands and CI run
steps. It rejects direct calls to the retiring provider launcher and legacy startup/bootstrap scripts,
in addition to wheel-owned Python. Previously, provider-launcher dependencies could pass preparation
and finalization even though the same transition removed that executable. Project-owned Python and
the retained unified launcher remain allowed. This does not claim transitive shell-wrapper discovery.

Three focused tests pass against source and a freshly staged offline compiled archive. Build and
TypeScript checking pass. The existing adopter remains on its wheel; no CI edits or runtime activation
were applied. Legacy script/environment retirement and coordinated category cutover remain open.

### Backed legacy startup/bootstrap retirement

Wheel transition now retires the two exact shipped startup/bootstrap scripts with the provider
launcher. It validates the entire present set against verified backup bytes, modes and known script
identities before deleting any file. Customized scripts refuse automatic retirement. Final admission
rejects remaining legacy entry points, and pre-write rollback restores retired scripts with original
permissions without overwriting unrelated content.

The integrated staging/transition/recovery test passes against source and a newly staged compiled
archive. It covers changed-script refusal before partial deletion, restored scripts and permissions,
then completed cutover with both scripts absent. Build and TypeScript checking pass. The installed
test initially exposed URL-path decoding in its own CLI fixture; using fileURLToPath fixed the encoded
scoped-package path, and the installed run passed. No adopter runtime was activated. Python environment
and external-store retirement, native host activation, category cutover and remaining qualification
requirements stay open.

### Public compiled continuity entry restored

C01/C16 inspection found the canonical continuity payload was packaged but not reachable through the
compiled public CLI. The unified `harness` namespace now calls that existing module directly for
retained task, context, checkpoint, artifact, budget, session/status, reconciliation, usage and history
operations. Importing the module no longer executes host arguments; standalone wheel invocation
retains its prior behavior. Managed invocation marks continuity access as a possible write before
dispatch because store/schema/session changes are possible even during observation.

A public CLI test creates, reads, resumes, checkpoints and exports across separate processes, checks
invalid-task exit behavior, and refuses legacy execution before creating a store. It passes against
source and the newly staged compiled archive. All 72 continuity tests, module/root typechecking and
build pass. Legacy execution adapter mapping and retained in-flight readback remain open; this does
not expose the old executor or establish complete C16 parity. No adopter activation occurred.

### Continuity parsing and help remain under one owner

The public continuity entry now passes its allowed command groups into the canonical parser instead
of inspecting the first argument independently. This preserves global flags before the command and
filters all help paths to the retained group set. Legacy execution/host commands are refused before
store creation; nested help no longer advertises them. Standalone current-wheel help remains unchanged.

The cross-process CLI test covers leading global flags and nested help as well as task/history flows,
and passes against source and a fresh offline installed archive. All 72 continuity tests, both
TypeScript checks and the build pass. This resolves the newly exposed routing discrepancy; it does not
close remaining legacy execution mapping or E5 activation gates.

### Read-only continuity migration inventory

Project migration discovery now inspects the standard continuity database without opening the mutating
store API. It reports exact-worktree task/action counts and bounded unresolved action bindings,
including prepared actions without job IDs. Absent stores remain absent; supported older schemas are
not upgraded, and unknown schemas refuse discovery. Shared-store tasks belonging to other worktrees
are not attributed to the selected adopter. Custom stores and actual process drain remain unverified.

The source inventory test and four migration-plan tests pass, as do the same five tests against a
newly staged compiled archive. TypeScript checking and build pass. Read-only inspection found no
standard continuity store in the authorized adopter; this says nothing about its explicitly located
preview ledgers. No store migration, command replay, runtime activation or process cleanup occurred.
The previous turn made progress by correcting compiled continuity routing; this checkpoint closes a
missing inventory observation, not the broader owner-drain/backup or C01/C16 acceptance gates.

### Existing preview workflow ledgers reconciled

A read-only inventory of existing test-job databases found 34 workflows for the authorized adopter:
32 terminal and two older unknown outcomes, with no resources currently held in the shared registry.
Both unknown ledgers were coherently backed up before invoking the compiled observation/reconciliation
commands. Original command receipts and their independently retained cleanup evidence resolved the
stage observations; exact workflow-bound release receipts then settled the workflows without dispatch.

The simulator run is failed because its inputs are now stale, and the physical interrupted run is
cancelled. Neither became successful. Original native receipt bytes remain unchanged. A fresh inventory
reports 14 succeeded, 10 failed, 7 cancelled and 3 blocked workflows, no unknown workflows and no held
resources. This closes these two historical preview records, not device transport/reboot qualification
or arbitrary custom-store discovery. The inventory covers 32 SQLite files in the test-job root and
excludes retained archive copies. No tests, app launches or cleanup commands were repeated.

The first administrative invocation lacked a revision in its compact audit projection and was safely
refused. Subsequent cleanup reconciliation initially received the outer batch summary rather than the
workflow-bound release receipt and was also refused. Supplying current revisions and the original bound
receipts completed reconciliation. These were audit-script input errors, not runtime acceptance bypasses.

### Development-loop benefit evidence boundary

Review of the frozen simulator/physical baseline criteria confirms they describe incremental launch
pilots with one sample and incomplete immutable input binding. Later E2/E4 functional qualification
adds different scenario and artifact work; their timings must not be compared as equivalent accepted
work or used to claim token savings. Whole-loop benefit acceptance remains open.

A bounded deterministic comparison of the original committed Metro classifier and current adopter
classifier now records exact source/case identities. The baseline recognized 6 of 9 labelled cases;
the candidate recognizes all 9, removes three missed RN error shapes and retains zero false alarms
on the three negative controls. Criteria were persisted after baseline evaluation and before candidate
replay. These known cases overlap regression tests and include log shapes from prior live fault
injection; they are not held-out accuracy evidence or a fresh device trial. The result supports the
specific failure-detection improvement only. No model calls or device/build work were required, and
elapsed/token/whole-loop benefits remain unmeasured. The comparison does not close E5 benefit acceptance.

### Linux installed-package isolation and Apple provider availability

The installed compiled package passed public task creation/resume/checkpoint/export and a bounded
isolation probe inside the existing Linux x86 container image. The image was selected by immutable
local image ID, with two CPUs, seven GiB memory, no network, no added capabilities, no Docker socket,
read-only package/test mounts and a bounded disposable temporary filesystem. The candidate could not
read an unmounted host publisher-canary file, receive selected publisher/package/model credentials,
or overwrite the mounted package. Both tests passed in an 8.759-second container invocation. Container
absence was confirmed afterward and the synthetic host canary was removed.

This qualifies the selected compiled continuity path and stated container controls. It does not prove
an existing adopter CI lane, hostile kernel resistance, a publisher broker, check publication, cold/warm
benefit or protected merge readback. No credential was injected and no remote check was published.

The adopter's native Apple provider doctor reports a degraded, non-ready slot: storage configuration,
image availability, guest profile qualification, runner authorization and service installation are
missing. Pinned Tart/softnet tools and host capacity checks pass even though those tools are not on the
shell's ordinary PATH. The provider's own diagnostic is therefore more accurate than command discovery.
No storage/image installation or GitHub routing mutation was performed. Apple VM and selected CI-lane
qualification remain open; this evidence does not narrow those requirements.

### Selected existing project check through Linux workflow execution

An existing fixed-catalog Apple preflight subcheck (portable simulator-selection contracts) passed
through the extracted compiled workflow executor in the Linux x86 image. The exact canonical test
command consumed a frozen two-file snapshot with declared hashes and no package/publisher credentials.
All three assertions passed. The workflow then completed declared container cleanup, independently
observed container absence and released its original resource claim. Frozen inputs remained unchanged;
the workflow duration was 1.373 seconds on the already available image, not a cold-build measurement.

The container retained the prior probe's no-network/read-only/no-capabilities controls. Its cleanup
command permits Docker's already-absent exit code, but resource release still requires independent
absence readback. The workflow API was invoked from the exact extracted package; this does not claim
a completed public CI-provider integration, whole preflight lane, fixed-row receipt validation,
publisher isolation/readback, cold/warm comparison or actual simulator execution within Linux.
No model management was needed between dispatch and cleanup, and no remote check was published.

### Existing local-CI publisher discovery boundary

Read-only remote configuration inspection found no repository-visible self-hosted runners or
rulesets. The main branch requires five checks bound to the existing Actions producer with strict
candidate freshness; protected release branches instead expose push restrictions without required
status checks in their branch-protection responses. No settings, checks or runner registrations were
changed. Exact remote identities and responses remain in external evidence.

This observation does not contradict the operator's report that local CI is authorized: an external
publisher, organization-level arrangement or separate delivery path has not yet been identified.
An asynchronous request asks for that existing integration's location. Until identified, preserve
current remote behavior and do not manufacture a publisher or treat the simple project receipt checker
(check ID, passed status, nonempty command list) as sufficient trusted publication validation.
The Linux selected-check proof remains execution evidence, not merge eligibility. Independent engine
migration work can continue while this integration discovery remains open.

### Consolidated engine regression checkpoint

The canonical full engine source suite passed: 385 tests, no failures or skips, in 20.070 seconds.
All 505 declared component/configuration inputs matched their pre-run hashes afterward. This integrates
recent continuity routing/inventory, legacy retirement, workflow recovery and existing provider/process
boundaries rather than relying only on individual focused proofs.

The initial external batch request was refused before submission because the adopter-installed wheel
was not the governance source checkout's destination runtime. No duplicate job was created. The
canonical checkout command then ran directly with a completion-aware wait and retained output. This
is source-suite evidence; it does not establish final installed-artifact applicability, native hook
activation, device transport recovery, complete category cutover, accepted-work benefit or CI publisher
qualification. The goal remains active and final Opus review remains gated on full completion.

### Public telemetry runtime comparison filter

C13/C16 command inspection found the native telemetry status command omitted the existing
`--runtime-version` filter despite recording runtime versions in terminal metrics. The native command
now accepts that filter and selects exact matching versions. Unfinished intents have no recorded
runtime version and are excluded from version-filtered results; unfiltered reporting retains their
explicit unfinished count. This avoids attributing unknown-version work to a selected release.

The focused telemetry test passes, including a different-version empty selection and matching-version
selection across stages. Root typecheck passes. This is source proof only; installed-command proof
belongs in the next package checkpoint. Review annotations and trigger filtering remain separate
public-command migration gaps, not implied complete by this change.

### Result-bound review annotations

The native `telemetry review --run-id --disposition` command now preserves the existing four
review dispositions: confirmed-issue, false-positive, mixed and unreviewed. It writes an advisory
annotation beside a retained native run, bound to its workspace, run ID and result digest. Updating
an annotation replaces the previous disposition without changing operational results. Status counts
annotations within the same selected run scope; stale or malformed annotations report invalid-review
without hiding the underlying run. Missing runs, invalid values and cross-workspace writes refuse.
Managed invocation classifies review as a write before dispatch so rollback cannot ignore it.

Focused telemetry coverage and root typecheck pass. The rolling metrics projection retains its
existing schema; annotations are read from the run records, not a second review database. This closes
the native annotation entry gap, not legacy-history import, trigger filtering or installed CLI proof.

### Compiled telemetry command qualification

Built and staged a fresh offline preview archive, then ran all three focused telemetry test files
against its compiled modules and executable: five tests passed. The public-command test exercises
review creation, filtered status readback, missing/invalid review arguments, and preservation of the
original failed result bytes. Projection tests retain their bounded storage, conflict and failure
isolation checks. Package identity and test output are retained externally. This qualifies the staged
preview telemetry command, not adopter activation or the remaining category/benefit gates.

### Check-origin telemetry across detached execution

Native checks accept `--trigger manual|hook|test`; direct CLI checks default to manual and the native
hook entry supplies hook. The detached dispatch binds the supplied origin into its request, queued
intent, running intent and terminal metric. The bounded projection accepts the optional field while
continuing to read prior records. Direct internal calls that omit origin remain unknown; historical
records are never relabeled. `telemetry status --trigger` validates and filters the recorded value,
excluding unknown origins from named-origin selections without changing unfiltered results.

Eight focused tests pass across native execution, detached-worker completion, telemetry CLI and
projection storage. Root typecheck passes. The detached-worker test confirms test-origin readback;
the execution test confirms hook-origin projection. This is source proof pending the next compiled
package checkpoint. Expected-status comparison and complete historical telemetry migration remain
separate from preserving check origins.

### Continuity-linked governance selection restored

C16 inspection confirmed that canonical `harness governance plan` delegates only to the public planner
and records a linked receipt; it does not depend on the retired execution adapter. The unified
namespace now exposes this existing group. Legacy `check` execution remains excluded. The focused
continuity CLI test creates a task, delegates a staged comparison to the canonical planner with its
source pack directory, checks the linked receipt and continues through resume/checkpoint/export.
It passes; root typecheck passes. Initial fixture failures exposed a missing comparison base and an
incorrect source-pack path, both corrected in the test fixture. This source integration proof does
not yet qualify the default installed launcher or complete C16 migration.

### Compiled public-command and detached-origin checkpoint

A fresh offline staged preview passes nine focused tests across continuity planning, telemetry,
check execution and detached-worker ownership. Task-linked planning now calls the actual compiled
public planner with packaged assets, both through an explicit executor and the conventional project
launcher location. The latter uses a temporary-project launcher; it does not imply live adopter
activation. The source continuity integration test and root typecheck also pass.

Origin metadata survives the compiled detached worker into terminal metrics. Older projection
records remain readable; review/status behavior and operational result preservation pass in the
same archive. Evidence, archive identity and qualification script remain external. This closes the
pending compiled proof for these command additions without claiming category-wide parity, native
hook delivery, measured workflow benefit or final device applicability.

### Expected-result observations preserved

Native checks now accept the existing `--expected-status passed|failed|warning|blocked` semantics
only with `--trigger test`. A shared observation-context validator applies before dispatch and again
at execution/projection boundaries. Detached request, queued/running intent and terminal metrics
retain the label. Telemetry reports matched, unexpected, unspecified and invalid expectation counts;
labels never change actual outcomes, process exit codes or task acceptance.

Nine focused tests and root typecheck pass. A detached run deliberately expects failure but passes:
its actual passed result remains intact and telemetry reports unexpected. A retained failed result
with a matching expected failure reports matched without becoming successful. Legacy metrics remain
unspecified. Invalid origins/statuses refuse. Compiled proof for this addition is pending the next
package checkpoint; previous archive qualification does not cover this source change.

### Public command gap inventory grounded in current parsers

The migration inventory now records the principal old-to-native command owners and unresolved
consumer differences. Inspection found that accepted summary flags are currently ignored, while
file-result output is absent; neither is marked as an accepted removal. Context and installation
commands have materially changed request contracts that need consumer conversion/readback.
Recent telemetry and continuity additions therefore do not establish full C16 completion.

Native hook delivery remains a separate external trust qualification gap. The next implementation
priority in this command audit is result consumption and required-context coverage, followed by
explicit installation/provider operation mappings. No live host trust or adopter installation was
changed during this audit.

### Compact result consumption restored

Native plan/check now honor `--summary` using compact JSON, consistent with the legacy summary
format's intent. Plans retain status, stage, selection order and blockers while counting rather than
copying changed paths. Completed checks retain actual outcome, termination, blocked dependencies,
nonpassing packs and active blocking/advisory findings. Command arguments, raw output and full source
inventories remain in the full result. Summary projection neither changes execution nor exit status.

Two focused tests pass for compact evidence preservation and parser selection; root typecheck passes.
Public compiled output proof is still pending. `--json-output` and incomplete/detached result
consumption remain separately open rather than inferred complete from terminal summarization.

### Full-result export and compiled result-consumption checkpoint

Foreground `check --json-output PATH` now atomically writes the full observed result independently
of stdout summary selection. An incomplete observation exports its explicitly unresolved envelope;
it never creates a terminal success. Detached export refuses before dispatch rather than publishing
a submission as a final result. Operational worker receipts remain the authority and are unchanged.
Summary finding/blocker messages are bounded to 500 characters.

A fresh staged archive passed the public CLI scenario: failing commit check, compact stdout/full-file
identity, unchanged worker receipt, exit code 1 and matched expected-failure telemetry. Plan summary
and detached-export refusal also passed. Eleven compiled focused tests cover continuity, telemetry,
observation context, summary and detached execution; root typecheck passes. Temporary project cleanup
completed. Evidence remains external; no adopter activation or broader E5 completion is implied.

### Configured JEV without credentials: routed-context proof

Extended the real context-route integration test with auto-mode JEV configuration and no token.
A network-call trap confirms zero calls. Mandatory file bytes and required packaged skills remain
present, optional context uses the missing-token fallback, and the packet remains ready. Environment
and fixture configuration are restored afterward. Both context-route tests pass against source and
the existing current compiled archive; root typecheck passes. No runtime source changed, so no new
archive was needed. This is fallback/required-context proof, not new JEV benefit evidence.

The command inventory now describes conversion from old routed context to `context-route`, including
revision provenance, staged selection, ready/blocker checks, and why `context-packet` alone cannot
replace required routing. Full legacy wire-field compatibility remains separately unclaimed.
