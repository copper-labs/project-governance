I'll do a read-only recheck directly — no plan file, no delegation, no writes.## Findings

**1. `cancelCheck` never attempts the F3 link recovery before refusing — Medium — `src/ops/execution.ts:291-295`**
`recoverExecutions` calls `recoverJobLink` first (`execution.ts:273`); `cancelCheck` does not. So the exact case F3 was built to survive — `submit` acknowledged, `linkJob` write failed, response durably in the `submission-response` event — hits `if (!binding?.jobId) throw "submission uncertain without linked job"`. Failure scenario: transient store failure during `linkJob`, real job running, operator runs `check cancel --action ID --authority-ref REF` and gets a refusal instead of a cancellation, with no hint that `harness recover` would repair the link and make cancel work. Remedy: `recoverJobLink(store, actionId)` as the first line after the action lookup. That keeps the deliberate refusal for genuinely unrecoverable submissions, which is the case worth refusing.

**2. The cancellation fix ships almost entirely untested — Medium — `test/execution.test.ts:42`, `test/helpers.ts:41`**
`FakeExecutor.cancel()` is never invoked by any test; `cancelCheck`'s dispatched branch (`execution.ts:296-300`) and the `check cancel` CLI verb (`cli.ts:248-252`) have zero coverage. The single cancel test exercises only the pre-dispatch local branch, and its title ("pending owner cleanup remains visible") claims coverage it does not contain. The other half of F6 — `listUnresolvedActions`' new `authorized`-with-execution clause (`store.ts:279`), which un-hides the invisible orphan — has no assertion anywhere either. Both are behaviors being signed off in this reconciliation. Remedy: extend the existing test to submit through `FakeExecutor`, cancel, assert `cancel` was called plus action `cancelled` and evidence `unconfirmed`; and assert that an authorized action holding a binding appears in `listUnresolvedActions`. (`cancelCheck` is also imported and unused at `test/execution.test.ts:26`.)

**3. A failure inside the new completion waiter aborts `check run` and drops the action/job identity — Low-Medium — `src/cli.ts:244`**
`GovernanceExecutor.wait` swallows only ETIMEDOUT and the cleanup `AgentError` (`execution.ts:73`). Upstream turns every other `AgentError`/`OSError` during `wait --until-terminal` into exit 1 + `{"error":…}` (`project-governance` `cli.py:139`), which `call()` rethrows; it propagates past `collectResult` to the top-level catch, emitting `{ok:false,error}` at exit 2 with no `actionId` and no `jobId` — for a batch already dispatched and running. The F1 fix is what widened this: the observer now blocks and polls for up to 30s instead of returning on the first event. Remedy: try/catch around `owner.wait`, carry the message as a `waitError` field, continue into `collectResult` so the pending payload still names the action, job and stage.

**4. A `status` failure turns a pending observation into an error — Low — `src/ops/execution.ts:243-247`**
The F15 fix calls `owner.status()` on every not-ready poll; if it throws or returns a mismatched `job_id`, `collectResult` throws rather than returning `pending: true`, so `check result` exits 2 ("blocked/refused/invalid/unconfirmed") for a job that is merely still running. No evidence is fabricated, so the direction is safe, but the exit code misreports a live job. Remedy: catch, return pending with `stage: null` plus a `statusError` note.

**5. `check cancel` exits 0 for outcomes the printed legend maps to 2 — Low — `src/cli.ts:251`**
Exit is `pending ? 3 : 0`, so a cancellation whose terminal receipt is unconfirmed (`state: "cancelled"`, `established: false`) exits 0 while `cli.ts:44` and `HOW-TO-USE.md:42` present "2 … unconfirmed" as global. Defensible — the verb succeeded — but undocumented. Remedy: one clause stating `check cancel` reports cancellation status, not the check verdict.

**6. One of the three exit-code descriptions is still unreconciled — Low — `docs/specs/execution.md:55`**
Reads "2 invalid/refused/blocked"; `cli.ts:44` and `HOW-TO-USE.md:42` both name `unconfirmed` explicitly. This was next-check #3 in the original review and is the only wording left out of step.

**7. `reconcile`'s declared-input comparison is unbounded, and compares blob bytes to worktree hashes — Low — `src/ops/continuity.ts:86-97`**
(a) One `git show` per manifest file per evidence item, no memoization, no cap on evidence count — while every neighbour is explicitly bounded (20 sources, 2000 manifest files, 8000-byte conflicts, 8 MiB tree inspection). Worst case is 20 × N × 2000 subprocess spawns inside one turn, on the tool whose pilot measures cost per accepted task. (b) `f.sha256` was computed by `fileDigest` over *worktree* bytes at submit (`execution.ts:133`), but the comparison hashes `git show` output, i.e. the stored blob. Under any eol/clean-smudge filter or LFS, every declared input mismatches and reports a false `stale` — conservative in direction, wrong in classification, and silent. Remedy: memoize by `${tree}:${path}`, cap total comparisons, and either use `git cat-file --filters` or state the filter limitation beside the `declared-inputs-match` label in `task.md`.

## Closure

**F1 and F2 are closed.** `wait` calls `--until-terminal` bounded by the observer's own spawn timeout, swallowing ETIMEDOUT and the cleanup `AgentError` only (`execution.ts:65-76`); the fixture owner actively rejects an event-stream wait (`test/cli-execution.test.ts:15`), so the flag cannot silently regress. `established` is now returned (`execution.ts:267`) and both check paths emit `pending?3:!established?2:passed?0:1` (`cli.ts:246`, `cli.ts:262`), asserted end to end in a fresh process across pass/fail/invalid/pending plus `timed_out`→2, against a fingerprint the fixture computes independently of `inputFingerprint`. Real source-CLI receipts agree: `check run --wait 5` → 0 and 1 directly (was 3/3), killed worker → 2 with `invalid-or-unverified`, withheld cleanup → 3 with stage then 0 after the resource-free acknowledgment.

**Also verified closed:** F3, F4, F5 (`task_path` dropped in V6; tests now exercise `report()`/`recordPaths`), F7, F8, F9, F10 (`touch` preserves the last explicit digest; regression asserts it), F11 (origin survives two fork generations), F14, F15, F16. F12's classification is implemented and tested — finding 7 is about its cost and filter edge, not its verdict. F6 is implemented correctly, including the refusal to locally cancel an unlinked uncertain job; I agree with that rejection — asserting local cancellation there would claim effects stopped without evidence — but findings 1 and 2 remain against it.

**F13:** I re-derived it independently. `JSON.stringify` → `parse` → `stringify` is order-stable for integer-like keys, including values above 2³² and non-canonical forms (`"01"`, `"-1"`, `"1.5"`). The original finding was wrong; keeping existing digest semantics with a regression test is the right call.

## Readiness

**Ready for a controlled provider-free pilot.** Nothing remaining can fabricate a passing result or distort the pilot's cost/outcome measurements; the two defects that gated it are closed against the real executor, not only the fake. I'd fix findings 1 and 3 first — both a few lines, both on the cancellation/recovery path a pilot will actually hit — and add finding 2's tests, since F6 is otherwise being signed off on unexecuted code. The stated boundaries stand unchanged: real-host and published-runtime qualification, unknown owner-state investigation, and the declared Node 22.18 minimum are open, not claimed.

No files were written or modified; no builds, installs, or subagents were run.
