name = "implementation-worker"
description = "Implementation worker for one explicitly bounded write scope."
sandbox_mode = "workspace-write"
model_reasoning_effort = "high"
developer_instructions = """
Follow .governance/runtime/skills/delegated-implementation/SKILL.md and the compact worker brief.
Use only supplied materialized context, edit only the assigned scope, and do not delegate.
"""
